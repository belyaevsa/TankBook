# CORPUS-2026-09-13 - register the owner's RN Tver-2 pair (pump-096, receipt-065)

**Scenarios: J3 · the receipt (the receipts class), J4 · the pump display (the pump class).**
Two photographs from the product owner, 2026-09-13, **already copied into the tree** by the
orchestrator (Telegram JPEGs, 1280 px, no EXIF - commit byte-for-byte, never re-encode):

- `Spike/ReceiptSpike/fixtures/pump/pump-096-tokheim-rn-tver-tc252-2000l-pair-ru.jpeg`
- `Spike/ReceiptSpike/fixtures/receipts/receipt-065-rn-tver-tc252-95firm-2000l-nonfiscal-terminal-slip-pair-ru.jpeg`

They are **one fill, two documents** - the same shape as `receipt-062`/`pump-085` and
`receipt-063`/`pump-086` (RN-Tver Chkalovskaya, 2026-09-11), from a different RN-Tver station.

## Ground truth - read by the orchestrator off the photographs (you cannot see them)

**pump-096** (Tokheim LCD, Cyrillic labels `Стоимость / Количество / Цена за 1 литр`, daylight
reflection across the glass, shot slightly from below):

| liters | unitPrice | total | fuelKind | currency |
|---|---|---|---|---|
| 20.00 | 71.30 | 1426.00 | (blank - a pump never states it) | RUB |

**receipt-065** (АО «РН-Тверь», АЗК Тверь-2 ТС252; a **non-fiscal PetrolPlus terminal slip**,
`******** Нефискальный отчет ********`, the same till family as 062/063; lying on denim, slightly
crumpled, sharp):

| liters | unitPrice | total | fuelKind | currency |
|---|---|---|---|---|
| 20.00 | 71.30 | 1426.00 | petrol95 | RUB |

The product line prints `АИ95фирм  20.00  1426.00`, `ИТОГО 1426.00`, and the reference block
`Цена за ед. 71.30` with the legend `1 ед.=1 литр для нефтепродуктов/СУГ` - the very legend that
made `receipt-062` commit `lpg` before RV.243. The date on the slip is 12/09/26 (terminal
12:00:33). No fiscal QR - there is no `.qr.txt` to add.

## What to do - the registration points, exactly as the last corpus commit (`4ad38dc6`) touched them

1. `receipts/expected.csv` and `pump/expected.csv`: one row each, the values above, in the
   folders' existing column format (see the 062/063 and 085/086 rows).
2. `swift run ReceiptSpike fixtures/receipts` and `swift run ReceiptSpike fixtures/pump` from
   `Spike/ReceiptSpike`: record what each new fixture scores, cell by cell. Do not tune the
   parser to these two files - a miss is a finding, recorded, not a fix (`receipts/README.md`:
   "Do not fix this by tightening the parser against these files").
3. `high-water.json` and `HIGH-WATER.md`: the receipts and pump totals grow by 5 and 3 cells;
   hits move by what the sweeps say. The ratchet guards absolute hits - a new fixture may only
   raise `total`; if `hits` would fall, that is a parser regression and you stop and report.
4. `ios/Tests/TankbookCoreTests/CorpusScorerFuelKindCurrencyTests.swift:127` - the row-count pin
   `64 + 95 + 3 + 8` becomes `65 + 96 + 3 + 8`.
5. `ios/Tests/TankbookCoreTests/CorpusCompressionTests.swift:46` - `recordedReceipts` (the
   compression-step floor) re-measured the way that test measures it; the floor moves with the
   corpus.
6. `PumpPhotoGate` constants (find them: `grep -rn "PumpPhotoGate" ios/Sources`) - the measured
   pump precision / coverage / recall re-recorded from the sweep. The mode stays off; nothing
   here turns it on.
7. `fixtures/README.md` counts; `receipts/README.md` and `pump/README.md` post-sweep addition
   lists - one entry each in the same voice as the 062/063 and 085/086 entries, naming the pair,
   the station, the surface, the reflection, and what the parser did with it.
8. `docs/EXTRACTION.md` "Measured reality" only if a headline number it quotes moved.

## Checks - verify by exit code, report every count

- `cd Spike/ReceiptSpike && swift test` - 0; report the two sweep lines verbatim.
- `scripts/gate.sh` from the repo ROOT - 0; report `swift test` count (the corpus suites
  `AccuracyRatchetTests`, `CorpusCompressionTests`, `CorpusScorerFuelKindCurrencyTests` run inside
  it) and the app-target bundle count.
- `recognised.csv` where a folder keeps one: regenerated, diff shown.

## Mutation - named

Break `receipt-065`'s `total` in `expected.csv` to `1425.00`; `AccuracyRatchetTests` must go red
on the receipts class (hits below the recorded mark) if the parser read the total, or the sweep's
cell must flip from hit to miss. Verbatim output, then restore.

## Vacuous trap

Registering the rows without running the sweeps and copying the previous high-water numbers
forward; re-encoding the JPEGs (the md5 must stay `1ca46d62cf6fdd2a1f0acef74d8d0a81` for the
pump and `06ffa48b67165d07b02994eb57960bb3` for the receipt).

## Report back

The two fixtures' per-cell results; every number that moved (before -> after); anything the
sweep found that is not a registration matter, with the row that owns it or a statement that
none does. No UI, no screenshots.
