# CORPUS-2026-09-13b - register the owner's Circle K Dresser Wayne set (pump-097..100, receipt-066)

**Scenarios: J3 · the receipt (the receipts class), J4 · the pump display (the pump class).**
Five iPhone photographs from the product owner, 2026-09-13, converted by the orchestrator from
HEIC to full-resolution JPEG (3024x4032, orientation baked in, EXIF and ICC stripped) and
**already copied into the tree** - commit byte-for-byte, never re-encode:

- `Spike/ReceiptSpike/fixtures/pump/pump-097-dresser-wayne-circlek-ee-2285l-1899.jpg`
- `Spike/ReceiptSpike/fixtures/pump/pump-098-dresser-wayne-circlek-ee-969l-2039.jpg`
- `Spike/ReceiptSpike/fixtures/pump/pump-099-dresser-wayne-circlek-ee-pump1-1064l-1899.jpg`
- `Spike/ReceiptSpike/fixtures/pump/pump-100-dresser-wayne-circlek-jarvevana-pump4-6404l-2024-pair-ee.jpg`
- `Spike/ReceiptSpike/fixtures/receipts/receipt-066-circlek-jarvevana-db0-pump4-6404l-pair-ee.jpg`

**`pump-100` and `receipt-066` are one fill** (Circle K Järvevana, Tallinn, pump 4) - the same
pair shape as `pump-095`/`receipt-064` (Circle K Peetri). This run follows directly on
`CORPUS-2026-09-13` (pump-096 / receipt-065), which is already registered; build on that tree.

## Ground truth - read by the orchestrator off the photographs (you cannot see them)

All four displays are the same Dresser Wayne Circle K Estonia face: `SUMMA` over `LIITRIT`
(`Vmin 5 LIITRIT`), a row of four price windows under grade badges, `HIND/1L`. The display
never names the grade dispensed; the unit price is the one price window the SUMMA/LIITRIT quotient
lands on. Every one has the photographer reflected in the LCD glass.

| fixture | liters | unitPrice | total | fuelKind | currency | notes |
|---|---|---|---|---|---|---|
| pump-097 | 22.85 | 1.899 | 43.39 | (blank) | EUR | price windows D 2.039 · 95+ 1.949 · 98+ 1.959 · 95 1.899; quotient → 1.899 |
| pump-098 | 9.69 | 2.039 | 19.76 | (blank) | EUR | windows D+ 2.139 · D 2.039 · 98+ 1.959 · 95 1.899 (leftmost partly cut); quotient → 2.039 |
| pump-099 | 10.64 | 1.899 | 20.21 | (blank) | EUR | pump number 1 on the red tile; windows D 2.039 · 95+ 1.949 · 98+ 1.959 · 95 1.899; quotient → 1.899 |
| pump-100 | 64.04 | 2.024 | 129.62 | (blank) | EUR | pump number 4; windows 95 1.884 · 98+ 1.944 · 95+ 1.934 · D 2.024 (the `D miles` window is the lit one); `SUMMA`/`HIND` labels cut at the right edge |

**receipt-066** (Circle K Järvevana teenindusjaam, Järvevana tee 2, Tallinn; held in a gloved
hand over the pump, the lower third curled and the card slip partly covered - the fuel block is
flat and sharp):

| liters | unitPrice | total | fuelKind | currency |
|---|---|---|---|---|
| 64.04 | 2.024 | 129.62 | diesel | EUR |

Lines: `D B0 miles  64,04L  129,62`, `Pump 4 Hind 2,024 EUR/L`, `EXTRA SOODUS -0,96 EUR`, `K O K K U
129,62`, `KÄIBEMAKSUTA 104,53`, `24% KM 25,09`, date `13/09/2026 15:32`, terminal T04, arve 0047,
and the footnote *Kütuse liitrihind kviitungil sisaldab allahindlust* (the printed per-litre
price already includes the discount - so 64.04 x 2.024 = 129.62 holds exactly and `-0,96` is
informational). No fiscal QR - no `.qr.txt`.

## What to do - the registration points, exactly as `4ad38dc6` and `CORPUS-2026-09-13` touched them

1. `pump/expected.csv` (+4 rows) and `receipts/expected.csv` (+1 row), the folders' column format.
2. `swift run ReceiptSpike fixtures/receipts` and `... fixtures/pump` from `Spike/ReceiptSpike`;
   record every new fixture cell by cell. Do not tune the parser to these files - a miss is a
   finding, recorded.
3. `high-water.json` + `HIGH-WATER.md`: receipts total +5, pump total +12; hits move by what the
   sweeps say - `total` may only rise, and if `hits` would fall you stop and report.
4. `CorpusScorerFuelKindCurrencyTests.swift` row-count pin: `65 + 96 + 3 + 8` → `66 + 100 + 3 + 8`
   (confirm the current values on the tree first - the previous run changed them).
5. `CorpusCompressionTests.swift` `recordedReceipts` re-measured the way that test measures it.
6. `PumpPhotoGate` constants re-recorded from the pump sweep. The mode stays off.
7. `fixtures/README.md` counts; `receipts/README.md` and `pump/README.md` post-sweep addition
   lists, one entry per fixture in the existing voice, naming the pair.
8. `docs/EXTRACTION.md` "Measured reality" only if a quoted headline number moved.

## Checks - verify by exit code, report every count

`cd Spike/ReceiptSpike && swift test` 0 (both sweep lines verbatim); `scripts/gate.sh` from the
repo ROOT 0 with the `swift test` count and the app-target count; `recognised.csv` regenerated
where a folder keeps one.

## Mutation - named

Break `receipt-066`'s `liters` in `expected.csv` to `64.40`; `AccuracyRatchetTests` goes red on
the receipts class if the parser read the litres, or the sweep's cell flips to a miss. Verbatim,
then restore.

## Vacuous trap

Copying the previous high-water numbers forward without running the sweeps; re-encoding the
JPEGs - the md5s must stay as the orchestrator records them in the dispatch commit.

## Report back

Per-cell results for the five fixtures; every number that moved (before → after); anything the
sweeps found that is not a registration matter, with the row that owns it or a statement that
none does. No UI, no screenshots.
