# CORPUS-2026-09-14 - register the owner's Gilbarco set (pump-101..105, receipt-067), an e-receipt (screenshot-009) and a parking invoice (expenses)

**Scenarios: J4 · the pump display (the pump class), J3 · the receipt (the receipts class), J5 ·
the RU receipt as an e-receipt (the screenshots class).** Seven files from the product owner,
2026-09-14, **already in the tree** - the six HEICs converted by the orchestrator to full-resolution
JPEG (3024x4032, orientation baked in, EXIF and ICC stripped), the e-receipt a PNG (624x1500).
Commit byte-for-byte, never re-encode. This run follows `CORPUS-2026-09-13b` (pump-100 /
receipt-066); build on that tree - the row-count pin there reads `66 + 100 + 3 + 8`.

- `fixtures/pump/pump-101-gilbarco-circlek-ee-1765l-2034-closeup.jpg`
- `fixtures/pump/pump-102-gilbarco-circlek-ee-1022l-1954.jpg`
- `fixtures/pump/pump-103-gilbarco-circlek-ee-4858l-1954.jpg`
- `fixtures/pump/pump-104-gilbarco-circlek-sikupilli-pump1-2650l-price-washed-pair-ee.jpg`
- `fixtures/pump/pump-105-gilbarco-circlek-ee-2555l-1899.jpg`
- `fixtures/receipts/receipt-067-circlek-sikupilli-95e0-pump1-2650l-pair-ee.jpg`
- `fixtures/screenshots/screenshot-009-lukoil-ekaterinburg-ai100-ru.png`
- `fixtures/expenses/parking-snabb-tallinn-et.png` (1488x1620, an emailed PDF invoice rendered to PNG)

**`pump-104` and `receipt-067` are one fill** (Circle K Sikupilli, pump 1, 14/09/2026 10:10) -
the same pair shape as `receipt-038`/`pump-019` (Sikupilli, 2026-08-27).

## Ground truth - read by the orchestrator off the photographs (you cannot see them)

The five displays are the Circle K Estonia **Gilbarco Veeder-Root** face the corpus already
holds (`pump-094`/`095`): a two-line LCD - money over litres, seven digits with leading zeros
(`0035,90` / `0017,65`) - `€` and `L` to the right on the wider crops, a separate `€/L` window
below, the "Circle K kütus tuleb Euroopast" roundel and the kiwa calibration seals at the left.

| fixture | liters | unitPrice | total | fuelKind | currency | notes |
|---|---|---|---|---|---|---|
| pump-101 | 17.65 | 2.034 | 35.90 | (blank) | EUR | tight close-up: LCD, `€/L` window and the Gilbarco mark only - no `€`/`L` labels in frame; photographer's reflection across the digits |
| pump-102 | 10.22 | 1.954 | 19.97 | (blank) | EUR | `€` and `L` labels visible; a `stop` button at the right |
| pump-103 | 48.58 | 1.954 | 94.93 | (blank) | EUR | same face, sharp |
| pump-104 | 26.50 | 1.949 | 51.65 | (blank) | EUR | **the `€/L` window is sun-washed to illegibility** - the price is from the paired receipt; a parser that reads a price here is guessing, `nil` is the honest answer. Total and litres are crisp |
| pump-105 | 25.55 | 1.899 | 48.52 | (blank) | EUR | glare across the top line; digits readable |

In every case `total / liters` lands exactly on the `€/L` window (`35.90 / 17.65 = 2.034`, and so
on), which is the oracle for each price.

**receipt-067** (Circle K Sikupilli teenindusjaam, Tartu mnt 86, Tallinn; held in a hand, the right
edge of the header and the card block under a thumb, the fuel block flat and sharp):

| liters | unitPrice | total | fuelKind | currency |
|---|---|---|---|---|
| 26.50 | 1.949 | 51.65 | petrol95 | EUR |

Lines: `95E0 miles+  26,50L  51,65  1`, `Pump 1 Hind 1,949 EUR/L`, `K O K K U 51,65`,
`KÄIBEMAKSUTA 41,65`, `24% KM 10,00`, `14/09/2026 10:10`, terminal T01, arve 0068. No discount
line, no fiscal QR. Same layout as `receipt-038` (which resolves through the inline
`Hind … EUR/L` form) - the expected outcome is 5/5.

**screenshot-009** - an electronic fiscal receipt (`Кассовый чек. Приход`) rendered by the OFD's
web page, light theme, no app chrome except the page itself; a `Проверить чек` button mid-page:

| liters | unitPrice | total | fuelKind | currency |
|---|---|---|---|---|
| 20.00 | 99.40 | 1988.00 | petrol100 | RUB |

Lines: `1. ТРК №5 Бензин автомобильный ЭКТО-100 (АИ-100-К5), л`, `20 x 99.40   1988.00`,
`Подакцизный товар   Полный расчет`, `НДС 22%`, `ИТОГО: 1988.00`, `НДС 22% 358.49`,
`Безналичными: 1988.00`, seller `ООО "ЛУКОЙЛ-Уралнефтепродукт"`, `АЗС 66 418`, Екатеринбург,
`14.09.2026 20:20`, ФД 10524, ФН 7380440903372785, ФПД 1842847803. Note the operand order is
`quantity x price` with an integer quantity (`20`), and the **22% VAT** rate - the corpus's RU
fixtures carry 20%; check the band/VAT logic does not treat 22% as a foreign marker. The fuel
kind is `АИ-100` (the corpus's second `АИ-100` after `receipt-0xx`; check the normaliser maps
`ЭКТО-100`/`АИ-100-К5` to `petrol100`).

**parking-snabb-tallinn-et** - a Snabb OÜ parking invoice (`Arve Nr. C2654962727`), the emailed
PDF rendered as an image: bilingual header (`Väljastamise kuupäev / Sale date: 14.09.2026`,
`Tasumisviis: Card`), a line-item table with columns `Nr. / Kirje / Kogus / Ühiku netohind / Kokku
ilma maksudeta / Maksu% / Maksu summa / Kokku maksudega`, and a large green `TASUTUD` (paid) stamp
across the totals block:

| category | total | currency | date |
|---|---|---|---|
| parking | 4.39 | EUR | 2026-09-14 |

Lines: `1  PARKING  10:51 14.09.2026 - 18:12 14.09.2026 Akadeemia tee 15b, Tallinn (X237); Sõiduki
numbrimärk: 004TXK  1  3,23  3,23  24  0,77  4,00`, `2  Snabbi teenustasu  1  0,31  0,31  24  0,08
0,39`, `Kokku  3,54  0,85  4,39`, `Kokku maksudega 4,39 EUR`, `Tasutud 4,39 EUR`, `Kokku tasuda 0,00
EUR`. **The total is 4,39** - what was paid, parking plus the 0,39 service fee - not the 4,00
parking line (the expense total is the grand total, unlike a mixed fuel receipt's fuel line); a
parser that reads `0,00` (`Kokku tasuda`, the balance due) or `4,00` is wrong. This is the
expense class's first invoice-shaped document and its first Estonian-only vocabulary beyond the
airport tickets (`PARKING`, `Sõiduki numbrimärk`, `TASUTUD`). The class scores the photograph
through Vision (`RV.278`): the `.txt` dump is regenerated with `--dump-text` and committed beside
it, never hand-written.

## What to do - the registration points, exactly as `CORPUS-2026-09-13b` touched them

1. `pump/expected.csv` (+5), `receipts/expected.csv` (+1), `screenshots/expected.csv` (+1),
   `expenses/expected.csv` (+1, the `filename,category,total,currency,date` format) - each
   folder's own column format.
2. `swift run ReceiptSpike fixtures/receipts`, `... fixtures/pump`, `... fixtures/screenshots`,
   `... fixtures/expenses` (and `--dump-text` for the new expense photo, per `expenses/README.md`)
   from `Spike/ReceiptSpike`; record every new fixture cell by cell. Do not tune the parser to
   these files - a miss is a finding, recorded.
3. `high-water.json` + `HIGH-WATER.md`: receipts total +5, pump total +15, screenshots total +5,
   expenses total +4 (kind, total, currency, date);
   hits by what the sweeps say - `total` may only rise; if `hits` would fall, stop and report.
4. `CorpusScorerFuelKindCurrencyTests.swift` row-count pin `66 + 100 + 3 + 8` → `67 + 105 + 3 + 9`
   (confirm the current line first).
5. `CorpusCompressionTests.swift` `recordedReceipts` re-measured the way that test measures it.
6. `PumpPhotoGate` constants re-recorded from the pump sweep. The mode stays off. **Watch
   `pump-104`**: a committed price there is a confident-wrong value; if the parser commits one,
   say so - it is precision, the number the gate exists for.
7. `fixtures/README.md` counts; `receipts/`, `pump/`, `screenshots/` README post-sweep entries in
   the existing voice, naming the pair and the washed window; the post-sweep declaration lists
   (`PostSweepCorpusAdditions`, which `CORPUS-2026-09-13` learned the gate enforces).
8. `docs/EXTRACTION.md` "Measured reality" only if a quoted headline number moved.

## Checks - verify by exit code, report every count

`cd Spike/ReceiptSpike && swift test` 0 (the three sweep lines verbatim); `scripts/gate.sh` from
the repo ROOT 0 with the `swift test` count and the app-target count; `recognised.csv`
regenerated where a folder keeps one (`expenses/` does - diff shown).

## Mutation - named

Break `receipt-067`'s `unitPrice` in `expected.csv` to `1.994`; `AccuracyRatchetTests` goes red on
the receipts class if the parser read the price (it should - `receipt-038` does), or the cell
flips to a miss. Verbatim, then restore.

## Vacuous trap

Copying the previous high-water numbers forward without the sweeps; re-encoding any file - the
md5s are recorded in the dispatch commit; scoring `pump-104`'s price as a hit because the ground
truth says 1.949 while the parser committed a digit soup that happens to round there.

## Report back

Per-cell results for the seven fixtures; every number that moved (before → after); anything the
sweeps found that is not a registration matter, with the row that owns it or a statement that
none does. No UI, no screenshots.
