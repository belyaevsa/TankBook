# Task P1.10 – Trends tile grid + honest labels + excluded-entry footnote

Repo: `/Users/sbelyaev/repos/fuel-counter-ios`. Work directly in the checkout on `main`.
No git worktrees. Do not commit.

Trends is where the app either tells the truth about how much it knows, or quietly implies more
confidence than the data supports. **The labels are the feature.** Replacing the Trends tab placeholder.

---

## READ THIS BEFORE YOUR FIRST TOOL CALL

**Write ONLY inside `/Users/sbelyaev/repos/fuel-counter-ios`.** Relative paths. Anything outside the repo
is outside the sandbox. Scratch goes in `./scratch/` and is deleted before you finish.

**Write code first, explore second.** Create files early with real content, then refine.

**Do not restructure P1.1–P1.9.** Fill in `TrendsRootView` in
`ios/App/Sources/Navigation/TabRootsRootContent.swift`. Do not redesign navigation or Home.

---

## What exists

- `ios/Sources/TankbookCore/Consumption/` – **the engine already computes everything you display**:
  segments, the 90-day/floor-3 headline, distance weighting, lifetime, cost/km, EV. **Call it. Compute
  nothing in the UI** (hard rule 2).
- `ios/Sources/TankbookCore/Validation/` – the conflict flags behind the excluded-entry count.
- `ios/App/Sources/Home/HomeSections.swift` – `HomeVitalsRow` and the existing "1 entry excluded"
  footnote. **The tile and footnote components should be shared with Home, not duplicated** – Home shows
  three vitals, Trends shows the grid, and they must never disagree about a number or its label.
- `-seedHome*` / `-presentScreen` launch arguments – extend them for the states below.

## Read before writing

1. `CLAUDE.md` – hard rules. **2** (stats derived), **5** (tokens; `taillight` fuel, `headlight`
   electric), **6** (numbers in DIN, `tabular-nums`), **7** (every message names its next step),
   **10** (EN + RU), **14** (baseline gate). Plus the screenshot convention: **EN and RU**.
2. **`design/screens/Trends*.dc.html`** (and `LightTrends.dc.html`) – the artboards.
3. **`docs/SCHEMA.md` → the consumption block** – the normative label rule, quoted below.
4. `docs/ERRORS.md` → **Trends** – two states, copy verbatim.
5. `docs/DESIGN.md` → "Trends (chosen: tile grid)": four sparkline stat tiles (consumption, cost/km,
   monthly spend, price/L), the EV-vs-petrol comparison card, insight cards below. Thin `inkSoft` grid,
   `taillight`/`headlight` series only, **no chart junk**.

## The honest-label rule – normative, quoted

From `docs/SCHEMA.md`:

```
HEADLINE   headline(segments, window = 90 days, floor = 3):
             take segments closing within `window` of today
             if count < floor: take the `floor` most recent segments overall (window extends)
             value = Σ liters / Σ km × 100        // distance-weighted, not mean of per100s
             label = honest span: "last 3 months" / "last 5 months" / "first estimate · N fill cycles"
```

**The label is not decoration.** It states what the number is actually made of:

- Enough segments inside the window → `"last 3 months"`.
- Fewer than the floor, so the window silently extended to reach three → the label must **say so**:
  `"last 5 months"`, not "last 3 months". A number computed over five months labelled as three is a lie
  the user cannot detect.
- Below the floor entirely → `"first estimate · N fill cycles"`. Say it is provisional.

And from `docs/ERRORS.md` → Trends, verbatim:

| Condition | Shows | Next step |
|---|---|---|
| Entries excluded (conflicts/duplicates) | Footnote "N entries excluded" (real plural rules, EN + RU) | Tap → the flagged entry |
| Below data floor | Honest label: "first estimate · 1 fill cycle" / extended window "last 5 months" | Keep logging; label explains itself |

The footnote count is **derived from the engine's flags**, never hard-coded, and **needs real plural
rules** – Russian has three forms, so `1 запись`, `2 записи`, `5 записей`.

## What to build

### 1. The tile grid
Four sparkline stat tiles per `DESIGN.md`: **consumption, cost/km, monthly spend, price/L**. Each carries
its value in DIN and its honest label. Sparklines use `taillight` for fuel and `headlight` for electric –
one accent per region, no gradients, thin `inkSoft` grid.

### 2. Honest labels, everywhere a figure appears
Implement the label rule in `TankbookCore` beside the headline computation, so Home and Trends get the
identical string from one place. A tile with **nothing honest to show is omitted**, exactly as Home does
– no "N/A", no "–", no "0.0".

### 3. The excluded-entry footnote
Real count from the engine, real plurals, and **tappable → the flagged entry** (rule 7: names its next
step).

### 4. Cost/km is all-in
From `SCHEMA.md`: `Σ homeAmount of ALL entry types in window / km in window` – fuel, charging, service
and expenses, not just fuel. A cost/km that only counts fuel is wrong and looks plausible, which is the
dangerous combination.

## Out of scope – do NOT build

- Anomaly insight cards and the monthly summary (P6), the EV-vs-petrol household comparison card **if**
  it needs data the engine does not yet produce – say so rather than faking it.
- Car switcher (P1.11), Vehicle detail (P1.12), capture, real sync, anything in `Catalog/`.

## Tests

**L1 (`ios/Tests/TankbookCoreTests/`, macOS, no simulator)** – the label logic is the deliverable:
- **Enough segments in window** → `"last 3 months"`.
- **Window extends to reach the floor** → the label reports the **actual** span ("last 5 months"), and a
  test asserts it is NOT "last 3 months". This is the lie the rule exists to prevent.
- **Below the floor** → `"first estimate · N fill cycles"`, with N correct and singular/plural right at
  N = 1.
- **Distance-weighted, not mean-of-per100s**: a case where the two differ, asserting the weighted value.
- **Cost/km includes every entry type** – add a service record and the figure moves.
- **Excluded count** matches the engine's flag count exactly, including zero (footnote absent, not "0").
- **Tiles omit rather than fabricate** when a figure is unavailable.
- The D1–D4 goldens still pass unchanged.

**L4 (`ios/App/UITests/`)**:
- The "first estimate" state renders with its label; the extended-window state renders with the longer
  span.
- The excluded footnote shows the real count and routes to the flagged entry.
- No tile shows "N/A", "–" or "0.0" in a data-poor state.

Read the **current** baselines before starting and report them; both must rise. `#expect(true)`, or
asserting a label exists without asserting its *text*, counts as not done – the wording is the feature here.

## Definition of done – the baseline gate is part of this

- `xcodegen generate`; `xcodebuild -project Tankbook.xcodeproj -scheme Tankbook -destination 'platform=iOS Simulator,name=iPhone 17' build` – no errors.
- `cd ios && swift build` clean; `swift test` green and above the baseline you measured.
- UI tests **actually executed** against a booted simulator. Say plainly whether you ran them.
- **`swiftlint lint` from the REPO ROOT exits 0.** Check `echo $?`. Zero errors. Never loosen a rule to pass.
- Deployment target stays **18.0**; simulator is iOS 26.5. Guard any post-18 API with `@available`.
- **Screenshots, EN and RU**: `design/screenshots/P1.10-trends.png` and `-ru.png`, **plus**
  `P1.10-trends-first-estimate.png` (the data-poor state – the one most likely to be wrong and the one no
  test can judge). Dark theme, **outside** a test run. **Seeds are idempotent** – pass
  `-homeResetDatabase` alongside the seed. RU: `-AppleLanguages "(ru)" -AppleLocale ru_RU`.
- Nothing written outside the repo; `./scratch/` deleted if created.
- En-dashes, never em-dashes.
- Tick **P1.10** in `docs/TASKS.md`. Leave other rows alone.

## Report back

What you implemented, **the exact label strings you produce for each of the three cases**, whether Home
and Trends share one implementation of the tile and footnote, the exact `swift test` count and its
baseline, the UI test count and whether you executed them, the `swiftlint lint` exit code, and anything
you could not compute honestly from the engine. Do not claim a test passed if you did not run it.
