# Task P2.1b (finish) – two defects in the owned tab bar, nothing else

Repo: **`/Users/sbelyaev/repos/fuel-counter-ios-p2.1b`** – a git worktree on branch
`p2.1b-wire-capture`. Work in it. Do not commit. Do not read, write or `cd` into another
checkout. Another agent is working in `...-p2.5` right now.

**Never `pgrep -f` / `pkill -f` for a build or test** – an agent's brief is in its command line,
so that pattern matches sibling agents and killing the match destroys their work. Use
`pgrep -x xcodebuild`. Your simulator is **`iPhone 17 Pro`** and no other.

## Read this first: the design is DONE. Do not redesign it.

A previous run built the owned tab bar and **it works**: `AppTabBar.swift` exists, the system bar
is hidden with `.toolbar(.hidden, for: .tabBar)`, the bar is attached via
`.safeAreaInset(edge: .bottom)`, and **12 of 13 Capture UI tests pass** – including
`testCenterButtonReachesCaptureWithoutLaunchArgument`, the test this whole task exists for.

That run was then killed for thrashing: it spent an hour adding and removing `print` statements
against failing tests without converging. **You are not repeating it.** Two specific defects
remain. Fix those, leave everything else alone, and stop.

If you find yourself adding a third `print` to the same test, stop and write down what you
actually know in your report instead. A precise "I could not fix this and here is the evidence"
is worth more than another hour of guessing.

---

## Defect 1 – `testBothDoorsSideBySideFromHome` CRASHES the app

Not an assertion failure: the app exits unexpectedly and XCUITest restarts it
("Restarting after unexpected exit, crash, or test timeout").

The test exercises **hard rule 15**: from Home, "Type it" reaches the manual form, **and** the
capture button reaches Capture, in one run. The likely cause is a presentation conflict – a
`fullScreenCover` presented while a sheet is still presented or mid-dismissal, or the tab-bounce
`onChange` re-entering. Diagnose it properly rather than reordering the test until it passes:
**a test rewritten to avoid the crash is the bug shipping.**

Both doors must genuinely work in one session, because that is the rule.

## Defect 2 – nested bottom safe-area insets

The owned bar is attached with `.safeAreaInset(edge: .bottom)`. **Five screens already use the
same mechanism for their save bars**: `ManualFillUpView`, `AddVehicleView`, `TrendsView`,
`VehicleDetailView`, `EditEntryView`. Stacking a second bottom inset on top of those changes
their layout, and that is what broke these:

- `EditEntryUITests.testToastShowsOldToNewAfterEditThatMovesConsumption`
- `VehicleDetailUITests.testEveryCatalogDerivedFieldIsReachableAndEditable`

They passed before this change, so they are a regression, not pre-existing flakiness. Work out
what the extra inset does to those screens and fix **the layout**, not the tests. Reducing an
assertion or adding a sleep is not a fix.

Note the app-wide bar should not apply inside a modal cover or sheet that already owns its own
bottom bar – if that is the cleanest fix, it is a legitimate one.

## Also clean up before you finish
- A `DEBUG ` `print` remains in `ios/App/UITests/VehicleDetailUITests.swift`. Remove every one.
- `./scratch/` must not exist.

## Out of scope – do not touch
The bar's visual design, the capture button's appearance, `.toolbar(.hidden,...)` vs any
alternative, the Capture screen's internals, `ConfirmManual/` (another task owns it), the corpus,
backend. **Do not replace `TabView`** – keeping it is what preserves each tab's `NavigationStack`
state, and that was decided.

## Tests
`swift test` is **437** on main. The UI suite must be **fully green** on `iPhone 17 Pro`:

1. `testBothDoorsSideBySideFromHome` passes **without crashing** and without being weakened.
2. `EditEntryUITests` and `VehicleDetailUITests` pass in full.
3. `testCenterButtonReachesCaptureWithoutLaunchArgument` still passes, still with **no launch
   argument** – if that regresses, everything else is moot.
4. Tab state still survives a switch (push in Log, go to Trends, come back, it is still there) –
   the property that keeping `TabView` buys.
5. Run the **whole** UI suite, not just the ones you touched, and report the real total.

## Screenshots
Re-capture `P1.1-shell-dark`, `P1.4-home`, `P1.4-home-ru` with
`scripts/capture-screenshots.sh` once green. It resolves this checkout's own build; do not
hand-pick a DerivedData path. **You cannot see them** – say so in your report.

## Definition of done
`swift build` 0 · `swift test` green · `xcodegen generate` + simulator build · **the whole UI
suite green** · `swiftlint lint` from the REPO ROOT exit 0, no new warnings against the 268
baseline · localization gate exit 0 · no `DEBUG` prints, no `scratch/` · en-dashes only ·
`docs/TASKS.md` ticks P2.1b and drops the "NOT reachable in a Release build" note from P2.1.

## Report back
The **cause** of the crash in defect 1, in one sentence – not just that you fixed it. What the
nested inset was doing in defect 2. The full UI-test count. `swiftlint` and gate exit codes.
Which screenshots you captured and that you could not see them. Anything you could not fix, with
the evidence you gathered.

Do not claim a test you did not write, and do not call a suite green unless you ran it.
