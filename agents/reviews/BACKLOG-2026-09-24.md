# Backlog review - 2026-09-24

Read-only pass over every OPEN (`[ ]`/`[~]`/`[!]`) row in `docs/TASKS.md` whose id is not `PU.*`.
Scope: RV, AG, PR, PJ, AD, SH, T = 89 indexed rows (one extra unindexed row, PJ.11b, flagged at the
end). Each verdict verified against the tree, not the row's prose.

## Verdicts

### SH

| ID | Verdict | Evidence |
|---|---|---|
| SH.2 | KEEP | `scripts/release.sh` exists; open: first real archive + CI re-enable (`gh workflow enable`). |
| SH.5 | KEEP | Beta app built; `deploy-blue-green.sh:132` defaults `Auth__AppleAudiences__1` to the beta id. Open: first `--upload --beta` + SH.6 push. |
| SH.6 | KEEP | `ApnsClient.cs:56` still sends one `_options.Topic`; `:99` still reads `DeviceTokenNotForTopic` as dead token. |
| SH.7 | KEEP | `release.sh --beta` + `EXPERIMENTS` condition shipped; open: owner promotion decision. |
| SH.3 | KEEP | Deprecated as tracked (owner's own device walk); kept deliberately as the checklist. |

### PR

| ID | Verdict | Evidence |
|---|---|---|
| PR.3c | KEEP | `[v1.1]`, owner-deferred; the `configPollInterval` key still in no bundled doc/seed. |
| PR.15 | KEEP | `[v1.x]`; no migration-safety copy exists; mandatory before a v1.1 migration. |
| PR.19 | KEEP | `[v1.0.x]`; no CI secret-scan step in `backend.yml`/`ios.yml`. |
| PR.21 | KEEP | `[v1.0.x]`; retention literals still scattered (no `RetentionPolicy.days`). |
| PR.22 | KEEP | `[v1.0.x]`; limits still hand-kept, no tier parity test. |
| PR.23 | KEEP | `[v1.0.x]`; three bundled config copies still differ. |
| PR.24 | KEEP | `[v1.0.x]`; ~15 `Extraction/` literals still inline, no named struct. |
| PR.25 | KEEP | `[v1.0.x]`; no `Idempotency-Key` on `/import/parse`, `/auth/session`, `/extract`. |
| PR.26 | KEEP | `[v1.0.x]`; no `-frozenNow` / `-loadArchive` launch seams. |
| PR.27 | KEEP | `[v1.1]`; no slow-query Npgsql threshold. |
| PR.29 | KEEP | `[v1.1]`; signed-out device id still in `UserDefaults`. |
| PR.30 | KEEP | `[v1.1]`; reminder fire hours still compiled, no user setting. |
| PR.31 | KEEP | `[v1.1]`; `freeTierLimit` not config-served. |
| PR.32 | KEEP | `HostAllowlist.swift:25-36` still says "PLACEHOLDER - this domain is not registered yet" though `tankbook.live` is live (W0). |
| PR.33 | KEEP | `[v1.0.x]`; 90-day window, `paceLimitKmPerDay`, `WireFormat` literals still unnamed. |
| PR.36 | DEFER | `[v1.0.x]`; waits on an iOS 18 simulator runtime being installed (row's own note). |

### T

| ID | Verdict | Evidence |
|---|---|---|
| T.3 | KEEP | Flaky `ConfirmOdometerPrefillUITests`; no fix evidence; test still 5 s timed predicate. |

### PJ

| ID | Verdict | Evidence |
|---|---|---|
| PJ.16 | KEEP | `[v1.1]`; no torch / luminance hint state machine exists. |
| PJ.18 | KEEP | `[v2]`; `GatewayScanSession` still collapses transport failure to `.answered`. |
| PJ.32 | KEEP | `[v1.x]`; `AnomalyInsightStore` still `UserDefaults`-only. |
| PJ.37 | KEEP | `[v1.1]`; no PDF dossier path. |
| PJ.41 | KEEP | `[v1.x]`; no "Add expense from this receipt" action on Edit entry. |
| PJ.42 | KEEP | `[v1.x]`; no demo-receipt door. |
| PJ.43 | KEEP | `[v1.x]`; two ERRORS.md rows still unrendered. |
| PJ.46 | KEEP | `[v2]`; decide-or-drop (`GET /sync/snapshot`), owner decision pending. |
| PJ.49 | KEEP | `[v2]`; `CaptureMode.modes(for: .ev)` still `[.service, .expense]`, no charge path. |
| PJ.52 | KEEP | `[v2]`; parts shelf (bought-then-fitted) not built. |
| PJ.53 | KEEP | `[v1.x]`; no widget extension target or App Intent. |
| PJ.54 | KEEP | `[v1.x]`; no disconnect-triggered log offer. |
| PJ.300 | KEEP | decide-or-drop; `MoneyBackfillService` still skips `ServiceItem.cost`; owner picks (a)/(b). |

### RV

| ID | Verdict | Evidence |
|---|---|---|
| RV.205 | DEFER | `[!]`; needs owner photographs (parking/toll/wash/insurance/fine) before expense-kind is measurable. |
| RV.225 | KEEP | `[v1.0.x]`; no offline flag on `capture.pipeline`. |
| RV.232 | KEEP | `[v2]`; owner-deferred; no cloud-OCR opt-out / silent queue. |
| RV.233 | KEEP | `[v1.0.x]`; no abandoned-scan event; fold into RV.225. |
| RV.236 | KEEP | `[v1.x]`; no share extension for charging screenshots. |
| RV.237 | KEEP | `[v1.x]`; no two-car cost comparison chart. |
| RV.238 | KEEP | `[v1.x]`; `Tariff` entity still created/read by nothing. |
| RV.242 | KEEP | `FieldWriterScanner.swift:408` still matches `field.path.joined(".")` (name-based); `Expense.recurrence` example now moot (dropped, `Migrations.swift:100`). |
| RV.252 | KEEP | `[v1.0.x]`; `ios.yml` still repeats the gate by hand. |
| RV.254 | KEEP | `[v1.0.x]`; no `spanDays` on the `/v1/rates/pack` `net.request` line (only domain `Headline.spanDays`). |
| RV.262 | KEEP | `scenario-index.py` has no rows-since-last-walk warning. |
| RV.266 | KEEP | `[v1.0.x]`; `capture.pipeline` has entry-level bool, no per-field list / OCR version. |
| RV.203 | KEEP | `SyncWriteTriggerTests.swift:29-37` now polls (`waitUntil`), but still times out under load (RV.306: 124 s); fences confirm the flake persists. |
| RV.210 | STALE | Premise resolved: `partNumber` has an editor (`ServiceEntrySections.swift:393`) and JOURNEYS.md:338/348 now attributes lifetime→PJ.22, partNumber→PJ.61. |
| RV.148 | KEEP | `[v1.1]`; owner-deferred (option 3); monthly push still sums a partial month. |
| RV.168 | KEEP | Process; `TEMPLATE.md` still lacks the two proof rules. |
| RV.169 | KEEP | No source-scan guard over `Money` snapshot-field assignment. |
| RV.181 | KEEP | Code landed: `SharePresenter` presents from top-most controller + `ShareOutcome` log; `[~]` for the owner's device confirmation (unblocks J8b/J13). |
| RV.175 | KEEP | About still has no screenshot scroll hook. |
| RV.194 | KEEP | 138 reconstructed capture lines still unverified. |
| RV.129 | KEEP | `SignInRouter.swift`, `ImportDateFormat`, `TireSetFormState.hasEdits()`/`ReminderFormState.hasEdits()` all still present. |
| RV.130 | KEEP | `[v1.0.x]`; `unused_declaration` analyzer not in CI. |
| RV.124 | KEEP | `[v1.1]`; tyre-change advisory not built. |
| RV.123 | KEEP | `[v2]`; car transfer loop not built. |
| RV.109 | KEEP | 51 s of `Thread.sleep` still in the three UI tests. |
| RV.283 | KEEP | Owner decision pending on which corpus receipts flip classification. |
| RV.287 | KEEP | `[v1.0.x]`; RU diagnostics title still truncates. |
| RV.289 | KEEP | Needs the owner's `llm_calls` ledger row pasted in before a brief; pump prompt still the receipt vocabulary. |
| RV.290 | KEEP | Kind is now known (`CapturePipeline.swift:63` sets `resolvedSource = .pump`), but the inbox card copy is still "receipt". |
| RV.295 | KEEP | iOS 27 device/simulator parser run not done; decides whether the parser needs work pre-floor. |
| RV.306 | KEEP | New (2026-09-24); `e5rtError` Vision contention + `SyncWriteTrigger` timeout; needs serialisation/retry decision. |

### AD

| ID | Verdict | Evidence |
|---|---|---|
| AD.2 | KEEP | Fresh (2026-09-24); "Send this scan" case packaging not built. |
| AD.3 | KEEP | Fresh; `POST /cases` not built. |
| AD.4 | KEEP | Fresh; admin viewer not built. |
| AD.5 | KEEP | Fresh; ledger read not built. |
| AD.6 | KEEP | Fresh; attachments read not built. |
| AD.7 | KEEP | Fresh; privacy-policy wording not yet owner-signed. |

### AG (all `[v2]`, all fresh)

| ID | Verdict | Evidence |
|---|---|---|
| AG.1 | KEEP | Decision record; quota + policy sentence not yet owner-signed. |
| AG.2 | KEEP | No fifth tab slot. |
| AG.3 | KEEP | No Ask screen. |
| AG.4 | KEEP | No `AgentSession` / read tools. |
| AG.5 | KEEP | No `/agent/turn`. |
| AG.6 | KEEP | No thread UI. |
| AG.7 | KEEP | No draft tools. |
| AG.8 | KEEP | No consent surface. |
| AG.9 | KEEP | No invoice-through-agent. |
| AG.10 | KEEP | No diagnosis surface. |
| AG.11 | KEEP | No answer gate. |
| AG.12 | KEEP | No paywall/entitlement. |
| AG.13 | KEEP | No per-release model re-check. |
| AG.14 | KEEP | No grounding/citation surface. |
| AG.15 | KEEP | Pricing policy not written. |
| AG.16 | KEEP | No example answers. |
| AG.17 | KEEP | Safety stance not written. |

## Counts

| Verdict | Count |
|---|---|
| KEEP | 86 |
| STALE | 1 (RV.210) |
| DEFER | 2 (PR.36, RV.205) |
| DONE | 0 |
| DUPLICATE | 0 |
| UNCLEAR | 0 (indexed) |

## Row rot found (outside the 89)

- **PJ.11b** (`docs/TASKS.md:914`) is an open finding (PJ.11's archive-import stamp coverage) with **no
  status checkbox and no index entry** - the index lists PJ.11 `[x]` but not PJ.11b. Needs either a
  `[ ]`/`[~]` marker + index rebuild, or closing. Verdict if counted: UNCLEAR (unindexed, no owner
  decision on whether it still wants doing).
- The "v1.1 priority queue" table (`docs/TASKS.md:745-757`) still shows PJ.28/22/24/23/26/27/25/19/
  34/35/45 as `[ ]`, but the index records every one `[x]` in TASKS-DONE - stale prose, not open work.

## Ten rows that most need the owner's attention

1. **RV.210 (STALE - close it).** `partNumber` now has an editor (`ServiceEntrySections.swift:393`)
   and JOURNEYS.md already attributes lifetime→PJ.22, partNumber→PJ.61. The drift the row was filed
   for is gone; the row is a ghost.
2. **RV.181 (`[~]`).** The share fix is fully coded (`SharePresenter` top-most presentation +
   `ShareOutcome`). Only the owner's one device step (share a photo + export + read the diagnostics
   preview) stands between it and close - and it unblocks J8b and J13.
3. **RV.295.** The app ships on iOS 27's Vision, whose macOS-27 proxy reads the corpus worse than the
   26 marks. A device/simulator run decides whether the parser needs real work before the floor. One
   measurement, launch-relevant.
4. **RV.205 (`[!]`).** Needs the owner to photograph a handful each of parking / toll / wash /
   insurance / fine receipts (RU + EN). Until then expense-kind recognition cannot be measured at all.
5. **RV.289.** The row explicitly asks the owner to paste the `llm_calls` ledger row for the pump
   misread. Without it the brief can't distinguish a prompt bug from a client decode bug.
6. **RV.283.** Changing the cross-check's discount pairing reclassifies some discount receipts; the
   owner must name which corpus receipts flip before anyone touches the ladder.
7. **PJ.300.** Decide (a) fold `ServiceItem.cost` into backfill/re-home or (b) drop its home side.
   Until then a rate-pending service item silently disagrees with its record after a re-home.
8. **RV.290.** PU.29 now makes the capture kind known (`CapturePipeline.swift:63`), so the inbox's
   "receipt" copy can finally be split - but the pump reader still ships off, so the copy reaches no
   user until the PU.6 ship decision. Owner: does the pump classification ship, or should this wait?
9. **RV.306 (new).** The gate goes red roughly half the time on `e5rtError` from parallel Vision
   suites, plus `SyncWriteTriggerTests` timing out at 124 s. It is the current top cause of false
   reds; needs the serialise-or-retry decision made and built.
10. **SH.6.** The beta app syncs without its push nudge: `ApnsClient.cs:56` still sends one topic and
    `:99` discards the beta device's token as dead. The smallest of the release-tooling gaps, but it is
    the one that makes beta sync testing lie.
