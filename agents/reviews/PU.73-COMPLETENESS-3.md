# PU.73 completeness review 3

Run 2026-09-24 against the uncommitted PU.73 diff. Scope is the ML files and PU.73 row named in `agents/briefs/REVIEW-COMPLETE-PU.73-3.md`; PU.72 `temperature.py` and concurrent PU.74/78/81 work are outside the diff. Read-only except this review. I ran `.venv/bin/python -m pytest -q` in `ml/pump-reader`: **exit 0, 64 passed in 14.03 s**. I did not run a Swift build or `PumpReadTool`; the four supplied tier logs are the app-path measurements. Review 2 verified the earlier closures in `agents/reviews/PU.73-COMPLETENESS-2.md:30-41,107-181,205-236`; this pass checks its four remaining text items against the current tree and rechecks the F2/F3 decision.

## Third-pass text checks

1. **MET.** `docs/EXTRACTION.md:1136-1144` now says the linear probe bounds *linear transfer* near 0.63, distinguishes the note's inferred ceiling near 0.75 for any loss/head, reports flatten at 0.726, and says nothing is near the 0.9 needed for the hard dp check. These match `agents/research/PU.73.md:35-46,60-66` and `/tmp/agentlogs/pu73-round-b-dpauc.log:2`.
2. **MET.** The row points to “PU.82 below” at `docs/TASKS.md:1079`; that row exists at `docs/TASKS.md:1084`. The dangling “R1 below” pointer is gone.
3. **MET.** `docs/TASKS.md:1079` attributes 0.653 to the build-commit re-measurement on the re-cut slices, and distinguishes the note's 0.657 on its earlier 873-cell cut (`agents/research/PU.73.md:24-29`). Focal s1 is 0.681 in both the row and `ml/pump-reader/REPORT.md:2271`, matching 0.6805 rounded to three decimals (`/tmp/agentlogs/pu73-round-a-dpauc.log:8`).
4. **MET.** `docs/TASKS.md:1079` explicitly records that A2(ii)'s per-bit-alpha focal variant was not built and the focal run used no alpha (`agents/research/PU.73.md:336`; `ml/pump-reader/src/pump_reader/train.py:135-150,183-186`). It also records that the note's `--dp-crop off` pin could not run as written: `train.py` has `gap|none`, and the existing off-framed pool supplied framing (`agents/research/PU.73.md:308-315`; `train.py:172-176`). The vocabulary repair is filed at `docs/TASKS.md:1085`. The step-0 hand-box re-export was also skipped and explicitly assigned to the owner's PU.82 decision (`docs/TASKS.md:1079,1084`); all arms used `.out/real` against a same-pool control.

## Seven completeness items

1. **Fidelity — MET.** Defaults keep unit dp weight, gamma 0, no alpha, and the GAP head (`train.py:135-150,183-186`; `model.py:43-54`); the BCE equivalence is pinned at `tests/test_segment_loss.py:6-15,44-47`. The shipped *loss/head* recipe is reproduced; the row discloses today's renderer difference from the shipped run (`docs/TASKS.md:1079`). Flatten and CoordConv follow the note's layouts (`model.py:48-65`; `agents/research/PU.73.md:252-263`). The mean rather than positive-target reduction departs from Lin et al. §4.1 and the note's A6; the measured claim is expressly limited to mean reduction in `docs/TASKS.md:1079`, `ml/pump-reader/REPORT.md:2271`, and `docs/EXTRACTION.md:1140`. The unrun A2(ii), step 0, and dp-crop pin are now disclosed at `docs/TASKS.md:1079` with follow-up rows; no result is claimed for them.
2. **App-path wiring — MET for a no-ship row.** No candidate replaced the app resource; the phone continues through `PumpDisplayCapture.classify` (`ios/Sources/TankbookCore/Extraction/PumpReader/PumpDisplayCapture.swift:302-317`) and the capture path named at `ios/Sources/TankbookCore/Config/PumpPhotoGate.swift:69-76`, in both configurations. The candidate tier logs explicitly exercise “the app's classify” (`/tmp/agentlogs/pu73-tiers-flatten-s2.log:17`). The checkpoint writes its head at `train.py:286-288`; production ML loaders read it at `export.py:48-50` and `score.py:336-338`; the adjacent PU.72 loader has the same read at `temperature.py:147-148`. The legacy fallback is GAP, matching `model.py:43`.
3. **App-path measurement — MET.** The heldout dp-AUC logs score 864 cells per Round-A run (`/tmp/agentlogs/pu73-round-a-dpauc.log:1-9`) and record Round-B's flatten 0.7316/0.7259/0.7214 (`/tmp/agentlogs/pu73-round-b-dpauc.log:1-3`); the row names 5-crop TTA and the 184-window/183-positive re-cut population (`docs/TASKS.md:1079`). The app-path tier logs report control 42/42 live and 111/109 annotated (`/tmp/agentlogs/pu73-tiers-control-s0.log:10,22`); flatten s0 44/42 and 116/114 (`/tmp/agentlogs/pu73-tiers-flatten-s0.log:10,22`), s1 43/40 and 107/106 (`/tmp/agentlogs/pu73-tiers-flatten-s1.log:13,24`), s2 56/54 and 123/121 (`/tmp/agentlogs/pu73-tiers-flatten-s2.log:9,17`). Wilson intervals sit beside these estimates in the following log lines, for example s2 live `[0.8788, 0.9902]` (`/tmp/agentlogs/pu73-tiers-flatten-s2.log:18`). The logs use 181 scored cells and flag the drift against `PumpPhotoGate`'s 183 (`/tmp/agentlogs/pu73-tiers-flatten-s2.log:55-56`); the row discloses it (`docs/TASKS.md:1079`). No candidate is committed as the new app model, so the shipped-model ratchet remains 47/47 live of 183 (`ios/Sources/TankbookCore/Config/PumpPhotoGate.swift:86-95`).
4. **No regression elsewhere — MET.** F2 treats the Round-A and CoordConv AUCs as inside the control's three-seed spread (`agents/research/PU.73.md:433-436`; `/tmp/agentlogs/pu73-round-a-dpauc.log:1-9`; `/tmp/agentlogs/pu73-round-b-dpauc.log:4-6`). Flatten's AUC gain fails F3: every scored seed adds wrong readings, including s2's 56/54 live and 123/121 annotated (`agents/research/PU.73.md:437-438`; `/tmp/agentlogs/pu73-tiers-flatten-s2.log:9,14-18`). `docs/TASKS.md:1079` and `docs/EXTRACTION.md:1141-1144` explicitly refuse shipment. Thus the receipt route, annotated floor, law ratchet and hot path do not change in this row. Package sizes are stated at `docs/TASKS.md:1079`; Release latency is correctly marked immaterial because no new model or Swift hot path ships there.
5. **Tests that fail on mutation — MET.** Loss and default tests are at `tests/test_segment_loss.py:6-47`; head tests at `tests/test_model.py:20-83`. The supplied mutation logs show red for M4-M7, then the restored suite passes 64 tests. Their named failures, verbatim:

   ```text
   FAILED tests/test_segment_loss.py::test_dp_pos_weight_vector_weights_only_the_dp_bit
   1 failed, 4 passed in 0.44s
   FAILED tests/test_model.py::test_coord_head_really_adds_coordinates - Asserti...
   1 failed, 4 passed in 0.45s
   FAILED tests/test_model.py::test_flatten_head_keeps_the_layout - assert torch...
   1 failed, 4 passed in 0.44s
   FAILED tests/test_segment_loss.py::test_cli_defaults_are_the_shipped_training
   1 failed, 4 passed in 0.43s
   ```

   Sources: `/tmp/agentlogs/pu73-mutation-M4.log:16-17`, `M5.log:35-36`, `M6.log:18-19`, `M7.log:16-17`. Review 2 records M1-M3 at `agents/reviews/PU.73-COMPLETENESS-2.md:165-181`.
6. **Docs reconciled — MET.** The four requested corrections above are present (`docs/EXTRACTION.md:1136-1144`; `docs/TASKS.md:1079`). `ml/pump-reader/REPORT.md:2261-2277` carries the round, and the 15 per-run JSON metrics are under `ml/pump-reader/runs/2026-09-24/pu73-metrics/` (review 2 verified byte identity at `agents/reviews/PU.73-COMPLETENESS-2.md:37`). PU.82/83/84 record the remaining decisions (`docs/TASKS.md:1084-1086`). No changed user-facing behavior calls for an ERRORS or JOURNEYS edit. The task-id comments in ML code follow that package's existing convention, as the brief expressly notes (`train.py:177`; `model.py:36`).
7. **Every Checks-cell sentence — MET.** `docs/TASKS.md:1079` has the controlled loss and head rounds, 3 seeds and dp AUC; the row states why F2-failed arms were not tier-scored, lists both-tier results for the control and flatten seeds, records candidate temperatures and zero digit flips (`/tmp/agentlogs/pu73-temperatures.log:1-4`), and gives sizes plus the no-ship latency reason. It states the live-up/zero-wrong gate and refuses all candidates under F3. Its result cell also records the unrun hand-box pool, A6 reduction, A2(ii), dp-crop pin, archived metrics, and PU.82/83/84 ownership. The flatten-s2 re-score after PU.74 is explicitly assigned there (`docs/TASKS.md:1079`).

## Verdict

**COMPLETE.** Items 1-7 are MET. The orchestrator may commit PU.73's scoped artifacts. No PU.73 gap remains unowned: the hand-box pool is PU.82, CLI framing vocabulary PU.83, inference-time gap framing PU.84, and flatten-s2 re-score PU.74 (`docs/TASKS.md:1079,1084-1086`).
