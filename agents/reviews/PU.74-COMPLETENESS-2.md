# PU.74 completeness review, pass 2 — 2026-09-24

Scope: PU.74 only. I inspected the working tree and supplied logs without building, running `PumpReadTool`, or changing code. I independently recounted `pump/windows.json`, `expected.csv`, and `split.csv` with the §5.2 rule: reviewed train and heldout stills; nonempty transaction windows; digit counts from displayed text; nonzero, parseable, non-`csvDisagrees` assertions for reproducing placements (`agents/research/PU.74.md:252-284`). This gave 324 stills, 920 digit-bearing windows, and RUB total placements 1 (19 windows) and 2 (77). The corpus test's 932 includes heldout2, which my note-rule recount excludes (`ios/Tests/TankbookCoreTests/PumpDisplayConventionsCorpusTests.swift:21-35`). Every coded read-placement **set** and, for currencies with at least three stills, every coded count **set** matches my recount (`ios/Sources/TankbookCore/Extraction/PumpReader/PumpReadingTypes.swift:251-276`). KGS is a retained video convention; the note's still population does not measure it (`agents/research/PU.74.md:151-164`; `PumpReadingTypes.swift:264-265`). Auditing counts only for currencies with at least three stills is a conservative reading of A8: a single observed head cannot bound a currency (`agents/research/PU.74.md:221-227`; `PumpReadingTypes.swift:217-220`).

## Seven checks

1. **Fidelity — MISSING.** The correction to RUB total is present, the unmeasured-currency refusal and read-placement sets implement A2-A5, and derived totals remain separate (`PumpReadingTypes.swift:259-280`; `PumpReadingLaw.swift:51-52,72-81`). The count constraint still runs **after** `candidates` has generated all digit strings, in `resolve` and `resolveWithoutPrice`, rather than making an inadmissible window yield no candidate (`PumpReadingLaw.swift:72-96,214-219,384-417`). The note explicitly locates both constraints in `candidates` and calls for `[]` on an impossible count (`agents/research/PU.74.md:139-151`); its A8 justifies a window-level audit but does not name this placement as a departure (`agents/research/PU.74.md:221-227`). This differs from the per-step mask in Willard & Louf §2.2-§3, the mapped published method. It is semantically defensible because count is fixed before decoding, but the brief requires an unlisted departure to be MISSING. Have the owner approve it or amend the note with the reason and align the Checks cell. The paper mapping itself is sound: Hokamp & Liu's Grid Beam Search handles lexical subsequences, while Willard & Louf's finite-state masks handle format languages ([ACL paper](https://aclanthology.org/P17-1141/), [FSM paper](https://arxiv.org/abs/2307.09702); `agents/research/PU.74.md:119-151`).

2. **App wiring — MET.** `CapturePipeline.readPumpDisplay` calls `PumpDisplayCapture.classify`, which reads the verified windows through `PumpReader.resolve` and `PumpReadingLaw.resolve`; there is no `#if DEBUG` guard on this chain (`ios/App/Sources/Capture/CapturePipeline.swift:126-134`; `ios/Sources/TankbookCore/Extraction/PumpReader/PumpDisplayCapture.swift:317-328,370-390`; `ios/Sources/TankbookCore/Extraction/PumpReader/PumpReader.swift:553-560`). The new reasons stay in the core `PumpDisplayReading`; the app composes only `Reading.extraction` and `law.caution` into prefill (`PumpDisplayCapture.swift:57-66`; `CapturePipeline.swift:78-100`). I found no payload or API mapping of `.currencyUnmeasured` or `.cellCountImpossible`, consistent with hard rules 9 and 16 (`PumpReadingTypes.swift:54-61`).

3. **App-path measurement — PARTIAL.** The second live run passed: annotated 123/123 of 183, Wilson precision interval [0.9697, 1]; `classify` live 47/47 of 183, interval [0.9244, 1]; train 122/118 of 683, four named wrong cells (`/tmp/agentlogs/pu74-measure-2.log:7-16,19-26,32-48`). The live test checks the 47/47/183 reader constants (`ios/Tests/TankbookCoreTests/PumpReaderPipelineTests.swift:98-117`; `ios/Sources/TankbookCore/Config/PumpPhotoGate.swift:84-95`). The heldout live tier has zero new wrong cells. The row's claimed annotated movement remains **116 → 121 of 181**, even though the owner restored pump-275 and the current measured result is **123/123 of 183**; it prints neither before nor after interval beside that movement (`docs/TASKS.md:1082`; `docs/EXTRACTION.md:1127-1134`). Reconcile both documents to one named population and put the intervals beside the estimate. The supplied second log contains only three pipeline tests; the asserted oracle 778/0.9987, fragility 0.036, and 42/42 law-suite run are in the row but not in the supplied gate logs (`/tmp/agentlogs/pu74-measure-2.log:1-49`; `docs/TASKS.md:1082`). Supply the law-run log.

4. **No regression elsewhere — PARTIAL.** Annotated and live counts improve or hold; the train wrong count falls from six to four (`docs/TASKS.md:1082`; `/tmp/agentlogs/pu74-measure-2.log:7-16,19-48`). Receipt routing is decided before `readDecision` invokes the law, so this change cannot raise the pump-classification receipt leak without changing the classifier (`PumpDisplayCapture.swift:321-325`; `ios/App/Sources/Capture/CapturePipeline.swift:60-82`). The note explicitly waives a Release latency number for these constant-time lookups (`agents/research/PU.74.md:351-357`). Oracle/fragility non-regression is only asserted in prose pending its log. A receipt-negative count would make the existing 5/116 baseline independently checkable; the call-path proof suffices for this row's code change.

5. **Tests that would fail — PARTIAL.** The initial three law mutations are red, and the RUB-total mutation makes the corpus test red with exactly 19 misses (`/tmp/agentlogs/pu74-mutation-gbpwide.log:57-62`; `/tmp/agentlogs/pu74-mutation-default.log:36-41`; `/tmp/agentlogs/pu74-mutation-cells.log:28-33`; `/tmp/agentlogs/pu74-mutation-rubtotal.log:19-53`). The corpus test reports 932 windows and checks that observed placements/counts are *members* of the coded sets, not that the table equals the derived sets; widening another table entry would stay green (`ios/Tests/TankbookCoreTests/PumpDisplayConventionsCorpusTests.swift:35-56`). The named RUB total test only asserts `total.value != nil`, so it can pass with a derived product rather than a one-decimal **read**; the truncated-total test likewise omits a `.derived` provenance assertion (`ios/Tests/TankbookCoreTests/PumpReadingLawExactTests.swift:103-123`). Strengthen these provenance/value checks and mutate them. The oracle ratchet scores committed cells and enforces a low historical floor plus precision; it does **not** assert that all 253 note-sweep fixtures retain their true close, or enumerate the 14 disambiguated fixtures (`ios/Tests/TankbookCoreTests/PumpReadingLawTests.swift:433-445,624-675`; `agents/research/PU.74.md:295-316,335-349`). Therefore oracle 778, even if logged, does not discharge the note's 0/253 wrong-refusal falsifier. Add the close sweep or an equivalent named per-fixture assertion with a red mutation. The app unit bundle shown is green 301/301, but it predates the second-pass changes (`/tmp/agentlogs/pu74-app.log:1854-1857`; `docs/TASKS.md:1082`).

6. **Docs reconciled — PARTIAL.** The pump section describes the behavior, and no numbered decision changes: decisions 9-11 govern other seams (`docs/EXTRACTION.md:873-1071,1127-1134`). The refusal has no new user copy: the app's existing empty/manual prefill remains the J4/F2 outcome, so `docs/ERRORS.md` and `docs/JOURNEYS.md` need no change (`CapturePipeline.swift:78-100`; `docs/ERRORS.md:295-315`; `docs/JOURNEYS.md:140-155,696-709`). The PU.74 section and row still carry stale population/numbers and omit intervals and PU.59's requested cautioned-tier result (`docs/EXTRACTION.md:1127-1134`; `docs/TASKS.md:1082`). The changed code comment still records a task ID, commit hash, and historical measurement, and the type doc comment cites a task-ID path, against the comment rule (`PumpReadingTypes.swift:205-208,251-252`; `CLAUDE.md:184-201`). Rewrite both as current invariants. The new corpus-test header also embeds that task-ID path (`PumpDisplayConventionsCorpusTests.swift:5-10`).

7. **Every Checks-cell sentence — MISSING overall** (`docs/TASKS.md:1082`). Constraints in `candidates`: **PARTIAL**, placement is there, count audit is outside (`PumpReadingLaw.swift:72-96,384-417`). Oracle ≥763 and fragility ≤0.10: **PARTIAL**, claimed 778/0.9987 and 0.036 lack the supplied law-run log (`docs/TASKS.md:1082`; `/tmp/agentlogs/pu74-measure-2.log:1-49`). pump-106 abstains: **MISSING**; the note establishes that this premise is false at current HEAD and says to revise the row to an audit catch, yet the Checks cell still promises abstention (`agents/research/PU.74.md:317-323`; `docs/TASKS.md:1082`). KZT whole-tenge total and RUB one-decimal price: **MET** in the table and named tests, subject to the read-provenance weakness above (`PumpReadingTypes.swift:259-260`; `PumpReadingLawExactTests.swift:91-112`). Live zero new wrong: **MET**, 47/47 on the app path (`/tmp/agentlogs/pu74-measure-2.log:19-26`). PU.59 cautioned tier remeasured and reported: **MISSING**; the row and log still provide no new cautioned count or precision after PU.74 (`docs/TASKS.md:1082`; `/tmp/agentlogs/pu74-measure-2.log:1-49`; prior 1/10 in `docs/EXTRACTION.md:1064-1071`). Measure and report it, even if unchanged.

## Required closure

Record approval or a justified note adaptation for the count-audit placement; reconcile the pump-106 Checks promise with the note and owner; add the 253-fixture close assertion and strengthen read/derived provenance tests with red mutations; supply the second-pass law log; remeasure and report PU.59's cautioned tier; update the row and extraction section to the 183-cell population with intervals; clean the touched comments. No separate row is needed for these PU.74 exit conditions. If the cautioned-tier result requires a new product decision, that decision remains with open PU.59 (`docs/TASKS.md:1062`).

**INCOMPLETE.** The orchestrator should fix these PARTIAL/MISSING items and re-dispatch the review before committing PU.74.
