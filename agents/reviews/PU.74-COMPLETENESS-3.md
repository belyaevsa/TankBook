# PU.74 completeness review, pass 3 — 2026-09-24

Scope: PU.74 only. I read the working tree and supplied logs. I ran a read-only Python recount of `windows.json`, `expected.csv`, and `split.csv` using the note's §5.2 population: reviewed train and heldout stills, displayed transaction digits for counts, and nonzero, undisputed asserted values for reproducing placements. I did not build or run `PumpReadTool`. The recount found 324 stills and 920 digit-bearing windows. For the six currencies with at least three stills, **every coded placement and cell-count set matches** the recount: EUR [2]/[3]/[2], RUB [2]/[1,2]/[1,2], KZT [2]/[0,1]/[0], GBP and AUD [2]/[3]/[2], BYN [2]/[2]/[2]; their count sets also match (`agents/research/PU.74.md:236-282`; `ios/Sources/TankbookCore/Extraction/PumpReader/PumpReadingTypes.swift:251-263`). The eight one-still currencies' observed placements match; BRL, PHP and TMT totals reproduce no asserted value, so their narrow read rows are seeds rather than an observed reproducing placement (`PumpReadingTypes.swift:265-275`). KGS retains its prior video convention outside the still population, as the prior review noted (`PumpReadingTypes.swift:263-265`; `agents/reviews/PU.74-COMPLETENESS-2.md:3`).

## Seven checks

1. **Fidelity — MISSING.** The placement table, unmeasured-currency refusal, derived-total exemption and union count sets implement A2-A5 and A8-A9 (`PumpReadingTypes.swift:211-282`; `PumpReadingLaw.swift:51-52,72-77,92-97`). Auditing a fixed window count once in `resolve` is a sound semantic choice: it refuses before `closingTriples`, preserves `.cellCountImpossible`, and repeats in the no-price path (`PumpReadingLaw.swift:87-104,211-228`; `docs/EXTRACTION.md:1133-1137`). But the note explicitly locates the count constraint in `candidates` and calls for an impossible window to produce `[]` (`agents/research/PU.74.md:130-151`); A8 specifies the union, not this relocation (`agents/research/PU.74.md:221-227`). The mapped paper's per-step mask is in Willard & Louf §2.2-§3 (`agents/research/PU.74.md:110-125`). The recorded rationale is in `EXTRACTION`, but the required note amendment or product-owner approval of this departure is absent. The Checks cell still says constraints are enforced in `candidates` (`docs/TASKS.md:1082`). The brief's explicit rule makes an unlisted departure MISSING even when justified in another document.

2. **App wiring — MET.** `CapturePipeline.readPumpDisplay` calls `PumpDisplayCapture.classify`, whose accepted read reaches `PumpReader.resolve` and `PumpReadingLaw.resolve` (`ios/App/Sources/Capture/CapturePipeline.swift:126-134`; `ios/Sources/TankbookCore/Extraction/PumpReader/PumpDisplayCapture.swift:317-328,372-390`; `ios/Sources/TankbookCore/Extraction/PumpReader/PumpReader.swift:553-560`). This chain has no Debug guard. The two reasons live on the core law reading; the app uses its extraction and caution, not the reason, in prefill (`PumpReadingTypes.swift:54-61,168-195`; `CapturePipeline.swift:78-100`). No reason mapping to a payload or API was found, so hard rules 9 and 16 are respected (`CLAUDE.md:70-78,164-170`).

3. **App-path measurement — MET.** The measured heldout population is 68 stills / 183 numeric cells: annotated 123/123, one-sided Clopper-Pearson lower 0.9759; live `classify` 47/47, lower 0.9382; train in-sample 122/118, Wilson 95% [0.9187, 0.9872] (`/tmp/agentlogs/pu74-measure-2.log:12-19,34-35`). The live test checks `PumpPhotoGate`'s 47/47/183 constants (`ios/Tests/TankbookCoreTests/PumpReaderPipelineTests.swift:98-117`; `ios/Sources/TankbookCore/Config/PumpPhotoGate.swift:84-95`). The row and extraction section report these numbers and intervals (`docs/TASKS.md:1082`; `docs/EXTRACTION.md:1138-1146`). There are zero new heldout wrong commits.

4. **No regression elsewhere — MET.** The oracle is 784/783 at 0.9987, above 763; fragility is 39/1005 = 0.039, below 0.10 (`/tmp/agentlogs/pu74-law-pass2.log:112-117`). Annotated and live measures hold or improve, and train wrong cells fall from six to four (`docs/TASKS.md:1082`; `/tmp/agentlogs/pu74-measure-2.log:12-19,34-58`). Receipt classification precedes this law; the gate's receipt sample reports 0/8 leaked (`PumpDisplayCapture.swift:321-325`; `/tmp/agentlogs/pu74-gate.log:8836`). The note explicitly waives a Release latency number for this lookup (`agents/research/PU.74.md:351-357`). The gate is Debug, but no Debug-only seam was touched in this path (`PumpReadingLaw.swift:46-52`; `PumpDisplayCapture.swift:317-328`).

5. **Tests that would fail — MET.** The corpus test re-derives placements and counts, including exact equality of read sets for the six measured multi-still currencies (`ios/Tests/TankbookCoreTests/PumpDisplayConventionsCorpusTests.swift:15-74`). Its `closes` calls the law's own `candidates` and `closingTriples`, and mirrors the count audit; the sweep tests existence of a true close, rather than a full verdict, exactly the note's F1/F2 close-enumeration target (`PumpDisplayConventionsCorpusTests.swift:79-101,133-175`; `agents/research/PU.74.md:295-344`). It excludes `csvDisagrees`, as §5.2 table 2 and its fit exclusions require (`PumpDisplayConventionsCorpusTests.swift:107-127`; `agents/research/PU.74.md:268-283`); pump-190 is disputed and pump-310 has a partial total, so the 12 tested fixtures are an honest subset of the note's 14 (`PumpDisplayConventionsCorpusTests.swift:155-163`). The sweep reports 229 fixtures, 224 true closes, 0 refused (`/tmp/agentlogs/pu74-law-pass2.log:109`). The exact tests assert both value and read/derived provenance for RUB and the pair count guard (`ios/Tests/TankbookCoreTests/PumpReadingLawExactTests.swift:103-137`). The named mutations are red: GBP widening, default restoration and count audit removal each fail their discriminating exact test (`/tmp/agentlogs/pu74-mutation-gbpwide.log:57-62`; `/tmp/agentlogs/pu74-mutation-default.log:36-41`; `/tmp/agentlogs/pu74-mutation-cells.log:28-33`); the later GBP permissive, RUB truncated removal and RUB price narrowing mutations fail the sweep/provenance tests (`/tmp/agentlogs/pu74-mutation-gbp-permissive.log:83-125`; `/tmp/agentlogs/pu74-mutation-rub-no-truncated.log:88-113`; `/tmp/agentlogs/pu74-mutation-rub-price-2.log:128-167`). Restored law and convention suites pass 45/45 (`/tmp/agentlogs/pu74-law-pass2.log:117`). The earlier RUB-total narrowing mutation also failed with 19 corpus misses (`agents/reviews/PU.74-COMPLETENESS-2.md:19`).

6. **Docs reconciled — PARTIAL.** The extraction paragraph now explains the window-level choice, read/derived RUB distinction, close sweep, intervals and pump-190 exception (`docs/EXTRACTION.md:1127-1146`). The row records review 2's closure, the revised pump-106 audit catch, and PU.59's held input population of five heldout and 22 train `.priceUnvalidated` stills (`docs/TASKS.md:1082`; `/tmp/agentlogs/pu74-measure-2.log:25-29,42-46`). The caution remains held in PU.59: `pairOutcome` still refuses it (`PumpReadingLaw.swift:293-330`), so reporting its input population discharges the re-measurement request without claiming cautioned commits. No user-visible surface changes, so `docs/ERRORS.md` and `docs/JOURNEYS.md` J4/F2 need no new copy (`CapturePipeline.swift:78-100`). Touched type and corpus-test comments now describe current behavior without task IDs (`PumpReadingTypes.swift:197-275`; `PumpDisplayConventionsCorpusTests.swift:5-12`; `CLAUDE.md:185-201`). The remaining defect is the Checks cell's literal `candidates` placement promise, which contradicts both the code and the new extraction paragraph (`docs/TASKS.md:1082`; `PumpReadingLaw.swift:92-97,211-219`).

7. **Every Checks-cell sentence — PARTIAL** (`docs/TASKS.md:1082`). **Count and placement enforced in `candidates`: PARTIAL** — placement is there; the count audit is in `resolve` (`PumpReadingLaw.swift:72-77,92-97,384-417`). **Oracle ≥763 and fragility ≤0.10: MET** — 784/783 and 0.039 (`/tmp/agentlogs/pu74-law-pass2.log:112-117`). **pump-106 audit catch and pump-137 live catch: MET** — corrected to the note's actual premise and covered by table and GBP mutations (`agents/research/PU.74.md:317-323`; `PumpReadingTypes.swift:259-263`; `/tmp/agentlogs/pu74-mutation-gbp-permissive.log:83-125`). **KZT whole-tenge total and RUB one-decimal price still commit: MET** (`PumpReadingLawExactTests.swift:90-113`). **Live zero new wrong: MET** (`/tmp/agentlogs/pu74-measure-2.log:18-19`). **PU.59 tier re-measured and reported: MET for its still-held input population**, five heldout and 22 train refusals (`/tmp/agentlogs/pu74-measure-2.log:25-29,42-46`; `docs/TASKS.md:1082`).

## Gate and disposition

The supplied gate reports exit 0 for package build, lint, app Debug build, package tests **2299/2299**, app unit bundle **303/303**, and all steps (`/tmp/agentlogs/pu74-gate.log:4,2284,2289,3547,8858,8973,11003-11017`). I did not rerun it or run UI/screenshot gates: this review is read-only and PU.74 changes no UI. I did not capture screenshots. Mutation red output and restored green output, verbatim summaries: `✘ Test run with 6 tests in 1 suite failed after 0.007 seconds with 2 issues.` (`/tmp/agentlogs/pu74-mutation-gbpwide.log:62`); `✘ Test run with 6 tests in 1 suite failed after 0.003 seconds with 2 issues.` (`...default.log:41`); `✘ Test run with 6 tests in 1 suite failed after 0.002 seconds with 1 issue.` (`...cells.log:33`); `✘ Test run with 13 tests in 2 suites failed after 0.218 seconds with 5 issues.` (`...gbp-permissive.log:125`); `✘ Test run with 13 tests in 2 suites failed after 0.207 seconds with 5 issues.` (`...rub-no-truncated.log:113`); `✘ Test run with 13 tests in 2 suites failed after 0.207 seconds with 10 issues.` (`...rub-price-2.log:167`); `✔ Test run with 45 tests in 3 suites passed after 2.953 seconds.` (`/tmp/agentlogs/pu74-law-pass2.log:117`). The law source diff contains only the PU.74 count/refusal edits reviewed here (`PumpReadingLaw.swift:51-52,87-97,211-219`); no independent pre-measurement source snapshot is supplied to prove byte identity, but the later full gate runs this tree.

**INCOMPLETE.** Amend `agents/research/PU.74.md` with the window-level placement as a named, justified departure (or record explicit product-owner approval), then align the PU.74 Checks cell in `docs/TASKS.md` with the code. Re-dispatch the completeness review before commit. No separate row is needed; these are PU.74's own method and documentation obligations. The still-held cautioned product decision remains in PU.59 (`docs/TASKS.md:1062`).
