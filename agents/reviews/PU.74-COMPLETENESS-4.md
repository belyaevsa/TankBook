# PU.74 completeness review, pass 4 — 2026-09-24

Scope: only review 3's open count-audit placement item and the Checks-cell sentence it drives. Review 3 marked the other items MET; they were not re-reviewed. This pass read the current working tree and made no source or documentation changes.

1. **Named departure — MET.** A10 is now in the note's named adaptation list, after A9 (`agents/research/PU.74.md:169-172,228-242`). It explicitly identifies the departure from §3's `candidates` placement, names both the triple and pair entry points, and justifies one check per window because every candidate from a window has the same cell count. It also explains why auditing in `candidates` would turn the intended `.cellCountImpossible` into `.cellUnknown` (`agents/research/PU.74.md:232-240`). The earlier §3 wording remains as the premise that A10 amends (`agents/research/PU.74.md:130-151`).

2. **Refused-set claim — MET at the close/commit boundary.** With a price window, `resolve` builds candidate arrays first, then checks the three transaction windows and returns `.cellCountImpossible` before the first `closingTriples` call, the preset close, or the repair loop (`ios/Sources/TankbookCore/Extraction/PumpReader/PumpReadingLaw.swift:72-97,99-164`). The repair loop changes one digit while preserving the cell count, and is reached only after that audit (`PumpReadingLaw.swift:128-148`). Without a price window, `resolve` filters boards by their price-role count before passing them on (`PumpReadingLaw.swift:56-61`); `resolveWithoutPrice` audits the liter/total pair before `cleanBoardClose` or `pairOutcome` can build a close or commit a pair (`PumpReadingLaw.swift:211-229,254-271,293-325`). `candidates` itself rejects empty and over-`maxCells` windows and emits values only at the supplied decimal placements (`PumpReadingLaw.swift:384-417`). The audit deliberately skips the all-zero idle verdict and skips count checks when a transaction window exceeds `maxCells`; those paths cannot commit from that window because the idle guard returns and `candidates` returns `[]`, respectively (`PumpReadingLaw.swift:85-97,214-218,298-306,384-385`). Thus no execution path builds a triple or committed pair from a transaction or board window whose measured count the audit would refuse. Candidate arrays can be built *before* the triple audit, but they cannot reach a close. A count mask inside `candidates` would therefore exclude the same closes; its abstention reason could differ, as A10 states (`agents/research/PU.74.md:237-242`).

3. **Checks cell, extraction paragraph, and code — MET for the open sentence.** The PU.74 Checks cell now assigns placement masking to `candidates` and the cell-count audit to `resolve`, cites A10, and says the audit covers the triple and pair tiers after structural guards (`docs/TASKS.md:1082`). The PU.74 extraction paragraph describes the same once-per-window placement and named-reason rationale (`docs/EXTRACTION.md:1127-1137`). The code uses measured `admits` sets (`ios/Sources/TankbookCore/Extraction/PumpReader/PumpReadingTypes.swift:218-239,252-268`), applies them at `resolve` and `resolveWithoutPrice`, and supplies the placement arrays to `candidates` (`PumpReadingLaw.swift:72-77,92-103,214-227,384-417`).

This was a read-only re-check: no builds, tests, UI suites, localization gate, screenshot manifest check, mutations, or screenshots were run or captured in this pass; their exit codes and counts are N/A. Review 3 recorded a prior gate exit 0, package tests 2299/2299, app unit tests 303/303, and restored law/convention suites 45/45 (`agents/reviews/PU.74-COMPLETENESS-3.md:15,23`); these are prior results, not new observations. The mutation red and restored green summaries are quoted verbatim in review 3 (`agents/reviews/PU.74-COMPLETENESS-3.md:23`). No newly found issue is left unfixed in this scoped review; the still-held cautioned-pair decision remains owned by PU.59 (`docs/TASKS.md:1062`).

**COMPLETE.** The sole open fidelity and Checks-cell placement issue from review 3 is closed. No change is required for PU.74 on this item.
