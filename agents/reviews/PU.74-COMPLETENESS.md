# PU.74 completeness review — 2026-09-24

**Verdict: INCOMPLETE. Do not commit this row.** Reviewed only the PU.74 files and the supplied gates; PU.72/PU.73 edits are outside this verdict. This review used read-only inspection and a read-only Python recount of the current corpus. I did not run `swift run PumpReadTool`, a build, or a mutation.

## Recount and method

I read reviewed `windows.json` entries in the train and heldout splits, counted digits in each non-empty transaction window, and computed each non-zero asserted value's reproducing decimal placement from `expected.csv`, excluding `csvDisagrees` fields as §5.2 specifies (`agents/research/PU.74.md:250-284`; `ios/Tests/TankbookCoreTests/PumpReaderTestSupport.swift:83-88`). The current corpus has 181 reviewed EUR stills and 114 RUB stills in those splits. All coded cell-count **sets** for currencies with at least three stills match the recount, including EUR price `{2,4}` and RUB price `{3,4,5}` (`ios/Sources/TankbookCore/Extraction/PumpReader/PumpReadingTypes.swift:254-261`). Limiting the count audit to currencies with at least three stills is a defensible conservative implementation of A8: the one-still rows cannot bound a new head (`agents/research/PU.74.md:188-195,221-227`; `PumpReadingTypes.swift:217-220,264-274`). The exact frequencies in the note have moved with corpus intake; the membership sets have not.

**A placement entry is wrong:** the recount finds RUB total placements `{1,2}`. Nineteen non-zero, non-`csvDisagrees` reviewed RUB totals exactly reproduce the asserted value at one decimal, including pump-018 `2499,8` = 2499.80, pump-044 `1707,5` = 1707.50, and pump-092 `1915.5` = 1915.50. The note also explicitly gives `{1,2}` and calls adding one-decimal *read* placements its only RUB change (`agents/research/PU.74.md:266-275`). The code has `totalDecimals: [2]` and only a derived `truncated: [1]` (`ios/Sources/TankbookCore/Extraction/PumpReader/PumpReadingTypes.swift:213-216,257`). A derived-total path may recover some values, but it is a different candidate and provenance, and it does not implement the measured read-placement set. Fix the row and test an exactly shown one-decimal RUB total through the read tier. The code comment that all one-decimal RUB totals are "only ever derived" is false for these fixtures (`PumpReadingTypes.swift:213-215`).

## Seven checks

1. **Fidelity — MISSING.** Decimal placement is constrained during candidate emission as A2 allows (`ios/Sources/TankbookCore/Extraction/PumpReader/PumpReadingLaw.swift:382-405`), and A3, A4, A6, A7, A9 are recognizable (`PumpReadingLaw.swift:51-52,72-81`; `PumpReadingTypes.swift:254-274`). The RUB row contradicts the note's measured format as above. The count audit is a preflight in `resolve` and `resolveWithoutPrice`, after candidate generation in the triple tier (`PumpReadingLaw.swift:72-96,211-219`), while the note maps both constraints to `candidates` and Willard & Louf §2.2 masks during generation (`agents/research/PU.74.md:139-151,181-187`). A8 explains a window-level audit, which is semantically reasonable because count is fixed before decoding, but this placement of that audit is not explicitly listed as a departure. Record owner approval or amend the research note to justify it, and make the row's `candidates` promise accurate.

2. **App wiring — MET.** The app's `CapturePipeline.process` calls `readPumpDisplay`, which calls `PumpDisplayCapture.classify`; its accepted reading reaches the composed Confirm prefill (`ios/App/Sources/Capture/CapturePipeline.swift:55-63,77-100,126-134`). `PumpDisplayCapture` calls the reader, which calls `PumpReadingLaw.resolve` (`ios/Sources/TankbookCore/Extraction/PumpReader/PumpDisplayCapture.swift:317-325,385-405`; `PumpReader.swift:553-559`). These calls are outside `#if DEBUG`. The new reasons remain inside `PumpDisplayReading` and field readings; app composition uses extraction and caution, not the reason (`PumpReadingTypes.swift:148-193`; `CapturePipeline.swift:77-100`). No sync payload, server validation, or API schema was changed, satisfying hard rules 9 and 16 (`CLAUDE.md:70-74,161-179`). `PumpReadTool` can serialize the reasons for its local diagnostic output (`ios/Sources/PumpReadTool/main.swift:226,256`); that is not an app payload/API boundary.

3. **App-path measurement — PARTIAL.** The named heldout live run measured 47/47 correct out of 181, Wilson 95% precision interval `[0.9244,1]`, and the annotated run measured 121/121 out of 181, interval `[0.9692,1]` (`/tmp/agentlogs/pu74-measure.log:21-28`). `PumpPhotoGate.readerCommitted` and `.readerCommittedCorrect` are 47, but `.readerNumericTotal` is 183, so `livePath` **failed** its constant cross-check: `measurement.numericTotal` was 181 (`ios/Sources/TankbookCore/Config/PumpPhotoGate.swift:83-95`; `ios/Tests/TankbookCoreTests/PumpReaderPipelineTests.swift:104-115`; `/tmp/agentlogs/pu74-measure.log:42-45,81`). Reconcile the corpus constant and rerun the live floor to green; the row cannot be gated by a failed live suite. The row's reported 116 → 121 annotated movement has no interval beside the point estimate in `docs/TASKS.md:1080` or `docs/EXTRACTION.md:1127-1134`, though the log supplies the after interval. Add the before and after intervals for the named 181-cell population. The train in-sample result is 122 committed/118 correct out of 680, with four named wrong cells; this is not a heldout zero-wrong claim (`/tmp/agentlogs/pu74-measure.log:48-72`).

4. **No regression elsewhere — PARTIAL.** Annotated and live counts are above the note's prebuild floors, and the supplied row text reports oracle 776 at 0.9987, fragility 0.041 (`agents/research/PU.74.md:327-345`; `docs/TASKS.md:1080`). The supplied `/tmp/agentlogs/pu74-*` files do not contain the claimed 38/38 law-suite run or its oracle/fragility lines, so that gate cannot be independently checked from the named evidence. There is no PU.74 receipt-leak measurement against the 5/116 baseline (`docs/TASKS.md:1069`; `ios/Sources/TankbookCore/Extraction/PumpReader/PumpDisplayCapture.swift:317-325`). The law runs only after display classification, so leak stability is plausible, but the requested non-pump fixture count should be reported or explicitly justified by a call-path proof. The note explicitly waives a Release latency measurement for this constant-time table lookup (`agents/research/PU.74.md:351-357`).

5. **Tests that would fail — PARTIAL.** The three new exact tests cover unmeasured currency, GBP shrink, and an impossible EUR price count (`ios/Tests/TankbookCoreTests/PumpReadingLawExactTests.swift:55-88`). Their matching mutations are red: GBP widened, `2 issues`; default restored, `2 issues`; count audit removed, `1 issue` (`/tmp/agentlogs/pu74-mutation-gbpwide.log:57-62`; `pu74-mutation-default.log:36-41`; `pu74-mutation-cells.log:28-33`). The app unit bundle is green, 301/301 (`/tmp/agentlogs/pu74-app.log:1855-1857`). There is no test that rederives the complete convention table at corpus intake, despite the note's stated deliverable (`agents/research/PU.74.md:360-364`); this let the RUB error pass. Add tests for the named KZT zero-decimal total and RUB one-decimal price and total, pump-106's measured-convention audit, the pair-tier count guard, and the derived-total exemptions (`docs/TASKS.md:1080`; `agents/research/PU.74.md:335-349`). The GBP synthetic is useful, but it does not replace the note's 253-fixture close sweep with zero wrong refusals (`agents/research/PU.74.md:298-320,335-342`).

6. **Docs reconciled — PARTIAL.** The PU.74 paragraph and row describe the new behavior (`docs/EXTRACTION.md:1127-1134`; `docs/TASKS.md:1080`), but their claim that the table is fully measured is wrong for RUB total. The row still says the PU.59 cautioned tier was to be remeasured without giving its result, and it leaves the constant mismatch to a future corpus commit. The refusal has no new user-visible copy: it produces an empty/manual prefill through the existing J4/F2 behavior, so no `docs/ERRORS.md` or `docs/JOURNEYS.md` change is required (`ios/App/Sources/Capture/CapturePipeline.swift:77-100`; `docs/ERRORS.md:286-315`; `docs/JOURNEYS.md:128-155,696`). The changed code comment contains a task ID, a commit hash, and historical narration, contrary to `CLAUDE.md:183-200` (`PumpReadingTypes.swift:251-252`); the doc comment also embeds a task-ID path (`PumpReadingTypes.swift:205-208`). Rewrite as current invariant without history. This row does not amend numbered decision 9, 10, or 11, so a new numbered decision is not needed (`docs/EXTRACTION.md:873-1008`).

7. **Every Checks-cell promise — MISSING overall** (`docs/TASKS.md:1080`). Constraint during candidate generation: **PARTIAL**, decimal placement yes, count audit outside `candidates`; oracle ratchet ≥763 and fragility ≤0.10: **PARTIAL**, numbers claimed but the law-suite log is absent from supplied gate evidence; pump-106 abstains: **MISSING**, the research note revises this premise to a regression audit, but the row's Checks cell still promises abstention and no test demonstrates either (`agents/research/PU.74.md:317-324`); KZT zero-decimal total and RUB one-decimal price still commit: **PARTIAL**, table rows exist but no named regression tests; live zero new wrong: **PARTIAL**, 47/47 measured but live suite is red on its denominator; PU.59 cautioned tier remeasured and reported: **MISSING**, no before/after count or decision in the row or gate logs. Reconcile the pump-106 promise with the note and owner, then report the cautioned tier explicitly, even if unchanged.

## Required closure

Correct the RUB total read-placement row; add a corpus-derived table/close-sweep regression and the named edge-case tests; reconcile `PumpPhotoGate.readerNumericTotal` with the reviewed corpus and rerun `livePath` green; supply the law-suite, receipt-leak, and PU.59 caution evidence; add intervals and accurate status to the docs; clean the code comments; resolve the `candidates` placement and pump-106 promise against the note. Re-dispatch this review after those changes. The PU.59 cautioned-tier question remains owned by PU.59 if the measured result needs a separate product decision; its measurement and report are still PU.74's exit condition (`docs/TASKS.md:1080`).
