# PU.77 completeness re-check 2 — 2026-09-25

**INCOMPLETE.** This re-check covers only the four requested items from review 1. The seed-2 B2 rows do not match the specifically named raw/scaled logs.

1. **Law-window departure — MET.** The report explicitly calls out §4.4 step 3 as undone and needing the owner's OK before rerun counts (`/Users/sbelyaev/repos/fuel-counter-ios-wt-pu77/agents/research/PU.77-SPIKE.md:42-48`). It says the law used shipped windows on raw and T-scaled posteriors and labels the B2 results as those windows' numbers (`PU.77-SPIKE.md:44-48`).

2. **Seed-2 B2 numbers — NOT MET.** The report assigns raw **158 / 156, 55 / 68 photos** and scaled **153 / 152, 54 / 68 photos** (`PU.77-SPIKE.md:80-84`). The named *raw* log instead says **153 / 152, 54 / 68** and only pump-041 wrong (`/tmp/agentlogs/pu77-s2-law-raw.log:8-10`); the named *scaled* log says **158 / 156, 55 / 68** with pump-023 and pump-041 wrong (`/tmp/agentlogs/pu77-s2-law-scaled.log:8-11`). Consequently the report's “without pump-041” allocation is also reversed (`PU.77-SPIKE.md:97-98`): by these logs, raw becomes **152 / 152 = 1.000**, while scaled becomes **156 / 157 = 0.994**. The files themselves contain no raw/scaled mode header (`pu77-s2-law-raw.log:1-13`; `pu77-s2-law-scaled.log:1-14`), so their names alone cannot establish whether the files or the report have the modes reversed. Reconcile the provenance and report before accepting these claims.

3. **heldout2 and scored-candidate list — MET to the supplied artifacts.** There is one `heldout2-b1.json` and one `heldout2-law.log` for each of seeds 0, 1, and 2. The B1 JSON values (`/Users/sbelyaev/repos/fuel-counter-ios-wt-pu77/ml/pump-reader/.out/pu77-s0/heldout2-b1.json:4-35`, `/Users/sbelyaev/repos/fuel-counter-ios-wt-pu77/ml/pump-reader/.out/pu77-s1/heldout2-b1.json:4-35`, `/Users/sbelyaev/repos/fuel-counter-ios-wt-pu77/ml/pump-reader/.out/pu77-s2/heldout2-b1.json:4-35`) round to the report's current and three row-reader entries (`PU.77-SPIKE.md:100-108`). Each law log reports 7 committed, 7 correct, 3/7 photos (`/tmp/agentlogs/pu77-s0-heldout2-law.log:1`, `/tmp/agentlogs/pu77-s1-heldout2-law.log:1`, `/tmp/agentlogs/pu77-s2-heldout2-law.log:1`), consistent with the table. The report states once-per-seed scoring and lists the three candidates and shared configuration (`PU.77-SPIKE.md:50-54`); the artifacts support one recorded score per seed, although they cannot independently prove that no prior scoring was run.

4. **Verdict and owner decision — MET.** The report explicitly calls this run a measured B3 no-go, says B2 fails and no Swift reader is built (`PU.77-SPIKE.md:7-18`), then presents the law row separately as the owner's decision and a full-protocol rerun as future work (`PU.77-SPIKE.md:118-129`).

This was a read-only artifact review. No builds, tests, UI suites, localization or screenshot checks were run; no screenshots were captured. Review 1's previously passed items were not re-reviewed. The remaining correction belongs in the PU.77 spike report and its seed-2 raw/scaled run provenance; the proposed repair-tier guard remains a separate owner decision at `PumpReadingLaw.resolve`.

**INCOMPLETE**
