# PU.77 completeness review — 2026-09-25

**INCOMPLETE.** The offline reader shows a real B1 gain and the pump-041 diagnosis is supported, but the spike does not complete the research note's protocol and the report contains several incorrect numerical statements. This is a review of the untracked spike in `/Users/sbelyaev/repos/fuel-counter-ios-wt-pu77`; no spike files were changed.

## Checks

1. **Fidelity — MISSING.** The CRNN topology, CTC loss, RGB input, aspect preserving padding, training mix, and substitution posteriors follow the named recipe (`agents/research/PU.77.md:452-519,520-658`; worktree `rowreader.py:61-124,130-229`, `rowtrain.py:149-195`). The beam of 32, fixed currency/field mix, and 20,000 steps are named in the report (`PU.77-SPIKE.md:33-37`). But §4.4 explicitly requires the law's nat windows to be *re-derived on train*; `roweval.py:109-148` fits T and exports posteriors only, and the Swift harness calls the unchanged `PumpReadingLaw.resolve` (`PumpRowReaderSpikeTests.swift:104-113`; `PumpReadingLaw.swift:34-44`). This unlisted departure makes B2 non-comparable to the specified bar. The note also requires a train-validation H48 decision, per-style separator results, an oracle ratchet re-run, and heldout2 once at go/no-go (`PU.77.md:538-543,611-622,647-653,661-676,764-773`). The training/evaluation code and report contain none of these (`rowtrain.py:116-129,189-203`; `roweval.py:98-150`; `PU.77-SPIKE.md:39-98`). The 2% source cap is a direct per-group truncation rather than the specified water-filling device, and run metrics omit the candidate's full config/pad mode (`PU.77.md:631-650`; `rowtrain.py:88-101,165-203`; `rowreader.py:61-80`). These departures need either implementation or explicit reporting with their effect on the gate.

2. **Numbers — MISSING.** The B1 values and intervals round correctly from `pu77-s{0,1,2}/heldout-b1.json`; current separator placement is 126/195 = 0.646 from `heldout/index.json`. The B2 committed/correct/precision entries match the six `/tmp/agentlogs/pu77-s{0,1,2}-law-{raw,scaled}.log` lines, and their WRONG sets match the report. Exceptions: `PU.77-SPIKE.md:63-67` gives a single seed-2 photo count, 55/68, while the scaled log says **54/68** (raw is 55/68). `PU.77-SPIKE.md:80` says removing pump-041 leaves *every* seed at 0.993-0.994; seed-2 scaled has only pump-041 wrong, so removing it yields **152/152 = 1.000**. `PU.77-SPIKE.md:33-35` says best path scores within 0.01 of prefix, but seed 2 differs by **0.0103** (0.9026 versus 0.8923, `pu77-s2/heldout-b1.json`). The 1.4e-14 F1 maximum at `PU.77-SPIKE.md:41-44` reproduces as 1.4210854715202004e-14 over the 30 seeded cases (`test_rowreader.py:15-28`). The report should state the exact evidence or make clear where a value is rounded.

3. **Selection and F3 — MET, with an audit gap.** Training partitions groups by `is_val_group` and picks the checkpoint by `val_real.string` (`rowreader.py:242-265`; `rowtrain.py:149-159,189-195`); `roweval.py:109-116` fits T on that train-side validation pool before it opens the heldout index. I found no code path using heldout to choose a threshold, checkpoint, or T. The configured law windows were not re-derived at all, however, and the required config log and heldout2 score are absent (`PU.77.md:609-622,647-653,801-807`; `rowtrain.py:165-203`; `PU.77-SPIKE.md:39-98`). Thus the selection that exists is train-side, but the complete F3 audit promised by the note is not present.

4. **B1 fairness — MET.** The Swift export warps each heldout hand quad once, writes that strip, and calls the shipped `PumpReader.read(image:windows:)` on precisely that window for its current-arm string (`PumpRowReaderSpikeTests.swift:43-50,57-81`). The index has 252 windows including all 195 transaction windows; `roweval.py:114-145` uses each same indexed strip for the CTC arm and scores that index entry's current string. A nil current read becomes an empty answer and remains in the denominator (`roweval.py:73-85,124-127`). This is a fair same-window comparison.

5. **Pump-041 diagnosis — MET.** Its total is annotated `54.93` and `legibility: partial`; liters and price are `30.62` and `1.784` (`Spike/ReceiptSpike/fixtures/pump/windows.json:4668-4720`; `Spike/ReceiptSpike/fixtures/pump/expected.csv:42`). `pu77-s0/posteriors-raw.json` decodes `54.93`, `30.62`, `1.784`; price digit 8 has log posterior about -0.005. The raw/scaled law logs all commit unit price **1.794**. The law's repair tier can replace the top digit with a confusable partner without a confidence guard (`PumpReadingLaw.swift:120-164`), and 30.62 × 1.794 rounds to 54.93 whereas 30.62 × 1.784 rounds to 54.63 (`PumpReadingLaw.swift:423-447`). This supports the stated failure mechanism. The report's proposed guard is a hypothesis; the claim that it *would* remove pump-041 is not measured (`PU.77-SPIKE.md:92-96`).

6. **Verdict and owner choices — PARTIAL.** B1 passes; B2 has five arms below 0.99, so the no-go and no app-path Swift follow the row and B3 (`docs/TASKS.md:1095`; `PU.77.md:753-773`; `PU.77-SPIKE.md:7-15,46-67`). The report does present closing versus a new law row as choices for the owner (`PU.77-SPIKE.md:90-98`). But B3 says a failed spike *closes as a measured no-go* after the full §4.5 protocol; the report leaves closure open and the missing protocol items above mean the B3 condition has not been fully executed. It should state an unambiguous measured no-go for this run and separate any proposed future law experiment from this row's verdict.

7. **Requested pytest — MET.** From the worktree's `ml/pump-reader`, `PYTHONPATH=$PWD/src /Users/sbelyaev/repos/fuel-counter-ios/ml/pump-reader/.venv/bin/python -m pytest -q` printed **76 passed in 21.85s**, exit code **0**. The supplied named mutation log says, verbatim:

   ```text
   FAILED tests/test_rowreader.py::test_forward_recursion_matches_ctc_loss - ass...
   1 failed, 2 passed in 0.47s
   ```

   The current green suite includes `test_forward_recursion_matches_ctc_loss` (`test_rowreader.py:22-28`). The mutation log gives only pytest's summary, and no red-then-green mutation output is supplied for the second F1 check, so that part of the protocol is not fully preserved (`PU.77.md:792-796`; `PU.77-SPIKE.md:41-44`). The repo gate, UI suites, localization, and screenshot checks were not run: this review is explicitly read-only except this file, while those checks write build artifacts. No screenshots were captured.

## Required changes

Complete or explicitly account for every missing §4/§6 gate item, especially train-side window derivation, oracle ratchet, heldout2, and per-style separator measurement; log every finalist's full config. Correct the seed-2 photos, seed-2 scaled removal arithmetic, and beam comparison. State the B3 no-go for the measured run while leaving a proposed law change to the owner. Preserve the full named mutation red and green output if claiming that protocol. No existing row owns the separate repair-tier confidence-guard decision; file it at `PumpReadingLaw.resolve`'s repair tier if the owner chooses to pursue it.
