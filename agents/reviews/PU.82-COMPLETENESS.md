# PU.82 completeness review — 2026-09-25

**INCOMPLETE.** The experiment and most reported measurements are present, but the verdict contains a false comparison and the PU.41 conclusion overstates what this control tested. The step-0 command and source hash also lack a reproducible log in the named evidence.

| Check | Status | Evidence and finding |
|---|---|---|
| Hand-only option | **MET** | `realglyphs.py:73-110,307-309,328,392` implements and records the flag. `tests/test_hand_only.py:87-101` exercises still, verified, and unverified tracked frames. |
| Step-0 re-export | **PARTIAL** | `agents/research/PU.82-REPORT.md:18-21` claims `PUMP_TRAIN_EXPORT=1` and corpus hash `cf836e56...`; `.out/pu82-export/train-slices.json` has 32,260 windows and `train-videos.json` has 18,303. The named `.out/pu82-runs.log:1-9` contains completion timestamps only, with no export command, full hash, or source provenance. The 50,563-window combined source is corroborated by both manifests. |
| Pool size and dp rate | **MET** | `.out/real-r12-full/manifest.json` has 172,318 cells, 39,078/50,563 windows; `.out/real-r12-hand/manifest.json` has 6,236 cells, 1,412/50,563. DP-bit counts are 38,825/172,318 = 0.225310 and 1,404/6,236 = 0.225144, both 0.225 at the report's precision (`PU.82-REPORT.md:23-28`). The ratio is 27.63, reasonably rounded to 28x. |
| One control per pool plus flatten; three seeds | **MET** | The nine `.out/pu82-*/metrics.json` files show gap-head full and hand controls and flatten-head hand arm at seeds 0, 1, 2, all 15,000 steps, contrast probability 0.15 and real fraction 0.3. `.out/pu82-runs.log:1-9` lists all nine completions. |
| Both tiers; T per candidate | **MET** | Each candidate has a `.out/pu82-*-score.log` with annotated and live measurements and a `temperature.json`. The reported table at `PU.82-REPORT.md:35-45` matches those logs, the metrics' final validation loss, and temperatures rounded to two decimals. The three chosen `.out/pu82-*-seg.log` files support `PU.82-REPORT.md:52-59`. Scoring logs record expected ratchet assertion failures because candidate scores differ from shipped floors; the measurements are still printed. |
| Pool effect isolated | **MET** | Both pool manifests have source `.out/pu82-export/train-slices.json`, 50,563 offered windows, cap 0, hard weight 1, seed 0, centred 0, dp crop `off`, dp bits `keep`; only `hand_only` differs. The six gap-head metrics match in training flags and paired seeds, with only `real` pointing to full versus hand. The effect at `PU.82-REPORT.md:47-49` is the correct difference of three-seed means. |
| Every number, mean, effect, WRONG list | **MET** | All nine score-log counts and WRONG entries, three segmenter-log counts and WRONG entries, nine validation losses and temperatures, the mean correct counts (116.3/45.0, 112.3/41.0, 117.3/41.3), and effects (-4.0/-4.0, +5.0/+0.3) agree at displayed precision with `PU.82-REPORT.md:25-59`. The annotated pump-041 price is WRONG in full s0, hand s2 and flatten s2, as claimed at line 50. |
| Frame selection | **MET** | `realglyphs.py:83-109` matches `(record, frame)` from `frames.verified = 1` for tracked `frame_windows` and video `labels`, while the still query at lines 88-92 is unchanged. `scripts/corpus_db.py:115-125` defines these tables. Against the current database, all 2,624 frame cells in the hand manifest have a verified `frames` row; both manifests contain the same 3,612 still cells. The hand manifest also includes 1,801 video cells, confirming verified video frames are admitted. The current DB may have changed since export, but no hand-manifest violation was found. |
| Verdict and PU.41 claim | **MISSING** | `PU.82-REPORT.md:13-14` says every arm is below shipped on both tiers with the shipped detector, yet flatten s2 is **48** correct live cells (`.out/pu82-flatten-s2-score.log:20`) versus shipped **47** (`PU.82-REPORT.md:32-33`). It has four wrong live cells, so a no-ship judgment can still be defended on precision and annotated performance, but the stated universal comparison is false. `PU.82-REPORT.md:69-71` says this control answers PU.41 round 11b; `docs/TASKS.md:1056` requires phase-aware centering, cleared dp bits, grey profile, cap and hard weight, while these manifests and metrics use centred 0, kept dp bits, cap 0, hard weight 1 and default priors. This run supports only the recipe it actually used. |
| Pytest | **MET** | `cd ml/pump-reader && .venv/bin/python -m pytest -q`: exit **0**, **78 passed** in 17.73s. Mutation evidence `/tmp/agentlogs/pu82-mutation-hand-only.log:1-2` verbatim: `FAILED tests/test_hand_only.py::test_hand_only_keeps_the_verified_frame_and_the_still` / `1 failed, 1 passed in 0.49s`; the current green suite includes both hand-only tests. |

To complete: correct the universal below-shipped sentence, explain why flatten s2's 48/52 live and segmenter 65/66 do not meet the ship criterion, narrow or substantiate the PU.41 round 11b claim against its specified recipe, and provide the step-0 export command plus full source hash/provenance (or label those details unverified). No files outside this review were changed. No screenshots were captured; this review has no UI change. The named ML mutation and pytest were run/checked; the unrelated app, UI, localization, and screenshot gates were not run for this read-only review.
