# PU.85 completeness review — 2026-09-24

**Verdict: COMPLETE.** Read-only except this file. I re-counted the joint table from the current corpus with an independent Python script, read every `decimals` call site in `PumpReadingLaw`, and ran the law + corpus suites myself (oracle 790/789 at 0.9987, fragility 0.0364, corpus table 0 misses, 46/46 green). The owner's `PUMP_CERTIFY=1` train run was mid-flight throughout, so `pu85-train.log` still shows only the build line; the train in-sample number is not part of this row's gate.

## Recount and method

I read the reviewed `windows.json` entries, counted the digit string of every non-empty `.liters/.unitPrice/.total` window, and computed each non-zero, non-`csvDisagrees` asserted value's reproducing decimal placements from `expected.csv`, exactly as `PumpDisplayConventionsCorpusTests.tableCoversTheCorpus` does (`PumpDisplayConventionsCorpusTests.swift:32-59`). Over the six audited currencies this yields 923 reviewed transaction windows (the test's 947 counts every currency, not just the six).

- **Field sets** — all 18 (6 currencies x 3 fields) match the code's `volumeDecimals/priceDecimals/totalDecimals`: EUR `[2]/[3]/[2]`, RUB `[2]/[1,2]/[1,2]`, KZT `[2]/[0,1]/[0]`, GBP `[2]/[3]/[2]`, AUD `[2]/[3]/[2]`, BYN `[2]/[2]/[2]` (`PumpReadingTypes.swift:275-289`).
- **`placementsByCells`** — every tied entry matches the corpus: RUB unitPrice `{3:[1],4:[2],5:[2]}`, RUB total `{4:[1],5:[1,2],6:[1,2],7:[2]}`, KZT unitPrice `{3:[0],4:[1]}` (`PumpReadingTypes.swift:283-286`). The only non-zero 3-cell RUB total is pump-128's idle `0.00`, excluded by the zero-value guard, so the untied 3-cell total correctly falls back to `[1,2]` and is never exercised.
- **Cell counts** — all 18 `cellCounts` sets match the corpus (`PumpReadingTypes.swift:275-289`).

## The five checks

1. **Joint table equals the corpus — MET.** See the recount above; the field sets, `placementsByCells` ties and cell counts all match. The corpus test itself passed in my run: `PU.74 conventions vs corpus: 947 windows, 0 misses` (all currencies).

2. **`decimals(_:cells:)` for every transaction window; boards keep `priceDecimals` — MET, split justified.** Every transaction read uses `conventions.decimals(_:cells:)`: the three raw sets (`PumpReadingLaw.swift:72-74`), the three repair-tier sets (`:139,142,145`), the clean-board-close liters/total (`:258,260`) and the pair-tier liters/total (`:303-304`). Boards use `conventions.priceDecimals` at the two board sites (`:225,264`); the truncated-total tier uses `truncatedTotalDecimals` (`:76`), correctly a field set, not a cell tie — it is the derived-total path, not a read placement. Justification for leaving boards untied: the joint was measured on transaction windows only (the corpus test iterates `.liters/.unitPrice/.total`, `PumpDisplayConventionsCorpusTests.swift:35`); a board stand-in commits only on a clean close with no substitution, inside the currency band, and the pair tier then validates the shown price within 5 % rather than committing it; EUR boards are single-placement `[3]`; and in the reviewed corpus every RUB board coexists with a transaction price window, so the board-as-price path never reads a non-zero RUB price.

3. **Tests would fail — MET.** (a) The RUB price-tie mutation is red: `pu85-mutation-rub-price.log` records 3 issues at `PumpReadingLawExactTests.swift:126,128,132` (the `45.94`, `3602.16` and 3-cell `68.3` assertions) and `Test run with 14 tests in 2 suites failed ... with 3 issues`; with the tie restored my run shows the same test green. (b) The corpus test's new equality check (field-set `Set(read) != measured` and per-cell-count `Set(tied[count] ?? []) != shown`, `PumpDisplayConventionsCorpusTests.swift:69-79`) is load-bearing — it re-derives both on every run and would flag any drift in a tied placement or a field set. (c) The moved repair test `totalCellRepairs` now uses NOK (`PumpReadingLawTests.swift:140`), whose `[2]/[2]/[2]` row has no cell tie, so `63,75 -> 53,75` still exercises the repair tier (index 0, 6->5); it passed in my run.

4. **Measurements — MET.** `pu85-tiers-shipped.log`: annotated **126/126** (precision 1.000, 45/68 photos), live **47/47**. `pu85-live-seg.log`: segmenter **62/61**, the one wrong cell `WRONG pump-063 total got 13.19... want 13.15` (the cautioned pair total, not shipped). `pu85-train.log`: still only the build line — the owner's `PUMP_CERTIFY=1` run was mid-flight (train in-sample is not a PU.85 gate and the row does not claim a number for it). My run of `--filter "PumpReadingLaw|PumpDisplayConventions"` (executed on the already-built test bundle because SwiftPM's `.build` lock was held by that run, `DYLD_FRAMEWORK_PATH`/`DYLD_LIBRARY_PATH` pointed at the MacOSX platform): `PU.21 oracle strings: committed 790, correct 789, precision 0.998734...`, `PU.21 fragility: committed 1071, wrong 39 (0.036414...), repaired 39`, exit 0, 46/46 tests. Shipped path has zero new wrong readings (126/126 and 47/47).

5. **Docs match code — MET.** The `docs/EXTRACTION.md` PU.85 paragraph matches the code: RUB price `3->1 / 4,5->2`, RUB total `4->1 / 5,6->{1,2} / 7->2`, KZT price `3->0 / 4->1` (`PumpReadingTypes.swift:283-286`); the RUB band is 40-500, widened for the 450 Crimean AI-100 (`FuelPriceBands.seed.json`, 2024 petrol/diesel); `placementsByCells` for transaction windows, boards keep the field's set, the corpus test re-derives on every run; measured 126/126, 47/47, 62/61, pump-063's cautioned pair total — all confirmed above. New code comments are current-truth, present-tense, no task IDs/dates (`PumpReadingTypes.swift:225-229,243-244`); the test-file ratchet history follows the file's established convention. Two non-blocking wording/API notes: (a) "boards keep the field's set, which the corpus measured separately" is ambiguous — the board field is *excluded* from the corpus re-derivation and reuses `priceDecimals`; the substance (boards keep the field set, not cell-tied) is right, but "measured separately" should read "the field set the transaction windows measured". (b) `decimals(_:cells:)`'s `default` branch lumps `.board` with `.total` and would return `totalDecimals` for a board, but `.board` is never passed (boards call `priceDecimals` directly), so it is a latent trap, not a live path.

## Notes

- The owner's in-progress corpus edits are untouched; my recount and the test run read the working-tree `windows.json`/`expected.csv` as the brief directed (the corpus test itself passed against it). The mutation log's "948 windows" vs my run's "947" is the owner's in-flight annotation delta, not a PU.85 discrepancy.
- Nothing found that a row does not own. The board-path tenfold asymmetry noted in check 2 is the row's stated scope (transaction windows only), bounded by the clean-close + band + 5 % pair validation, and does not occur in the reviewed corpus.
