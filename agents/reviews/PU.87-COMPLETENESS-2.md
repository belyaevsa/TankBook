# PU.87 completeness re-check — 2026-09-25

Scope: only the six disputed findings in `PU.87-COMPLETENESS.md`. Read-only review; no implementation, corpus, or task status changed.

1. **Gate — causally explained, but the required green gate is still absent.** `PumpRowAssignmentTests.swift:22-24,40-56,62-76` scores the annotated windows and geometry; it never loads a locator. `PumpReaderTestSupport.swift:17-18` points it at the owner's modified `Spike/.../windows.json`. On the committed corpus, `/tmp/agentlogs/pu87-assignment-committed-corpus.log:1-2` reports 1319/1330 (0.992), 5/5 passed. The other full-run failure is the Vision `e5rtError` at `/tmp/agentlogs/pu87-gate-3.log:12428-12430`; `/tmp/agentlogs/pu87-capture-orientation-isolated.log:1-3` shows three isolated passes, and `docs/TASKS.md:980` assigns the recurring parallel-test failure to RV.306. These isolate both failures outside PU.87 without touching the owner's corpus. The app bundle separately passed 303/303 (`/tmp/agentlogs/pu87-app-2.log:1714-1725`), and the full gate's build, lint, generation and app build exited 0 (`pu87-gate-3.log:3,2358,2363,6791`). Still, `pu87-gate-3.log:12531,12645,12648` records package tests 2309 tests/299 suites with 2 issues, localization target 45/45, and `swift test` exit 1; the gate stopped before its app bundle. Therefore the evidence discharges **PU.87 causation**, but does not meet the literal “Gate green” acceptance line in `docs/TASKS.md:1103`. A passing gate needs the owner corpus settled and RV.306 addressed; neither is a permissible PU.87 review edit.

2. **Rule 12 — withdraw review 1's finding.** The touched `PumpReaderPipelineTests.swift:619-620,652` does print got/want amounts. Its input is the repository fixture path (`PumpReaderTestSupport.swift:17-20`), and the existing annotated scorer already prints got/want in `PumpReaderPipelineTests.swift:265-266,284`. `docs/LOGGING.md:3,7,26-29` frames privacy classification around the app and server logging tiers, including device and production log handling; `CLAUDE.md:160` says “Never log domain values” but does not define test-harness output as a tier. A local test report over fixture expected values is not app/server logging. There is no cited line in either document that makes this test output a tier, so the prior rule-12 blocker was an overextension.

3. **Strides — code fixed; green test evidence missing.** `PumpRowSegmenter.swift:71-95` checks row-major contiguous strides before reading the raw float16/float32 pointer and uses indexed `MLMultiArray` access otherwise. `PumpRowSegmenterParityTests.swift:77-88` builds a noncontiguous 2×3 float32 array with strides `[1, 2]`, expects logical order, and also checks the contiguous case. The named mutation went red at that assertion (`/tmp/agentlogs/pu87-mutation-strides.log:1-3`). None of the supplied logs records a passing `floatsFollowStrides` run, so the requested red-then-green evidence is incomplete.

4. **Camera path — directly covered in code; passing run not supplied.** `PumpRowSegmenterParityTests.swift:51-74` creates a 32BGRA `CVPixelBuffer`, draws the fixture image into it, calls `rows(in: pixels)`, then checks count and quad parity with `rows(in: image)`. That exercises `PumpRowSegmenter.swift:60-65` and its VideoToolbox conversion directly. It is a synthetic frame test, not a device-camera measurement. None of the supplied logs names a passing `pixelBufferMatchesImage` run.

5. **Comments — partly fixed, with a remaining explicit rule violation.** `PumpRowSegmenter.swift:7-16` now says the app bundles RowSeg; `PumpRowDetector.swift:7-13,79-88` describes both locator backends and the distinct pixel-buffer paths. But `PumpRowDetector.swift:10` still says the object detector is one the segmenter “replaced,” a before/after story disallowed by `CLAUDE.md:211-212`. `PumpReaderOrientationTests.swift:14-16` still records named mutations and a red-then-green report claim, which is completion evidence in a code comment under the same rule. `PumpReaderPipelineTests.swift:61-64` embeds the measured “one wrong cell” result; `CLAUDE.md:217-219` says not to copy mutable test totals or accuracy facts into comments. These are limited comment edits, but review 1's comments item is not fully discharged.

6. **Evidence — timing and mutation red logs supplied, green evidence partial.** `PumpLocatorTimingTests.swift:12-40` times both locators over heldout stills in an opt-in optimized build; `/tmp/agentlogs/pu87-timing.log:1-2` reports 12 stills, object detector 9.5 ms, segmenter 26.5 ms, 1/1 passed. `docs/TASKS.md:1103` reports the two-run ranges 9.1–9.5 and 26.5–29.8 ms; the second run is attested there, not independently in the supplied timing log. Red mutation output is present in `/tmp/agentlogs/pu87-mutation-strides.log:1-3`, `pu87-mutation-orientation.log:1-4`, and `pu87-mutation-uncautioned.log:1-5`. The normal orientation and live-path tests passed in `pu87-gate-3.log:12483,12529`. No supplied log identifies a green run of the new stride or camera-input tests. These logs do not record shell exit codes for the isolated and mutation commands. No new builds, UI suites, localization checks, screenshots, or manifest checks were run during this read-only re-check.

**INCOMPLETE.** The gate's PU.87-specific failures are ruled out, rule 12 does not apply to these fixture diagnostics, and the stride and camera branches have direct tests. The literal green gate and green runs of the two new input tests are not evidenced; the updated comments still contain history and mutable measurement claims barred by `CLAUDE.md:211-219`.
