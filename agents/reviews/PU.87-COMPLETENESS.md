# PU.87 completeness review — 2026-09-25

Scope: `git diff HEAD` and the three named untracked implementation paths. I excluded `Spike/` and `ml/pump-reader/runs/` from the change review. This is a read-only review; no implementation or task status was changed.

## Findings

1. **MISSING — the required full gate is red.** `/tmp/agentlogs/pu87-gate-3.log:12648` records `[gate] tests   exit 1   swift test`; the app unit bundle was therefore not reached by that gate. The two issues are `PumpRowAssignmentTests.swift:76` (0.9882697947214076 against 0.99; log lines 8291–8294, with owner corpus changes under `Spike/`) and `CaptureOrientationTests.swift:41` (Vision `.e5rtError`, log lines 12428–12430). Their causes may be unrelated to PU.87, but neither has a passing isolated rerun in the supplied evidence. `pu87-app.log:3920–3931` independently shows the app unit bundle 303/303 green. The baseline gate still needs a clean, verified exit 0 after the corpus is settled and the Vision failure is isolated.

2. **MISSING — hard rule 12 in a touched diagnostic.** `ios/Tests/TankbookCoreTests/PumpReaderPipelineTests.swift:619–620` builds a wrong-cell diagnostic with `got` and `want` amounts; line 652 prints it. The line was modified by PU.87, and `/tmp/agentlogs/pu87-tiers-2.log:69` demonstrates the resulting amount values in output. `CLAUDE.md:158–160` forbids logging amounts at any level in any build. Keep the caution marker and field name; remove actual and expected domain values from this diagnostic (and the same output path).

3. **PARTIAL — fast map conversion assumes contiguous storage.** `ios/Sources/TankbookCore/Extraction/PumpReader/PumpRowSegmenter.swift:69–85` handles float16, float32 and a boxed fallback, but the first two cases walk `dataPointer` linearly without checking `MLMultiArray.strides`. A strided `maps` backing would be decoded in the wrong order. The supplied live results establish the current model/runtime path, not every backing. Use a stride-aware fallback when the layout is not contiguous and cover that case directly. The pixel-buffer method at lines 61–65 converts with `VTCreateCGImageFromCVPixelBuffer` and calls the CGImage path, but the signed UI test feeds a decoded image (`ios/App/Sources/Capture/PreviewGuidance.swift:151–160`); no supplied test drives the camera `CVPixelBuffer` branch at lines 163–168.

4. **MISSING — production comments do not describe current truth.** `ios/Sources/TankbookCore/Extraction/PumpReader/PumpRowSegmenter.swift:15` still says “the app does not load it”; `PumpRowDetector.swift:7–12` calls this an object detector, and lines 78–81 say the preview hands its buffer to Vision without a CGImage copy. Those statements conflict with the segmenter backend at `PumpRowDetector.swift:29–31,70–86` and app load at `CapturePipeline.swift:30–32`. `PumpReaderOrientationTests.swift:6–13` also describes only geometry selection and an old mutation, and the new comment at `PumpReaderPipelineTests.swift:61–64` records a task ID/date/outcome. `CLAUDE.md:202–215` requires current behavior and excludes task history and measured totals from code comments. Update the comments with the code.

## Requested checks

| Check | Verdict | Evidence |
| --- | --- | --- |
| 1. App bundle and backend | **MET** | `project.yml:40–44` includes app resources; `CapturePipeline.swift:30–32` requests compiled `RowSeg`; `PumpDisplayCapture.swift:113–116` calls `PumpRowDetector.load`; `PumpRowDetector.swift:56–63` selects segmenter by the compiled model's `maps` output, then lines 70–86 dispatch both image and buffer detection. `DigitRows` now lives at `ml/pump-reader/detector/DigitRows.mlmodel`; no executable app-path reference remains. The app build generated `RowSeg.swift` (`pu87-app.log:1559`). |
| 2. Preview, compute units, map storage | **PARTIAL** | `PreviewGuidance.swift:163–168` sends camera buffers through `PumpReaderHandle.detectedRows` (`PumpDisplayCapture.swift:441–444`) to `PumpRowSegmenter.rows(in: CVPixelBuffer)` (`PumpRowDetector.swift:82–86`; `PumpRowSegmenter.swift:61–65`). `.cpuAndNeuralEngine` at segmenter lines 32–38 matches the simulator zero-map finding recorded in `docs/EXTRACTION.md:1026–1027`. Direct buffer coverage and stride-safe map conversion are missing as above. |
| 3. Honest gate and cautioned cell | **MET** | `PumpPhotoGate.swift:84–100` is 62/61. `PumpReaderPipelineTests.swift:65–67,103–110` has exact committed count, separate zero-uncautioned-wrong assertion, and precision floor 0.98. `measureLive` at lines 575–585 uses the app classification path without annotation rotation; lines 609–620 score committed cells and count absent cautions. `/tmp/agentlogs/pu87-tiers-2.log:34–70` reports annotated 126/126, live 62/61 and only pump-063 total wrong, marked cautioned. `CapturePipeline.swift:98–102` carries the caution to Confirm; `ManualFillUpView.swift:168` renders it. The hard-rule-12 diagnostic finding remains separate. |
| 4. Orientation decision | **MET** | `PumpReader+Orientation.swift:41–58` first chooses by geometry and compares only that pick with its 180-degree opposite, only when both candidates keep rows. Lines 101–118 compute the mean classifier margin over kept cells; higher opposite margin wins, ties stay with geometry. `PumpReaderOrientationTests.swift:30–51` checks pump-019 picks 90 and matches an explicit-90 read, plus an upright control. The test is discriminating for that fixture, although the supplied logs do not contain the claimed red-then-green mutation output. |
| 5. DigitRows move | **MET** | `PumpReadTool/main.swift:42,207` defaults to `RowSeg` and accepts either model through `load`; `scripts/pump-auto-annotate.py:57` points to `RowSeg`; `tools/pump-annotate/server.py:114,135,156–169` marks RowSeg as shipped and lists the moved object detector and candidate packages; `PumpTraceParityTests.swift:16` uses the moved `DigitRows`. The old detector remains selectable, not loaded by the app. |
| 6. Decision 10 and comments | **PARTIAL** | `docs/EXTRACTION.md:1012–1033` agrees with the backend, 62/61, caution, 6/116 leak, 1.8 MB size and the Release timing stated in `docs/TASKS.md:1103`. The stale/history comments in finding 4 violate `CLAUDE.md`'s code-comment rule. |
| 7. Hard rules 13, 15, 12 | **PARTIAL** | Rule 13: `CapturePipeline.swift:91–104` produces `ConfirmPrefill`; `ManualFillUpView.swift:168` shows the caution alongside editable fields (`PumpReadingCautionNotice.swift:4–7`). Rule 15: the typed door still opens at `CaptureView.swift:208–213`, with a review-screen typed door at lines 374–385. The touched test output violates rule 12 as described in finding 2; no new production logging of domain values was found. |

## Observed checks and limits

- Supplied `/tmp/agentlogs/pu87-gate-3.log`: `swift build` exit 0; root `swiftlint lint` exit 0; `xcodegen generate` exit 0; Debug app build exit 0; `swift test` exit 1 with 2 issues across 2309 tests/299 suites (plus localization test target 45/45); app-target unit bundle not reached in that invocation. Separate `/tmp/agentlogs/pu87-app.log`: app unit bundle 303/303, `TEST SUCCEEDED` (shell exit code not recorded in the file).
- `/tmp/agentlogs/pu87-tiers-2.log`: focused 12/12 passed, annotated 126/126, live 62 committed/61 correct, one cautioned wrong. `/tmp/agentlogs/pu87-leak-2.log`: 1/1 passed, 6/116 routed, zero commits. The log files do not record their shell exit codes.
- `/tmp/agentlogs/pu87-ui-4.log`: signed `CaptureGuidanceUITests` 2/2 and `TEST SUCCEEDED`; shell exit code not recorded. It uses the test image feed, not a real camera pixel buffer. No screenshots were captured or visually assessed in this review.
- The row at `docs/TASKS.md:1103` records Mac Release locator medians 9.1 ms (object detector) and 29.8 ms (segmenter); the named supplied logs contain no timing-test output or independent exit code. iPhone timing remains unmeasured.
- No evidence supplied for a named orientation mutation's red-then-green output, localization gate key count/RU percentage, EN/RU dark screenshots, screenshot manifest exit code, or separate runs of the two failing package tests. I did not run build, mutation, UI, localization, or screenshot commands in this read-only review.

## Verdict

**INCOMPLETE.** Remove amount values from the touched diagnostic, make `floats(_:)` correct for strided arrays and directly exercise the camera buffer path, correct stale code comments, then obtain a full gate exit 0 (including isolated resolution of the two observed failures). Attach the missing timing, mutation and standing-check evidence required for ship verification. The known cautioned wrong cell is disclosed and owner-authorized; it is not itself a hidden precision failure.
