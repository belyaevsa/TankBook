# Built panels: regenerate with `python3 design/store/build.py`. The specs and
# the source screenshots are what is versioned; the composites are output.
out/

# Derived inputs: the rounded-corner screenshot copies and the shared blurred
# background, rebuilt by build.py from committed sources.
.cache/
