# Tankbook – Task Backlog

*`PHASES.md` broken into agent-sized tasks. Each task names its deliverable and the **checks that make it done** – a task without its checks green is not done, and checks land in the same PR as the code (TESTING.md levels: L1 unit · L2 contract · L3 sync-scenario · L4 UI/snapshot · L5 accuracy · M manual). Task IDs are stable – reference them in branches/PRs (`p0-4-grdb-migrations`).*

## Implied in every task's checks: it builds and it lints

The per-task checks below are **in addition to** the baseline gate, which is never restated per row because it applies to all of them: every tier a task touches must compile and its linter must exit **0** (`CLAUDE.md` hard rule 14, `docs/TESTING.md` → the baseline gate). Run `swiftlint lint` **from the repo root**, and judge it by exit code. A task whose feature tests pass but which leaves the build broken or the linter red is **not** done and does not get an `[x]`.

## Status legend

`[x]` done and independently verified – its own checks **and** the baseline gate re-run outside the implementing agent · `[~]` partially landed, gap named · `[ ]` not started · `[!]` **blocked on an action only the product owner can take** (a purchase, a legal decision, an account) - not work an agent can pick up · `[cut]` **decided against, with the reason recorded in the row and in the owning doc** – a cut task is not work waiting to be picked up, and re-proposing it needs the recorded reason to have changed. Status is recorded here so a fresh session knows what to pick up without re-deriving it.

## P0 · Foundations

| ID | Task | Checks (done when…) |
|---|---|---|
| **[x]** P0.1 | iOS scaffold: SwiftPM package `TankbookCore` (all pure logic + persistence), SwiftLint, CI workflow. **Decided during P0:** the core is a package, not app-target code – tests run fast and simulator-free in CI, and widgets/extensions can reuse it. The SwiftUI app target and its UI-test target arrive with P1.1. | CI builds a clean checkout; lint green; one placeholder test runs in CI |
| **[x]** P0.2 | Design tokens: `design/tokens.json` extracted from DESIGN.md → generated `Theme.swift` (script, not hand-copy) | L1: generated values equal tokens.json; custom lint rule fails any literal hex in UI code |
| **[x]** P0.4 | GRDB setup + migrations for the full SCHEMA.md model (all entities, envelope, tombstones) | L1: migrate-from-empty and migrate-from-seeded round-trips; CRUD per entity; soft-delete filtering |
| **[x]** P0.5 | Domain types: `Money`, enums, `ConflictState`, IDs (UUIDv7) | L1: Money semantics suite (rate direction, snapshot immutability, backfill fill-blanks, edit clears) |
| **[x]** P0.6 | Consumption engine (segments, headline, lifetime, cost/km, EV) | L1: D1–D4 golden vectors verbatim + edit-cases (volume edit, isFull split/merge, date reorder) + distance-weighting check |
| **[x]** P0.7 | Timeline validation engine (order, pace, receipt-date priority) | L1: full check-matrix incl. save-anyway flagging and segment exclusion |
| **[x]** P0.8 | Backend solution scaffold: ASP.NET Core, health endpoint, local Postgres+MinIO scripts, CI | CI: build + test green from clean checkout; L2 smoke test hits /health via Testcontainers |
| **[x]** P0.9 | Server migrations: accounts, devices, records, account_seq, blobs, llm_usage, exchange_rates, vehicle_catalog, feedback | L2: apply+rollback; constraint tests (uniques, per-account SCN monotonicity under concurrent tx) |
| **[x]** P0.11 | **Logging foundations** (`docs/LOGGING.md`): backend structured JSON logging with the request line + per-operation events + redactor; iOS OSLog categories with the mutation begin/outcome pair, sync-cycle events, and the breadcrumb ring; `X-Tankbook-Trace` correlation end to end | L1: redaction test both tiers – a fully populated entity through the log path leaks no Sensitive/Never value. L2: traceId from a client request appears in the server request line, operation line, and problem+json body. L1: every mutation path emits `begin` + terminal `ok`/`fail` with errorCode; log volume is O(1) per sync batch, not O(n) per record |
| **[ ]** P0.12 | **Remote config, client** (`docs/CONFIG.md`). **Split into three slices after three single-run attempts delivered nothing** – the task was too large for one agent run, and each slice below is independently testable. P0.12 is done when a, b and c are all green | The union of the three slices' checks |
| **[x]** P0.12a | Canonicalization (the six rules in `backend/…/ConfigCanonicalizer.cs`) + Ed25519 verification + typed `ConfigDocument` preserving unknown keys | L1 **parity**: `Fixtures/config/parity.document.json` canonicalizes byte-for-byte to `parity.canonical.json` (788 bytes, generated by the real C# canonicalizer). L1: signature accept; tampered document / tampered signature / wrong key all rejected. L1: number **source tokens** preserved (`1e3`, `9007199254740993`) asserted on bytes, not parsed values. L1: ordinal key sort at every level; non-ASCII raw; malformed input throws |
| **[x]** P0.12b | Bundled `Config.default.json`, atomic JSON cache excluded from backups, three-layer resolver with per-key sparse override, typed injected `ConfigStore` with snapshot semantics, Ed25519 verification **on every cache read**, Keychain rollback floor, document expiry | L1: bootstrap with no cache and no network uses bundled defaults and the app is usable. L1 tamper: edited document or signature rejected **on read** → bundled defaults; version below the Keychain floor rejected; expired document rejected; the server's transient empty-signature baseline (migration 003) rejected. L1: unknown key ignored for resolution, retained in cache, rest of the document applies. L1: a document setting one key leaves every other resolved value at its bundled value. L1: truncated cache file falls back cleanly |
| **[x]** P0.12c | `apiBaseUrl` guardrails: compiled-in host allowlist, health-gate before adoption, auto-revert on N consecutive failures, and the HTTP client's request-time allowlist + host-bound `Authorization` | L1 **brick-proof**: unreachable `apiBaseUrl` + N failures auto-reverts to bundled and recovers. L1: non-allowlisted host rejected by the HTTP client **and no `Authorization` header ever constructed for it**. L1: allowlist near-miss (`evil-tankbook.live`, `tankbook.live.evil.com`) rejected against a `tankbook.live` suffix rule. L1: failing candidate URL discarded, previous value stands |
| **[x]** P0.13 | **Remote config, server** (`docs/API.md` → `GET /config`): signed document served with ETag/304, versioned and monotonic, staged-rollout fields, maintenance notice as inert plain text; signing key in the platform secret store | L2: 304 on matching `If-None-Match`; document validates against its own schema; signature verifies with the published public key; a document with a lower version than the previous is refused at publish time |
| **[x]** P0.10 | **Payload contract** (`SYNC.md` → Payload contract and versioning): generate `docs/schemas/v1/*.schema.json` from the domain model; `payload_schemas` + `payload_migrations` tables seeded by migration; server-side envelope + JSON Schema validation on push with the typed rejection codes; iOS encode/decode against the same schemas; upcaster scaffold + fixture corpus | L1: every entityType has a v1 schema (build fails otherwise); every fixture validates against its version. L2: push rejects oversize/malformed/schema-violating payloads with the right code and JSON pointer; unknown entityType accepted unvalidated; `minSupported` gate returns 426 on push while pull still succeeds. L1+L2 parity: Swift upcaster and server declarative transform produce byte-identical output for every fixture. Round-trip: unknown fields and unknown entity types survive decode→encode unchanged |

## P1 · Local core loop

| ID | Task | Checks |
|---|---|---|
| **[x]** P0.3 | String Catalogs EN/RU + pseudo-localization CI step. **Moved here from P0** – `TankbookCore` has almost no user-facing strings, so gating a P0 build on localization would gate a build with nothing to localize. The strings arrive with the app target, so this lands alongside P1.1. ID kept stable for branch/PR references | Build fails on a deliberately hardcoded string; sample keys resolve in both languages |
| **[x]** P1.1 | App shell: tab roots, navigation stacks, sheet infrastructure per SCREENMAP conventions | L4: XCUITest walks every SCREENMAP edge that exists so far; back-path audit (no dead ends) |
| **[x]** P1.2 | Add car screen + bundled catalog seed pack + suggestions | L1: catalog lookup/pre-fill; L4: snapshot + ERRORS states (empty name, implausible odometer, offline hint) |
| **[x]** P1.3 | Manual fill-up form (ConfirmManual) + save path | L4: snapshot all states incl. currency chips; L1: third-value derivation, crossCheck=.notApplicable |
| **[x]** P1.4 | Home: garage card, vitals, guest/empty/first-estimate states | L4: snapshots ×(guest, 1-fill, full) ×(dark, light) ×(EN, RU); "no N/A tiles" assertion |
| **[x]** P1.5 | Log stream: entry cards, month dividers, purchase-group rendering | L4: snapshot; L1: ordering (date) and group collapse |
| **[x]** P1.6 | Edit entry + full-vehicle recompute + delta toast | L1-through-UI: edit-case goldens via the real store; toast shows old→new |
| **[x]** P1.7 | Recently deleted + sync-overwritten section + 30-day purge | L1: tombstone restore returns entry to stats **exactly**; purge honors grace (both sides of the 29/31-day boundary); purge idempotent; purged rows gone from the backup stream; days-remaining countdown (deleted today = 30, never negative); deleted entry excluded from stats while deleted | L4: list + countdown, Restore removes row and entry reappears in Log; Delete-all system confirmation, cancel leaves intact, confirm empties; empty state, no fabricated rows; sync-overwritten section with Compare affordance. Screenshots EN + RU (RU plural: 1 день / 4 дня / 27 дней) |
| **[x]** P1.8 | Duplicate detection (S2 heuristic) + conflict badges (F9a UI) | L1: heuristic boundary cases; single-count-until-resolved invariant; L4: badge + combined card snapshots |
| **[x]** P1.9 | Tank-level sheet + segment adjustment math | L1: tank-level suite (incl. no-capacity fallback); L4: snapshot with/without capacity |
| **[x]** P1.10 | Trends tile grid + honest labels + excluded-entry footnote | L1: window/floor/label logic; L4: snapshots incl. "first estimate" and extended-window states |
| **[x]** P1.11 | Car switcher + multi-vehicle state | L1: capture-logs-to-selected-car invariant; switch persistence; L4: sheet snapshot incl. archived row |
| **[x]** P1.12 | **Vehicle detail: per-car settings, editable** (`CLAUDE.md` hard rule 13 – the app suggests, the user decides). Every value Add car pre-filled from the catalog or the locale – name, make/model/year, powertrain, fuel kinds, tank capacity, battery size, home currency, units, `initialOdometer` – changeable afterwards on the car in the Garage (`docs/DESIGN.md`: per-car settings live here, never in Settings). **Moved out of P6.4**, where the screen was scheduled with the rest of the polish: until it exists a user can correct a suggested value exactly once, at Add car, and never again – which breaks the rule outright | L1: editing a suggested value persists and full-vehicle recompute follows (hard rule 2); editing `tankCapacityL` changes partial-fill math and the volume-vs-capacity warning on Confirm. L1 **override permanence**: a user-edited value is not overwritten by a later catalog pack (`SYNC.md` → Reference data), and is preserved across a sync merge. L4: every catalog-derived field is reachable and editable; snapshot dark+light, EN+RU |

## P2 · Capture

| ID | Task | Checks |
|---|---|---|
| **[x]** P2.1 | Capture camera screen: modes, auto-detect surface, permission fallback | L4: F8 denied-permission flow (manual form + deep link); mode row snapshot; "Type it" path |
| **[x]** P2.2 | Port Spike parser into the app module; fixture corpus in CI with ratchet | L5: existing suite green; high-water mark recorded; every future fix adds its fixture |
| **[x]** P2.1b | **Wire Capture into the app.** The artboard's centre capture button in the tab bar was never built, and every `onCapture` callback opens the manual form instead, so `ModalRoute.capture` was set only by the `#if DEBUG` launch hook - the screen was dead code in Release. This also meant the app offered one door, not two (hard rule 15) | L4: a UI test reaches Capture **without any launch argument** - that assertion is what stops this recurring; centre button matches `design/screens/*.dc.html`; `SCREENMAP.md` line "Home -->\|capture button\| Capture" is true |
| **[x]** P2.2b | **`FuelExtraction` types money as `Double`.** `total` and `unitPrice` are `Double?`, but `docs/SCHEMA.md` types money as `Decimal` (`Money.amount`, `FillUp.unitPrice`) and P2.6's fiscal parser does it correctly. Feeding a `Double` into a `Decimal` field loses precision at the boundary - `4201.68` does not survive a binary float round-trip. Caught after P2.2 merged; its validator only checked the fiscal module for money-valued `Double` | L1: a value like `4201.68` round-trips exactly from extraction to a saved `FillUp`; no money-valued `Double` remains in `Extraction/` |
| **[x]** P2.3 | Confirm sheet: pre-fill, confidence dimming, tap-to-verify crops, cross-check lock animation. The scanned path lands in the **same** ConfirmManual sheet (hard rule 15): an optional `ConfirmPrefill` (extraction + per-field crops + fiscal QR anchor) pre-fills present fields, nil fields stay blank and focusable, resolved-but-unconfirmed fields dim to 60% opacity yet stay fully editable (hard rule 13), the QR total outranks the OCR total (disagrees → QR fills the field; mixed receipt → fuel line stands, hard rule 4), the lock animates under Reduce Motion as a plain state change, and `Double` enters the form only through `Decimal(string:)` on a formatted string (P2.2b owns the `Decimal` type fix in `Extraction/`) | L1: gating thresholds; L4: verified/mismatch/dimmed snapshots; reduced-motion variant |
| P2.3b | **The Fuel row offers fuels the car cannot burn.** Confirm shows a `95 / Diesel` chooser because the seed car is `fuelKinds: [.petrol95, .diesel]` - a car that burns both, which does not exist. `docs/DESIGN.md` already makes fuel kind **conditional for display** ("a diesel-only car printing Diesel on every row is noise dressed as information"); the same rule must govern **input**. Petrol grades (92/95/98/100) share a tank and are a real driver's choice; **LPG/CNG/E85 alongside petrol are real** (bi-fuel and flex-fuel); **diesel with petrol is not**. So: the row offers exactly `vehicle.fuelKinds`, and with a single kind it is a static value rather than a chooser - while staying correctable, because hard rule 13 forbids a value the user cannot change. Note `fuelGrade` is separate and still varies for a diesel car (the corpus has `ДТ-Е-К5 Танеко` and `АТ-Л-К5 Ультра`) | L1: a diesel-only car is never offered petrol, and vice versa; a petrol car with 92+95 is offered exactly those two; a single-kind car renders no chooser yet the value is still editable; AddVehicle rejects diesel+petrol while allowing petrol+LPG/CNG/E85; the seeds stop describing impossible cars |
| [~] P2.4 | Mixed-receipt detection + "Also on this receipt" UI + grouped save. **Detection, the "Also on this receipt" section and grouped save landed and are L1/L4-verified; the `≥95% fuel-line isolation on mixed corpus` L5 gate is NOT met and stays pending.** The corpus has exactly one mixed fixture (`receipt-009`), and a percentage over one fixture is arithmetic, not a gate (the same lesson as the earlier "3/3 over one photo"). | L1: `receipt-009` isolates the fuel line (6135.24, never 6264.00); the 129.00 collision (water total vs fuel price-per-litre) is distinguished by operand position + product name, not by a repeated number; FillUp+accepted Expenses share one `purchaseGroupId` and the logged sum ≤ the receipt total (property-style over compositions); a dismissed line persists no Expense row; non-mixed corpus fixtures are never flagged; a mixed receipt with no QR is still detected by line structure. L4: the section renders only when lines are detected and the sheet stays the ordinary form otherwise. **L5: pending more mixed fixtures** - the corpus now has TWO (`receipt-009` fuel+water, `receipt-025` fuel+service fee), which is enough to exercise the detector against a second shape but still not enough for a percentage. `receipt-025` is now classified by the fiscal cross-check (its QR grand total exceeds the fuel line), but its detection by **line structure** - the path that works on the 20 of 32 receipts with no usable QR - is still unasserted, and that is what this gate needs. **A THIRD mixed fixture landed 2026-08-26**: `screenshots/screenshot-008-circlek-jugla-lv-mixed.png` (Circle K Jugla, Rīga) - eight lines, the fuel line **third**, Latvian item names, and it **reconciles completely** (fuel 112.63 + non-fuel 10.36 = 122.99 total; fuel discount 2.39 + shop discount 0.78 = the printed 3.17). It is the corpus's first mixed document that is not a photograph and not in Russian, and the parser currently returns the **grand total 122.99** for it - a live hard-rule-4 violation this gate exists to catch. Its discriminator is the unit token, `L` against Latvian `Gab.` (pieces), which the vocabulary did not have. |
| **[x]** P2.4b | **Wire `FiscalDocumentIdentity` into duplicate detection.** `receipt-015` and `receipt-026` are two genuine fills - different stations ~100 km apart - with the **same chain, day, volume and total to the kopeck**. `DuplicateDetector.isDuplicate` keys on same-vehicle / within-30-minutes / volume-within-5%, and these are **34 minutes apart**, so they escape a false merge offer **by four minutes**. P2.6 already built `FiscalDocumentIdentity` (`fn`+`i`+`fp`) and deliberately left it unwired: two different fiscal identities are a **proof** the purchases differ, not a heuristic | L1: two fills with different fiscal identities are never offered as duplicates however identical their amounts; identity absent (no QR - 14 of 26 corpus receipts) falls back to the existing heuristic unchanged; the S2 invariant that an unresolved pair counts once still holds |
| P2.9 | **Resolve an unmarked operand pair by decimal places.** `receipt-033` (KZ) prints `24.690 Х 243.00=6000.00` - no `л`, no currency, no labelled column - so the ladder returns nil for both volume and price (hard rule 13: it will not guess which operand is which). But the operands are **not** symmetric: the volume carries **three** decimals and the price two, and litres-to-the-millilitre is a real CIS receipt convention. Unlike the `25,52 X 70.92` tie that P2.2 correctly refused - both operands there had two decimals - this is a signal in the document itself. Add it as a ladder step **above** price bands and user history, and only when **exactly one** operand has three decimals. **Counter-evidence added 2026-08-26 - read before implementing.** The five Circle K app screenshots (`screenshots/screenshot-004` .. `-008`, EE/LV/LT) invert the rule exactly: the **price** carries three decimals (`1.884`, `2.159`, `1.799`, `1.614`, `1.924`) and the **volume** often carries none (`67 L`, `68 L`, `64 L`). So "three decimals -> volume" is a CIS convention, not a general one, and applied to a Baltic document it swaps the pair - the precise failure `receipt-007` already demonstrates. The step must therefore be **gated on the same signal that makes it true** (a three-decimal operand whose magnitude is implausible as a per-litre price, or a document already localised to a CIS format), and the vacuous-assertion trap is testing it only on CIS fixtures. Those five screenshots are the regression set | L1: `receipt-033` resolves 24.690/243.00 for that reason; a pair where BOTH operands have three decimals, or neither, still returns nil; `receipt-007` (the documented swap) does not regress; L5: receipts hits rise and the high-water mark is raised in the same change |
| **[x]** P2.10 | **KZT is never detected.** `receipt-033` prints its total in tenge and the marker `тг` OCRs as `гг`, so `FuelExtraction.currency` stays nil on every KZ receipt. Money is a pair (hard rule 3) - a fill-up with no detected currency cannot be converted, and the Confirm sheet must then ask rather than assume the vehicle's home currency. Add KZT (and the OCR confusions `гг`/`тr`/`ТГ`) to currency detection, and check the other CIS codes the corpus implies | L1: `receipt-033` resolves KZT; the `гг` misread resolves; a receipt with no currency marker still returns nil rather than guessing; L4: a nil currency asks on the Confirm sheet (P2.5's low-confidence path) rather than silently converting |
| **[x]** P2.11 | **Mixed-script OCR of Cyrillic receipts.** `receipt-034`'s product line reads `Plus (AИ-95-К5), л` - Vision returns a **Latin A** followed by a Cyrillic И, so the octane regex `А[ИН]` (Cyrillic) misses it and the fuel kind resolves to nil. The same confusion class covers О/O, Р/P, С/C, Е/E, Х/X, К/K, М/M, В/B, Н/H, Т/T, А/A - homoglyphs Vision picks by shape, not by the document's language. Normalise homoglyphs to one script before matching, in the extractor only (never in stored text: a station name must survive as printed) | L1: `receipt-034` resolves petrol95 from the Latin-A line; a genuinely Latin product line (`DIESEL`, `G-DRIVE 95`) still resolves; the normalisation is not applied to `station`/`vendor` text; L5: receipts hits rise and the high-water mark is raised in the same change |
| **[x]** P2.12 | **The cross-check has four outcomes, not two** (`docs/EXTRACTION.md` -> Cross-check). Today `liters x unitPrice != total` is a binary lock/mismatch, and on loyalty receipts it is systematically **mismatch while every field was read correctly** - all five Circle K screenshots miss by exactly the printed discount (`screenshot-004`..`-008`). Add **reconciled** (the residual equals a discount line on the document) and surface the residual in words rather than a bare amber state. Also add the mixed discriminator this batch exposed: the unit token `L` against Latvian `Gab.` on `screenshot-008`, whose fuel line is the **third** of eight, so neither "first priced line" nor "line nearest the total" finds it | L1: each of the five screenshots reaches `reconciled`, not `mismatch`, and a genuine mismatch still does; a receipt with no discount line and an unexplained residual stays `mismatch`; `screenshot-008` returns the fuel line 112.63, never the grand total 122.99 (hard rule 4); the residual message names its next step (hard rule 7). **Vacuous trap**: asserting the outcome enum without asserting the value the fields resolved to - a `reconciled` that reconciles to the wrong number is the bug this is meant to prevent. L5: screenshots hits rise and the high-water mark is raised in the same change |
| **[x]** P2.13 | **Cross-multiplication as seven-segment digit repair** (`docs/EXTRACTION.md` -> digit repair). When `liters x unitPrice` misses `total` by roughly one least-significant step of one operand, the cause on a pump display is one misread segment, not three errors: `pump-015` reads `1.884` under glare where `15.89 x 1.889 = 30.02` exactly, and the same correction resolves `pump-013` (`7.34 x 1.779 = 13.06`). Substitute the confusable pairs 4/9, 8/9, 8/6, 8/0, 3/9, 5/6, 1/7 and accept a repair only when **exactly one** substitution closes the arithmetic. Pump source only - thermal print has no segment topology | L1: `pump-015` and `pump-013` repair to the ground truth in `expected.csv`; a case where **two** substitutions both close returns nil rather than picking one; a receipt-source document is never repaired; the repaired value is returned as a suggestion with its confidence lowered, never as a locked field (hard rule 13). **Vacuous trap**: testing only the cases that repair - the two-candidate refusal is the half that keeps this from inventing digits. L5: pump hits rise and `PumpPhotoGate.measuredHits` is updated in the same change |
| **[x]** P2.14 | **`FuelExtractor.modal` is non-deterministic, and it destabilises every accuracy number in the project.** The total-finder resolves ties with `Dictionary(grouping:)`, whose iteration order Swift does not guarantee, and then `modes.max(by:)` on equal `primaryCounts` returns whichever element that order left last - with `?? modes.first` order-dependent too. So when two total candidates tie on count **and** on primary-label count, the answer is whatever the hash seed decided. Found by P4.13, which required three sweeps per image and watched the receipt score flip between **29/96 and 30/96** across identical runs (`receipt-021`, `-026`, `-029`). **This is not only a parser bug: `high-water.json` and `PumpPhotoGate.measuredHits` are gates asserted against a live score, so a tie-break that moves makes CI itself intermittently red - and it means the receipts mark cannot safely be raised from its stale 45/93 to the live 46/96 until this lands.** The fix must be deterministic **and the tie-break must be chosen deliberately and stated**, not just stabilised: the strongest candidate is to **return `nil`**, because the parser already refuses rather than guesses elsewhere (a printed `0.00` becomes `nil`, an unmarked operand pair returns `nil`) and hard rule 13 says a confident wrong value is worse than an empty field the user fills. If refusing lowers the score, that is **information, not failure** | L1: a fixture whose totals tie on both counts returns the **same** answer across 100 runs **in separate processes** - one process shares a hash seed and would pass while the bug is live; the chosen tie-break is asserted **by name** (nil, or the stated rule), not merely 'stable'; `receipt-021`/`-026`/`-029` are asserted individually. L5: re-score after the fix and **raise the receipts high-water mark in the same change**, with the new number proven stable across repeated runs. **Vacuous traps**: asserting determinism by calling the function twice in one process (same seed, always agrees); asserting 'a value is returned' rather than which; fixing `modal` alone and leaving `?? modes.first` order-dependent |
| P1.13 | **The Confirm sheet's odometer renders ungrouped.** The prefill writes raw digits into the field (`119486`) and only groups on blur, while Home renders `123 600` from the same `OdometerFormat`. Same class of defect as the thin-space separator: the shared formatter is correct and one screen bypasses it. Group the pre-filled value the way `ServiceEntry` does (`form.odometer = lastKnown.map(OdometerFormat.grouped)`), keeping the type-in-raw / group-on-blur round trip intact | L1: a pre-filled odometer arrives grouped and survives a focus/blur round trip unchanged; L4: the Confirm sheet and Home render the same figure identically - the assertion that catches a screen bypassing the shared formatter |
| **[x]** P2.5 | Foreign currency: detection chip, conversion card, rate cache + seed pack. The Confirm sheet enters the foreign state when the extracted or chosen currency differs from the vehicle's home currency (never the device locale). The conversion card (ConfirmForeign.dc.html) shows the original amount, the converted home amount and the rate with its date; a rate miss saves rate-pending (F9), and a low-confidence currency asks instead of converting. `RateStore` (core) keys a local cache `(date, base, quote)`, ships a bundled seed pack (`Rates.seed.json`) so a first launch offline converts common pairs, looks up the **entry's** `rateDate` (never today), and backfills fill-blanks-only via an injected `RateFetcher` protocol (no HTTP yet) | L1: pending-rate save (exact original, nil homeAmount); `rateDate` = entry date; snapshot immutability (second backfill with a different rate leaves the snapshot untouched); determinism; seed-pack conversion with no network/cache; low-confidence never converts; Decimal (4.3287 / 289.50) round-trips exactly. L4: ConfirmForeign converted + pending snapshots (EN+RU); ordinary sheet unchanged when currency matches home |
| [~] P2.6 | Fiscal QR: payload parser + instant fill + background enrichment. **Blocked on a product decision before the enrichment half can start** (see `Spike/ReceiptSpike/fixtures/fiscal/README.md`): the QR carries only total, timestamp and fiscal ids, and the OFD's document URL is keyed on an opaque `RawId` that is **not** derivable from the QR - verified against a real receipt. So line items need either the official ФНС API (a per-user registered account), a paid third-party aggregator (sending fiscal ids to a third party), or no lookup at all (total + date exact, litres typed). The QR *parser* half is unblocked and has a real fixture | L1: FNS fixtures incl. malformed/truncated; enrichment fill-blanks-only; L4: F5 degraded state |
| [~] P2.7 | Pump-photo mode (feature-flagged). **The flag, the gate and the honest degradation landed and are L1/L4-verified: the flag defaults off and is gated on the build's measured pump accuracy (`PumpPhotoGate`, 0/30 today), a remote config document cannot enable it while the gate fails (the CONFIG.md "config can never disable a security control" reasoning applied to an accuracy gate), and the flag-off pump path opens the ordinary empty manual form with no error (hard rule 15). The `pump corpus ≥95%` L5 gate is NOT met and stays pending** – "mode ships off, gate enforced" is the committed state, and the gate test fails the build if the flag is ever on while the corpus is below 95%. | L1: the gate (flag on + below 95% => violation, off => pass, at/above => pass) with constructed input; remote-cannot-enable; bundled default off; off degrades to a nil prefill. L5: the real corpus number (0/30) is asserted against the gate constant – the ≥95% flip itself is NOT ticked. L4: flag-off pump capture opens the empty manual form and saves, no error text |
| **[cut]** P2.8 | ~~Foundation Models normalization layer (iOS 26+ gated)~~ **Cut 2026-08-25, not deferred.** The on-device model reports itself `unavailable` on an unsupported device language - the shipped iOS 26.5 SDK carries `SystemLanguageModel.UnavailableReason.unsupportedLanguageOrLocale` next to `supportedLanguages` - and **Russian is not in the supported set** (nor Kazakh). The corpus is 32 RU/KZ receipts and receipts score 41.6%, so the layer would have been unavailable exactly where the rules parser is weakest and available only where it already does best. The task's own check (*ships only if it strictly improves on an A/B against the corpus*) is therefore unsatisfiable in principle, not merely unmeasured - a stronger reason to stop than P2.7's 0/30. **Not blocked on the EU**, which has had Apple Intelligence since iOS 18.4 (April 2025); the constraint is language, not jurisdiction. Reasoning and the re-open condition (Apple adding Russian to `supportedLanguages`) live in `docs/VISION.md` -> "Why tier 2 was cut". Normalization for Russian receipts is now tier 3's job (the cloud LLM gateway) | **No code shipped, by decision.** The measurement that would have gated it cannot be taken |

## P3 · Service, expenses, reminders

| ID | Task | Checks |
|---|---|---|
| **[x]** P3.1a | ServiceEntry typed screen: mode row, vendor + total header, line items (title + category + cost), lump-sum fallback, date + odometer card with the km-lifetime odometer rule, note, save. **P3.1b carries the scan half** (multi-page invoice capture, attachments, OCR line split) which lands in this same screen as a pre-fill. | L1: odometer rule both directions (blank saves with no km lifetime; refuses and names the next step with one); lump sum round-trips byte-identical and appears in the Log; `.other(String)` free text survives persistence; money exact (148.00 = 89.00 + 59.00, item sum = header total). L4: sections render, add/delete a line item updates the total, discard guard fires on a dirty form. Screenshots EN+RU incl. the lump-sum variant. |
| **[x]** P3.1b | ServiceEntry scan half: multi-page invoice capture, attachments, deterministic OCR line split (drops into the typed P3.1a screen as a pre-fill). | L4: snapshots incl. fallback; L5: parser accuracy on the invoice corpus |
| **[x]** P3.2 | Expenses + parts shelf + install linking. Expense entry (category, title, money, date) as a peer of the service path from the Service & expenses surface's mode row; the parts shelf lists `.parts` expenses with `installedInServiceId == nil` under a visible "on shelf" badge; the ServiceEntry Link row offers on-shelf parts (suggestion-matched to the service's line-item categories) and linking writes both halves (`installedInServiceId` + `usedParts`) in one transaction. | L1: **double-count property test** - cost/km identical with/without the link over five compositions, asserting the EXPECTED rate (each amount once), through link/unlink/relink; both sides of the link written then cleared (through the repository); a linked part counts once at purchase (12.40 stays 12.40, never re-priced into the service); shelf membership + a deleted service returns its parts to the shelf; suggestion ordering puts matching parts first. L4: shelf renders its "on shelf" state + empty state; the Link row offers a matching part and the service shows it linked; an empty shelf leaves the ordinary service sheet unchanged; Parts mode opens the Expense entry. Screenshots EN+RU (shelf, service-link). |
| **[x]** P3.3 | Tire sets + derived mileage | L1: span math across mount/unmount records (closed spans sum, swapping back adds, not restarts); the open span runs to the latest known odometer and grows when a later fill raises it; "–" when unknowable in both directions (a span missing a bounding odometer is excluded and yields nil, a set with a usable span still returns a number); never-mounted renders "–", never "0 km"; derived-never-stored (recomputing after an odometer edit yields the new number with no invalidation). L4: list renders mileage and "–"; mounting from the Tires mode saves and requires the odometer. Screenshots EN+RU (list, mount) |
| **[x]** P3.4 | Reminders screen + reminder form + status logic | L1: full state-machine transition table (schedule/attention/done/dismissed × complete/reschedule/delete); **completion anchors at COMPLETION not the due date** (late-completion proof); `.done(nil)` declines still recur; reschedule resets a fired `.attention`; `.dismissed` ≠ delete (row + reason survive; delete tombstones); whichever-comes-first both directions; neither-due-field refuses to save and names the next step; derived-at-read `.attention` stored, terminal rows never re-fire. L4: both groups render from a seed, empty state, form saves and returns to the list, complete leaves the attention group. Screenshots EN+RU (list, empty, form) |
| **[x]** P3.5 | ReminderComplete sheet → pre-filled entry → next cycle | L3-style integration through a real in-memory repository: complete→pre-filled entry saved→`.done(entryId)` carrying **that** entry's id→next occurrence as a NEW row anchored at COMPLETION (late-completion proof, literals) and the old row survives as history; decline (Skip) yields `.done(nil)` with **no entry written** and still recurs; non-recurring produces no next occurrence; pre-fill is editable (defaults, never facts); the category→entry mapping covers every `ReminderCategory`. L4: sheet renders from a seed, Skip returns to the list (non-recurring row gone), Type amount lands in the entry with title + odometer pre-filled and editable. Screenshots EN+RU (sheet with next-cycle line, pre-filled entry) |
| **[x]** P3.6 | Local notifications: arming at write time, humane scheduling, cancellation on status change | L1 over a pure `ReminderNotificationPlan` (never `UNUserNotificationCenter`): a save crossing `dueOdometer` arms an odometer notification and a non-crossing save does not; every fire time is 09:00 or 10:00 local asserted as `DateComponents.hour` (incl. a 23:30 due date); the overdue follow-up is exactly one at due+7/+14/+21 (the never-a-nag-loop rule tested as a loop); complete/reschedule/dismiss/delete each cancel the reminder's identifiers; a fired `.attention` reschedules and re-arms; permission requested at first reminder creation not launch, denied card once. The `UNUserNotificationCenter` adapter is a thin injected `LocalNotificationScheduling` conformer; the odometer arming is wired into the fill-up save path. L4: denied card renders (`-notificationStatus denied`), a reminder created while denied still saves and shows. Screenshots EN+RU (denied card) |
| **[x]** P3.7 | **The tab bar double-counted the home indicator.** `AppTabBar.bottomPadding` is `28`, taken from `HomeA.dc.html`'s `padding: 10px 12px 28px` - but the artboard is a browser page with **no safe area**, so that 28 px *is* the designer's home-indicator allowance. The app then draws the bar through `safeAreaInset(edge: .bottom)`, which adds the real inset (~34 pt on iPhone 17) underneath it, leaving **~62 pt of dead space below the labels against the artboard's 28**. Spend the artboard's allowance **through** the safe area instead: `max(8, 28 - safeAreaInsets.bottom)` - ~42 pt on an indicator device, exactly 28 on an SE, and the 8 pt floor stops the labels touching the indicator on tall devices (the opposite mistake). `contentHeight` is derived from this padding, so `TabRoots.toastBottomClearance` and `contentBottomClearance` (used by `HomeView` and `TrendsView` to clear the raised capture circle) move with it. **General lesson worth carrying: an artboard's bottom padding is never a safe-area inset - porting it verbatim double-counts every time.** | L4: the existing "last row clears the tab bar" assertion still holds; a **device without a home indicator renders the artboard's exact 28 pt** and one with an indicator renders the floor, asserted against injected insets rather than a runtime measurement; screenshots in **both themes** (the defect was reported on the light shell) and EN+RU |
| **[x]** P3.8 | One reset per launch + EN captures that are actually EN | **L2-level defect fixes.** (1) `-homeResetDatabase` reset the database once per *seed*, not once per launch: five types called `AppStore.resetForTests()` and only three guarded it with a per-type private flag, so a launch carrying several `-seed*` arguments reset repeatedly and wiped the earlier seeds' data (P1.4-home rendered empty). One gate: `AppStore.resetForTestsOncePerLaunch()` performs the wipe at most once per process; all five call sites (Home, RecentlyDeleted, TankLevel, TireSet, Reminder seeds) use it; the three per-type flags are gone. (2) The capture script's `en` branch passed no language arguments, so a preceding `ru` launch's `-AppleLanguages "(ru)"` persisted and `en` captures rendered Russian (P1.4-home, P3.2-parts-shelf). The `en` branch now passes `-AppleLanguages "(en)" -AppleLocale en_US` explicitly. Re-captured; the two known-bad files verified by size and re-run. | Seeds intact (no seed weakened or deleted, idempotence preserved); `swift test` 0 at 529+; `xcodebuild test` 0 at 108; `swiftlint` 0; EN captures no longer follow RU into Russian |

## P4 · Account, sync, blobs

| ID | Task | Checks |
|---|---|---|
| **[x]** P4.1 | Auth endpoints: session exchange, refresh rotation | L2: test-signer tokens, garbage/expired, concurrent-first-sign-in creates exactly one account, **reuse-revokes-chain** |
| **[x]** P4.2 | Sync push/pull + SCN allocation | L2: ordering, pagination under concurrent writes (no skip), idempotent replay, per-item conflict, 410 per device, 426 blocks push not pull, SCN monotonic under concurrency, no payload in logs |
| **[x]** P4.3 | Blob endpoints + S3 storage + quotas + orphan sweep | L2: dedupe (per account, not global), 25/10 MB per-type caps, content-type allow-list, 429 quota, commit-verifies (no upload / size mismatch → no row), **cross-account 404 with no presigned URL minted**, upload vs download presign lifetimes differ, orphan sweep (unreferenced past grace deleted; live and recently-tombstoned survive), account-deletion prefix purge, no presigned URL or secret in any log line, migration 006 applies and rolls back |
| **[x]** P4.4 | iOS: Sign in + Restoring screens, Keychain session, J11a detection | L4: wrong-provider empty-account flow; provider-notice snapshot; sign-out escape; L1: token storage |
| **[x]** P4.5 | iOS sync client: dirty queue, pull/merge/push loop, domain revalidation after merge, **field-level merge for `Vehicle`** (every other entity stays record-level LWW) | **L3: the S1–S9 simulator suite, one deterministic test per scenario.** S9 specifically: a stale device writing one `Vehicle` field must not revert another field edited more recently elsewhere – the hard-rule-13 case, and the one whose failure is invisible |
| **[x]** P4.6 | Attachment sync: rendition generation, inline thumbnails, lazy/eager download | L1: ≤2048px/size caps, sha verify-on-download; L4: "photo syncing" shimmer state |
| **[x]** P4.7 | Restore end-to-end | L3: pull-from-zero hash-equals origin (a count-only assertion passes a silently dropped field - the hash moves, asserted); interrupted restore resumes at a page boundary AND mid-page, hash-equal, no loss no duplication; restore never blocks on photos (blob transport down -> every text record lands, zero blob fetches during pull); backend down -> `.unreachable` (F7 copy + import path reachable, L4); empty pull -> `.empty` recovery entry point shown before add-a-car is usable (L4 ordering); verification stats are literal numbers from a seeded dataset. L4: Restoring renders progress/stats, server-down and empty states. Screenshots EN + RU |
| **[x]** P4.8 | Silent APNs nudges + server throttle | L2: pusher excluded (sibling only); a ten-push burst collapses to one nudge per sibling; throttle reopens past the window (injected TimeProvider, no sleep); a dead token (`BadDeviceToken`/`Unregistered`/410) clears the row and the device still pulls; a transient failure does **not** clear the token; no token -> no nudge and the push is unaffected; APNs down -> push still returns its normal result (nudge out of band); the payload carries `content-available` and no `alert`/`sound`/`badge`; no push token in any log line; migration 008 applies and rolls back |
| **[x]** P4.9a | Account lifecycle (server): device management (`GET /account/devices`, `PUT …/push-token`, `DELETE …/devices/{id}`), `DELETE /account` tombstone, grace purge job + orphan-sweep reachability | L2: deletion is a tombstone (records survive, `deleted_at` set); every device 410 after account deletion; per-device revocation leaves siblings working and deletes no records; purge fires only past grace (the surviving-within-window case asserted); cross-account device access 404 with the target row untouched; push-token round-trip + clearing; no token/push-token/email in any log; migration 007 applies and rolls back |
| **[x]** P4.9b | Settings UI account states (guest/synced/pending/revoked/quota) **plus the sync surface** (`docs/SYNC.md` -> The Settings sync surface, `docs/ERRORS.md` -> Settings): the relative last-synced status, a **"Sync now"** manual trigger, and the **count-and-link** to flagged entries. The status is reassurance and never turns amber with age; "Sync now" is idempotent, is not an error offline, and may never be the only path to a synced state (hard rule 1); Settings shows a conflict **count** and navigates to the Log filtered to flagged entries, and **resolves nothing itself** (hard rule 8) | L4: all Settings states (guest/synced/pending/revoked/quota); the flagged-count row navigates to a filtered Log and Settings contains no resolution control. L1: the count is **derived** from records carrying a `ConflictState`, not stored - assert it changes when a conflict is resolved elsewhere, with no Settings write; "Sync now" while a sync is in flight issues **no second push** (assert the transport call count, not the UI state); offline it produces the waiting copy and **no error**. **Vacuous traps**: asserting the button exists without asserting the no-second-push behaviour, and testing the count with zero conflicts only |
| **[x]** P4.10 | **LLM gateway server: `POST /extract`.** The endpoint is specified in `API.md` and its quota table (`llm_usage`) was migrated in P0.9, but **nothing builds the server that calls the model** - P6.3 is the client and assumes this exists. After the P2.8 cut this is the **only** model-assisted extraction path in the product, and the only one available to Russian receipts. Server-side provider key (hard rule 11 - an IPA is a zip), per-user quota from `llm_usage` returning the documented `402`/`429`, the 4 MB envelope cap, images processed **transiently and never written to disk or blob storage**, and a response that is exactly `ExtractionMeta` fields with per-field confidence. The server returns **field values, never domain judgements** - it does not decide mixedness, duplicates, or whether a fill is plausible (hard rule 9). **Device-side budget, normative and owned by `API.md` -> "The device's side of `/extract`":** the image is **downscaled and JPEG-compressed on device before upload**, and the device gives the round trip **3 seconds per attempt** before it stops making the user wait. On expiry the user proceeds with the locally recognized data and a message that names that next step (hard rule 7); a late answer may **only fill fields that are still blank and untouched**, marked as a suggestion (hard rule 13), and **never** changes anything after save (F4). | L2: `402` with quota exhausted and `429` per-period, both with the client falling back rather than erroring; oversize body rejected at the envelope, not the model; a request whose image is never persisted - assert the temp path and the blob bucket are both untouched after a successful call; **hard rule 12: no log line carries the image, the extracted values, or the prompt**. L1 (client): the 3 s budget fires deterministically against an injected slow transport and the sheet stays usable throughout; a late response fills a blank field and **cannot** overwrite a touched one; nothing arrives after save. **L5: compression must not cost accuracy** - re-score the receipt corpus through the compression step with the existing `AccuracyRatchetTests` scorer; hits may not fall below the recorded high-water mark, which is what stops "compress harder" from silently becoming "read worse" |
| **[x]** P4.11 | **`Date` does not round-trip exactly through persistence.** `Records.swift` stores every date as `timeIntervalSince1970`; adding the 978307200 s epoch offset shifts the fractional bits out of the Double mantissa, so a record read straight back after a write differs from what was written by **one ulp (~1.2e-7 s)** on `createdAt` / `updatedAt`. Found by P3.1a's round-trip test - the first test in the repo to assert **whole-record** equality rather than field-by-field, which is why it went unnoticed through P0.4. Measured severity: the drift is **idempotent** (a second round trip is stable), so sync's `updatedAt` LWW ordering is not at risk today - but the invariant "a record read back equals the record written" is one that sync, backup round-trips and the S1-S8 scenarios all lean on, and it is currently false. Fix is to store `timeIntervalSinceReferenceDate` (an exact Double round trip), which is a **column format change and needs a migration** - hence a task, not a patch | L1: a record of every entity type read straight back after a write equals what was written, whole-record and with no normalization; the existing sub-millisecond tolerance in `ServiceEntryTests` is **deleted** rather than loosened; migration applies forward over a seeded database and the migrated values are equal to the originals to the second |
| **[x]** P4.12 | **Corpus A/B against the cloud vision model, before P4.10 is designed.** Run 2026-08-26, full sweep (61 images, 0 errors), scored LLM-vs-rules with the one shared `CorpusScorer` and tolerance. **Result per class (rules -> LLM): receipts 46/96 -> 84/96, pump 1/46 -> 31/46, fiscal 1/3 -> 2/3, screenshots 7/24 -> 22/24.** Median latency 6.5-8.3 s, all above the 3 s device budget. The two silent failure modes are committed and asserted as named cases: `receipt-035` is the volume/price swap (70.44 read as litres) and `pump-009` is the decimal-separator shift (40.00 read as 400.0) - **both pass the arithmetic cross-check**. The probe's shift on `pump-005` did not reproduce: that fixture read exactly, three runs in a row, and `pump-009` flipped between correct and shifted across runs - the model is stochastic. **Recommendation (docs/EXTRACTION.md): the gateway cross-checks and suggests, never trusts** - a confident swap is worse than the parser's nil (hard rule 13), and the cross-check is scale- and swap-blind | L5: every class scored LLM-vs-rules with the same scorer and tolerance (offline, against committed `vision-ab/*.json`, no model call in CI); latency per image; the swap and decimal-shift cases asserted explicitly as known failure modes; a written recommendation in `docs/EXTRACTION.md`. `high-water.json` unchanged |
| **[x]** P4.13 | **PaddleOCR as a third extraction arm: embed it in Docker, measure it against the other two.** P4.12 measured the cloud VLM (receipts 84/96, pump 31/46, screenshots 22/24) but found its failures **silent** (swaps and scale shifts that pass the arithmetic cross-check), its output **stochastic**, and **every class median latency above the normative 3 s device budget** (pump peaks at 40 s). PaddleOCR is worth measuring because it attacks three of those four at once: self-hosted, so **no per-call cost and no third-party image sharing**, and plausibly far faster. **Russian is supported** (`cyrillic_PP-OCRv5_mobile_rec`; PaddleOCR-VL covers 109 languages) - the gap that killed the Foundation Models tier. Runs as a **container** (plain `docker run`, never compose) with ASP.NET reaching it behind an injected seam, exactly like `IBlobStorage` / `IApnsClient` / `IRateFeed`. **Two arms, because together they answer what neither answers alone**: **(A)** PP-OCRv5 Cyrillic recognition -> the EXISTING `FuelExtractor`, isolating recognition - if this scores about the same as Vision+parser it **confirms** `docs/EXTRACTION.md`'s central claim that interpretation is the bottleneck; if it scores far better, **that claim is wrong and the doc must be corrected in the same change**; **(B)** PaddleOCR-VL -> fields directly, competing with the DeepSeek arm on accuracy, latency, cost and determinism. Gateway-only: hard rule 1 means this can never be the primary path - on-device Vision stays tier 0. **Measured 2026-08-26:** Arm A (server det + `cyrillic_PP-OCRv5_mobile_rec`) -> `FuelExtractor` scored receipts **29/96** (rules 46/96, DeepSeek 84/96), pump **2/46** (1/46, 31/46), fiscal **1/3** (1/3, 2/3), screenshots **7/24** (7/24, 22/24) - **not** far better, so the interpretation-not-recognition split is **not falsified**, but the real finding is that the parser is **coupled to Vision's output**: PaddleOCR's detector merges `1,869 EUR/L` into one line (Vision splits it), and the Cyrillic-only reader misreads `ИТОГ` as `НТОГ`. PaddleOCR's OCR is **byte-identical across 3 runs**, but the sweep exposed a **pre-existing parser** non-determinism (`FuelExtractor.modal` ties two total candidates and breaks the tie via `Dictionary(grouping:)` iteration order) that flips `receipt-021`/`-026`/`-029` between runs (receipt score 29-30/96). Median latency 4.5-8.4 s, max 21.0 s - **above the 3 s budget in every class** even self-hosted. **Arm B (PaddleOCR-VL-0.9B) is blocked**: `paddlex` calls `is_bfloat16_supported()` with no place argument (raises on paddlepaddle 3.2.2 aarch64); forcing float32 OOM-kills the 7.6 GB Docker VM (exit 137). **Conclusion (docs/EXTRACTION.md): the ops cost is not justified** - Arm A is worse than on-device Vision, self-hosting buys no latency back without a GPU, and Arm B cannot run on modest hardware; PaddleOCR does not earn the fallback slot | L5: both arms scored with the **existing `CorpusScorer` at the same tolerance** (P4.12) - a second, subtly different comparison invalidates everything; per-class hits/total beside the rules and DeepSeek arms; **latency median and max per class against the 3 s budget**; **determinism measured, not assumed** - every image run 3x with variance reported, because the DeepSeek arm flipped `pump-009` between correct and shifted on re-run; scoring runs **offline against committed result files** so CI never needs the container; `high-water.json` unchanged. **Vacuous traps**: reporting only aggregate percentages (two silent failures matter more than a higher average); running each image once and calling it deterministic; scoring arm A against a different tolerance than the rules arm it exists to be compared with |

## P5 · Currency, localization, importers

| ID | Task | Checks |
|---|---|---|
| **[x]** P5.1 | Rates service + daily fetch job (ECB + CIS) + endpoints | L2: weekend/holiday carry-forward fixtures; append-only; immutable caching headers |
| **[x]** P5.2 | Money end-to-end in app incl. manual rate + S8 backfill | L1: full J10/S8 suite; the manual-rate pair (rateDate = entry date, `.manual`), comma/dot parse equality, refused rates write nothing, RU plural footnote forms (1/2/5); L4: rate-pending → filled transition is silent (no toast/alert/banner), the pending card's manual-rate next step is reachable and flips the card in place, the footnote appears at N>0 and vanishes at 0, and Edit can change a rate already set (hard rule 13 "again afterwards") |
| **[x]** P5.3 | RU localization pass | **Case governance:** every `%@` that receives user/server text audited (`docs/LOCALIZATION.md`, the single authority); four sentence shapes fixed - the archived-returned banner, the install-part offer, the wrong-provider question, the removed-on-device attribution - each pinned by a regression test asserting no governing preposition sits before the slot. **Plurals:** RU forms asserted on the RENDERED strings at 1/2/5/11/21 (11→many, 21→one); `Synced N hours/days ago` regained the verb (`Синхронизировано N часов назад`). **The `литр` row is corrected, not satisfied:** the app never renders a count with the spelled-out unit (volume is `42,3 л`; `литр` appears only in count-free labels), so inventing that plural would satisfy a checklist no screen displays. **`Text(_: String)` blind spot:** the gate now flags literals inside `String`-typed `Text` arguments (coalescing, mixed ternaries, concatenation) - it found and fixed the Provider/Vendor placeholders on Edit entry; interpolated literals are empirically NOT the trap (`@_disfavoredOverload`). **M: native review + no clipped DIN numerals on device - the sole outstanding clause** (a human reading rendered RU on a device; no test or screenshot can substitute) |
| **[x]** P5.4 | **Importer: My Fuel Manager ONLY - parsed SERVER-side** (`POST /import/parse`, hard rule 9's named exception, amended 2026-08-27). The CSV is uploaded, parsed and mapped on the server, which returns **candidates the device reviews and commits** - the server writes nothing to the user's garage. Stored in blob storage and **purged after 30 days** (`docs/SECURITY.md`). Works signed out. (narrowed 2026-08-27 by the product owner; the other five are deferred to P5.4b). **UNBLOCKED 2026-08-27**: the real export is committed at `Spike/ImportFixtures/mfm/` (plates scrubbed, `trips.csv` dropped - no `Trip` entity - everything else intact). Read its README before writing a line: the header is on **line 2**, the delimiter is **`;`**, dates are **`M/D/YYYY`** and ambiguous, there is **no unit-price column**, and `Fuel` is a numeric code. It also preserves a **real odometer defect** (a Volvo row reading `9`) that makes a naive span read 3.4 L/100km - do not clean it. **Per-car (2026-08-27)**: an MFM export imports as **one car** - a new one, or merged into a car the user picks and is told about before it happens (hard rule 13); merging into a car that already has entries runs the S2 duplicate heuristic and flags rather than drops (hard rule 8). Mappings already recorded: `Runned` → derived check only; `Tank status after fillup` → `tankLevelAfterPct`; `Tank filled/Free` → capacity inference; `Fuel quality` → `fuelGrade`. F6 rules apply: ambiguity (units/currency) asks **once per file**, unparseable rows import **partially** with a review list, `provenance = .import(source)` on every row, conflicts flagged never dropped | L1: the committed real export reproduces **8.222 L/100km** through the consumption engine - a known-value assertion, not a round-trip tautology; units and currency resolved once per file; a deliberately corrupted row imports the rest and lands on the review list rather than failing the file; every imported row carries `provenance = .import`. **Vacuous traps**: asserting a round-trip through our own writer (that tests our writer, not their format); a synthetic CSV the importer's author invented, which cannot disagree with the parser |
| P5.4b | **The other five importers** (Fuelio, Drivvo, Fuelly/aCar, Spritmonitor, CarScope). **Deferred 2026-08-27** - postponed until the MFM path is proven end to end. Each needs its own real export as a fixture for the same reason P5.4 does. Notable mappings are already in `docs/SCHEMA.md` → Import mapping (Drivvo income → skip in v1 with a warning; Spritmonitor bi-fuel rows → separate `FillUp`s by `fuelKind`; Fuelly/aCar service logs → `ServiceRecord` with a single item) | Per format: L1 known-value assertion against a real export, plus the F6 rules above |
| **[x]** P5.5a | **The versioned backup format: writer, reader, round-trip** (core, no UI). **Per-car scope** (product owner, 2026-08-27): the export button produces **one car's** archive - its `Vehicle`, that car's entries of every type, its reminders and tariffs, the stations they reference, the attachments and **all the tombstones** - and `manifest.json` **declares `scope`**. Whole-account archives remain for backend snapshots and the F7 restore path. `docs/SCHEMA.md` → Backup format is the authority and it is fully specified, so this is unblocked. The same shapes serve user-held export, backend snapshots and the F7 restore path - it is **not a separate pipeline**. Additive evolution only (new optional fields); a `schemaVersion` bump needs a migrator. Optional passphrase (AES) at export time - **never required**, because a user-held key that is mandatory is a way to lose your own data | L1: round-trip **including attachments and tombstones** - a tombstone that does not survive is a deletion that comes back (hard rule 8). L1 **scope**: a `scope: "vehicle"` archive round-trips to exactly one car and **carries no other car's data** - assert the other car is absent, not merely that the count is 1; and a reader **refuses to treat a vehicle archive as an account restore**. L1: `scope` is read from the manifest and **never inferred from `vehicleCount == 1`** - a one-car user's account export and a one-car export are identical by count, which is the mutation to try. L1: a v1→v2 migrator fixture. L1: the passphrase path round-trips **and** an export without one still opens. L1: a truncated or corrupt archive is rejected **whole**, never partially applied. **Vacuous trap**: round-tripping only entities that happen to have no optional fields |
| **[~]** P5.5b | **Export/import UI (SHIPPED 2026-08-27 with three visual defects filed as P6.15, and the units/currency row not yet an editable picker - F6a's "everything shown is adjustable here" is half-met)** (share sheet, file picker, the F6 partial-import review list). **Asks which app the file came from** and shows the supported sources from **`GET /import/formats`** - server-driven, because a format the picker cannot list is a format that does not exist, which would throw away half the reason parsing moved server-side. The app never sniffs the format (`docs/ERRORS.md` -> Import wizard). **The preview gate** (`JOURNEYS.md` F6a, product owner 2026-08-27): parsed candidates are shown as **figures the user can check from memory** - fill-up count, date range, odometer span, currency, units, total spend, and the **derived consumption** - with the target car named and the S2 duplicate count when merging. Everything shown is adjustable there; **nothing is written until the user confirms**, and cancelling deletes the stored file. Narrowed 2026-08-27: **one working export/import path is the goal**, not a format zoo. Drives P5.5a's archive and, once unblocked, P5.4's importer | L4: export produces a file the user can hand off and re-import on a clean install; the review list names its next step per row (hard rule 7); EN+RU, dark, no clipping at Dynamic Type XL (see P6.13). L4: the source picker renders the **server's** list, and an unknown-source path offers to send the file rather than dead-ending. L4: declaring the wrong source shows the specific message, never a generic failure (F7). L1: with no network the import entry point says **why** and the rest of the app is untouched (hard rule 1's exception is the parse only). L4: the preview renders the figures above and **nothing is written before confirm** - assert the repository is untouched while the preview is on screen. L1: the preview's consumption comes from **the same engine** that computes it after commit, so approving it cannot approve a different number - compute both and assert equality. L1: cancelling deletes the stored parse. **Vacuous traps**: a hardcoded format list in the picker - it passes every test and silently defeats the architecture; and a preview test that asserts the labels rendered without asserting the **values**, which is how a display-only figure ships |
| **[x]** P5.6 | **Vehicle catalog: server curation + `GET /catalog`** (`SYNC.md` → Reference data). Curation path into `vehicle_catalog` (seeded P0.9), monotonic `packVersion` bumped on curation, delta-since-version and full-pack responses, ETag/304, published pack validated against its schema before it can serve | L2: delta returns only entries changed since the requested version; full pack served when the delta would be larger or the client is too far behind; `304` on matching `If-None-Match`; a pack failing its schema is refused **at publish time**, never served; publishing a `packVersion` not greater than the current one is refused |
| **[x]** P5.7 | **Vehicle catalog: client updater** (`SYNC.md` → Reference data, `ERRORS.md` → Vehicle catalog updates). Three-layer resolution (bundled seed < cached pack < server pack), atomic cache write at `Application Support/Tankbook/catalog.cache.json` excluded from backups, background/throttled fetch that is never launch-blocking | L1: with no cache and no network the bundled seed pack still answers Add-car suggestions. L1 **master rule**: an overlapping server entry replaces the cached one. L1 **and its limit**: a corrected pack does **not** rewrite a `Vehicle` already saved, including a value the user typed over. L1: malformed pack rejected **whole**, previous pack still serves; `packVersion` older than held is ignored; truncated cache falls back to the seed pack. L1: every failure is silent – no user-facing error (ERRORS.md). L1: a catalog miss is counted **without** the typed text (hard rule 12) |

## P6 · Polish & ship

| ID | Task | Checks |
|---|---|---|
| **[x]** P6.1 | Anomaly engine + insight cards + dismiss-with-reason. **ENGINE DONE 2026-08-27 (P6.1a)**: seasonally-fair detection against a 365-day-lagged baseline, conservative thresholds, dismissal-by-cause, silence on insufficient history. **The insight CARD, its evidence chart and the dismiss-with-reason UI are P6.1b** and were not built - ios/App/ was held by another agent | L1: seasonality fixtures (winter never false-fires); dismissal suppression; L4: card snapshot |
| P6.2 | Monthly summary (opt-in) | L1: schedule + content correctness; L4: notification copy snapshot |
| **[x]** P6.3 | **LLM gateway client** (device side of P4.10). **UNBLOCKED 2026-08-28** - the "3 s budget needs a product decision" note was wrong, and had been since 2026-08-25: `docs/API.md` → "The device's side of `/extract`" already resolves it in the doc itself. **The budget is about the user's next step, not about aborting the work** - at 3 s the **UI** moves on and the request may finish in the background, precisely because a hard abort would cancel almost every request on a mobile link (a 250 KB rendition alone takes ~2 s at 1 Mbit/s). Measurement (median 6.5-8.3 s, max 40 s) is therefore not a contradiction; it is the reason the rule is written that way. Build: the **downscale-and-compress** step before upload (long edge 1600 px, JPEG), the **3 s UI budget**, the proceed-with-local-data message naming its next step, and the **fill-blanks-only** handling of a late answer. Server responses (`402`/`429`/`426`) are **P6.11, already shipped** - consume `SyncServerError`, do not re-classify. **Pro tier is DEFERRED, not cancelled** (product owner, 2026-08-28): build no paywall and no quota UX here, and leave the existing Pro rows alone | L4: F4 degraded states incl. the timeout message and its next step (hard rule 7); **UI assertion: no paywall reachable from any capture flow**. L1: the 3 s budget fires against an injected slow transport and **the request is NOT cancelled** - assert it completes afterwards; the sheet stays usable throughout; a late response fills a blank untouched field and **cannot** overwrite a touched one; nothing arrives after save (F4). L5: **compression must not cost accuracy** - re-score the receipt corpus through the compression step with the existing scorer; hits may not fall below the recorded mark |
| P6.4 | **Remaining screens: Garage tab root and Account & devices.** **REWRITTEN 2026-08-27** - the row listed four. **Import wizard is drawn** (`ImportSource`/`ImportPreview`/`ImportReview.dc.html`, built in P5.5b), and **Paywall is cancelled** with the Pro tier. What is left: **Garage tab root** (the vehicle grid; Car switcher covers quick switching today but the tab needs a real screen) and **Account & devices** (Settings' signed-in target: device list, revoke, delete account - the server side shipped in P4.9a/P4.9b). **Vehicle detail** left this list in P1.12 | L4: snapshots dark+light, EN+RU, **no clipping at Dynamic Type XL** (see P6.13); `docs/SCREENMAP.md` updated and the no-dead-ends walk extended to both. L1: revoke and delete-account surface what the server already enforces - a tombstoned account answers `410` and the local log survives (P4.9a), so the screen must never imply local data is gone |
| **[~]** P6.7 | **CODE LANDED 2026-08-28 with W8; NOT ticked - the EN+RU screenshots its own L4 check requires were never captured, because the agent was killed by a provider usage limit before that step (see HANDOVER, sixth failure mode). No test asserts colour, so until those are taken and read, this change is unverified where it is most visible.** **Cyan means electric: the `action` token.** `docs/DESIGN.md` says `headlight` encodes *electric* and that a fuel card never shows cyan, but ~66 of its uses across ~25 files are buttons, links and inert icons - only two are electric (the `powertrain == .ev` ternaries). The root cause is that the palette has **no interactive colour**: both accents are reserved by DESIGN.md's own rules, so there was nothing else to reach for. Adds `action` (dark `#8FB4D9`, light `#2F6690`, AA in both themes), re-points every non-electric use to `action` or `inkSoft`, and writes the rule down. Decided by the product owner 2026-08-27 | L1: a source-scanning **escape guard** - occurrences of `Palette.headlight` in `ios/App/Sources` must equal an explicit allowlist of *electric* uses (an allowlist of all ~66 is the vacuous version and guards nothing); the token resolves to the exact hexes in both themes. L4: screenshots EN+RU dark for Settings, Home, Trends and Sign in, compared against the previous set - this changes how most of the app looks, and no test asserts colour |
| **[~]** P6.8 | **Respect Low Power Mode (CORE DONE 2026-08-27; the app-side wiring is the follow-up - `ios/App/` was frozen by a parallel agent, so nothing constructs `ProcessInfoPowerState`/`LowPowerResumer` or passes `.background` at the launch/foreground/timer triggers yet, and the Settings status-row copy is unwritten)**: defer background sync and heavy jobs** (`docs/SYNC.md` -> Low Power Mode). While iOS Low Power Mode is on, the app postpones **opportunistic** work - launch/foreground/timer sync cycles, attachment upload and prefetch, the rate pack refresh, the catalog pack fetch - and changes nothing else. **The load-bearing distinction is background vs user-initiated**: a save, and any sync/restore/export/retry the user actually tapped, must run exactly as before, because a silently cancelled action the user is watching has no next step (hard rule 7) and reads as a hang. Resumes on `NSProcessInfoPowerStateDidChange`, not at next launch. Surfaces **only** as a reason on S7's existing passive status row ("Waiting to sync - 5 changes - Low Power Mode is on") - reassurance, never a warning: no amber, no badge, no toast, no modal (hard rule 8). The power state is an **injected value in core**, never `ProcessInfo` read inline, or the policy cannot be tested (`TabBarMetrics`/`PumpPhotoGate` precedent) | L1: with the state on, an opportunistic cycle defers and the dirty queue is **unchanged, nothing dropped** (hard rule 8); with it off, the same cycle runs. L1 **the distinction**: a **user-initiated** sync/restore runs while the mode is on - this is the test the naive implementation fails. L1: resume fires on the power-state change, not only at launch; the deferred queue then drains. L1: a save, an edit and a delete are unaffected while the mode is on (hard rule 1 - no screen is ever sync-gated). L1: blob upload/prefetch defers **without** losing the pending record. L4: the status row shows the reason in EN+RU and is **not** amber; it vanishes when the mode ends. **Vacuous traps**: a gate consulted but not awaited; asserting only that the mode was read; testing the defer path without the run path (an implementation that always defers passes); a test that reads the real `ProcessInfo` and therefore only passes on the machine that ran it |
| P6.9 | **The ConfirmManual screenshot blind spot: the conversion card never reaches the frame.** `-seedConfirmForeignPending` dates its fill 2020-01-01 so the rate falls outside the seed pack - which also trips the odometer-timeline warning, and that block pushes the currency conversion card **below the fold**. `simctl` cannot scroll, so every `capture-screenshots.sh` shot of that sheet records a screen with the feature off-frame; P5.2b's four confirm captures were taken, opened, found useless and deleted rather than committed (a capture that misses its subject is evidence for the wrong code). Affects `P2.5-confirm-foreign*` equally. Fix by giving the sheet a scrolled launch state, or by capturing sheet screens from a UI test (which can scroll) as `DebugLaunch` already does for presenting them - **do not simply re-date the seed**, because the 2020 date is what makes the state rate-pending in the first place | L4: a committed capture of the ConfirmManual sheet shows the conversion card in both its `.ratePending` and manual-`.converted` states, EN+RU, with the manual-rate next step visible and not clipped by the save bar; the existing `P2.5-confirm-foreign*` shots re-taken the same way. **Vacuous trap**: asserting the capture script exited 0 - it always does; the file is written whatever is on screen |
| **[x]** P6.10 | **Alpha-testing notice on the capture screens** (product owner, 2026-08-27). Capture is honest about what it can do: the corpus measures receipts **48/106** and pump **2/61** fields resolved, and `docs/EXTRACTION.md` records that Vision misreads a digit at confidence 1.00. The screens should say so - the feature is in alpha, recognition quality is not guaranteed, it is actively being improved, and **the user's captures and cases are what improve it**, so keep sending them and expect mistakes meanwhile. **Non-negotiables**: (a) EN **and** RU through the String Catalog (hard rule 10) - and RU runs 20-30% longer, which is what overflows a two-line notice; (b) **never amber** - amber is attention/warning only (`docs/DESIGN.md`), and "this is alpha" is a disclosure, not a fault; (c) it must not frame capture as the failure branch **nor** discourage it - scanning and typing are peers (hard rule 15), and the whole point of the notice is to keep captures coming; (d) **never mid-capture and never on the Confirm sheet** - a notice between the shutter and the result is the one place it becomes a nag, and `ERRORS.md` already forbids interrupting there; (e) it is **not** an error state, so it gets no next-step action bar - though a "send us this case" path to the existing feedback endpoint (`API.md`) is the natural companion and should be decided explicitly rather than by omission. Placement, persistence and dismissal are open: decide them in the brief and record them in `ERRORS.md` / `SCREENMAP.md` in the same change | L4: the notice renders on the capture surface in EN **and** RU, dark + light, **not amber**, **not truncated** at default and at Dynamic Type XL - RU is where it breaks. L1: dismissal (if any) persists, and the notice never appears mid-capture or on Confirm. L1: no hardcoded string - the localization gate stays 0, **and** the `Text(_: String)` blind spot is avoided (a `String`-producing expression renders its English key while the gate still passes - four instances are recorded at the top of `L10n.swift`) |
| **[x]** P6.11 | **Tier and version responses: an old client meeting a newer server** (product owner, 2026-08-27). **The core half landed 2026-08-27** - status classification and the engine outcomes, with tests and a mutation; what remains is the SURFACE. **There is no Pro tier - every user has full access** - but the server will be upgraded while clients stay on older versions, so a client must handle a response class it never provokes itself: `402` (tier lacks quota), `429` (per-period), `426` (client `schemaVersion` below `minSupported`), and any future gate. Two defects exist today: **`426` is handled in core and dropped on the floor** (`SyncEngine` sets `outcome.upgradeRequired`; nothing in `App/Sources` reads it), and **`402` is misclassified as `transportUnavailable`**, so a tier-gated answer reads to the user as "server unreachable" - the generic lie `JOURNEYS.md` F7 forbids. **Framing, and it resolves a rule conflict**: hard rule 7 allows monetization in no error surface except the car-limit sheet, and `API.md` forbids an upsell mid-capture - so the copy is **version-first, not an upsell**: "This needs a newer version of Tankbook" with **update** as the next step, never "go Pro". That is also simply true while no tier exists. Nothing is ever lost: a refused push leaves the queue dirty exactly as S7 does, and no screen is gated (hard rule 1) | L1: `402`, `429` and `426` each map to a **distinct** client outcome and **none** of them to `transportUnavailable` - the mutation that matters is collapsing one into the transport case and watching no test fail. L1: an **unknown** 4xx from a future server degrades honestly and never as a generic failure; the local queue survives every one. L4: the surface names its next step (hard rule 7) in EN+RU, carries **no** upsell, and appears on the passive Settings row rather than as a modal (hard rule 8). L1: a refused push leaves records dirty and re-pushable |
| **[x]** P6.12 | **The catalog wire cannot express a removal, so `SYNC.md`'s full-pack rule is unimplementable on both sides.** `docs/SYNC.md` → Reference data states *"Entries the server no longer publishes are dropped on a full pack"*, but the shipped `GET /catalog` response (`{ packVersion, entries[] }`, P5.6) carries **no delta-vs-full marker and no tombstone**, so a client cannot tell "here is everything" from "here is what changed". Both halves independently reported the same gap from opposite ends: P5.6 said a removal is not representable, P5.7 said every pack is therefore consumed as an identity overlay. **Consequence, and it is narrow**: a fresh client (empty cache) lands exactly on the server's set - the important path - while a **stale** client that receives a full pack can retain an entry the server has since removed, and will keep offering it as an Add-car suggestion. Nothing is corrupted and no saved `Vehicle` is affected (catalog values are copied, never referenced by id), so this is wrong suggestions, not wrong data. Fix needs a **wire change** - a `full: true` marker, or an explicit removed-ids list - so it is a server change and a client change in one reviewed step (`API.md` breaking-change review) | L2 (server): a full pack is marked as such and a delta is not; publishing a pack that drops an entry is expressible. L1 (client): a full pack **replaces** the held set rather than overlaying it, so a server-removed entry disappears from suggestions; a delta still overlays. L1: a stale client that has missed several versions converges on exactly the server's set. **Vacuous trap**: testing removal only from an empty cache - that path already works today and proves nothing about the bug |
| P6.13 | **Russian clips at Dynamic Type XL where English does not.** Found by reading P5.3's committed XL screenshots, which is the only way it can be found - no test asserts appearance. On Home at XL the RU stat-tile label renders **«РАСХОДЫ ЗА А…»** while EN's "AUGUST SPEND" fits, because Russian runs 20-30% longer and short strings expand worst. `docs/DESIGN.md` → Accessibility floor makes full Dynamic Type support non-negotiable, so a clipped label is a floor violation, not a cosmetic nit. **Scope it honestly**: the entry row truncates in **both** languages at XL (`123 6… · Aug…` / `123… · 27 а…`), so that half is **pre-existing and not P5.3's doing** - fix it as its own thing rather than blaming the RU pass. Fix by letting the tile grow, shortening the RU label, or scaling it - **never by clipping**, and never by shortening EN to match. The affordance case is already correct and shows the target: at XL the parts rows wrap to two lines with `Связать` still reachable, so no next step is lost (hard rule 7) | L4: at Dynamic Type XL, dark, EN **and** RU - every stat-tile label renders in full, and the Home entry row's odometer and date are legible in both. **Vacuous trap**: asserting a label's text via the accessibility tree - XCUITest reads the string the view was given, not the pixels it drew, so a clipped label passes. This is a screenshot check, read by a human |
| **[x]** P6.14 | **Two blind spots in the L5 accuracy gate, both found by P2.11 rather than by reading it.** (a) **The ratchet scores only `liters`/`unitPrice`/`total`** (`CorpusABScorer.score`), so `fuelKind` and `currency` - both real extracted fields - are **invisible to it**. P2.11 fixed fuel-kind resolution on `receipt-034` and the receipts score stayed flat at 48/106, correctly. That is not P2.11's failure: an entire class of extraction correctness cannot move the number that guards extraction, which also means **P2.10 (KZT currency detection) will be equally invisible when it lands**. (b) **The "stored text is never normalised" invariant is structural, not tested.** P2.11's homoglyph canonicalisation must stay in the matching path so a station name survives as printed - but `FuelExtraction` carries **no free-text field at all** (every field is a number, an enum, or a numeric date string), so there is nothing for a leak to corrupt yet and no test can catch one. Verified by mutation: applying `canonicalKey` to the stored `date` passes the whole suite, because `27.08.2026` has no letters. The guarantee currently rests on where `canonicalKey` is *called*, which is true today and unenforced tomorrow | L5: extend the scorer to `fuelKind` and `currency` with their own per-class marks, so a fix in either can move a number - and **re-baseline in the same change**, since adding scored fields changes every total. L1: when an extracted **free-text** field lands (station/vendor), a test asserts it is byte-identical to what was printed while matching still canonicalises - that is the test P2.11 could not write. **Vacuous trap**: adding `fuelKind` to the scorer without re-baselining, which reads as a sudden accuracy collapse |
| **[x]** P6.15 | **Three defects in the import UI that every test passed over, found by opening the RU screenshots.** (a) **Double header on the preview**: a nav title "Импорт" sits above the screen's own "Просмотр импорта / Назад / Отмена" row - two stacked headers where `ImportPreview.dc.html` draws one. (b) **RU action labels hyphenate mid-word** on the review rows: «Испра-вить», «Импортиро-вать как есть», «Пропу-стить» - three actions do not fit one line in Russian, which is the documented 20-30% expansion doing exactly what it always does to short strings. A hyphen-broken next step is still readable, but it is the warning sign hard rule 7 cares about. (c) **A row that could not be parsed renders our own wire JSON** (`"entityType": "fillUp",`) instead of the original CSV line. `JOURNEYS.md` F6b says the raw line is the right answer for exactly this case - but it means the line **the user's file contained**, not an internal representation they have never seen | L4: preview has ONE header; the three review actions fit without mid-word hyphenation in RU at default Dynamic Type; an unreadable row shows the original delimited line. **Vacuous trap**: asserting the actions exist via the accessibility tree - it reads the string the view was given, not the pixels it drew, so a hyphen-broken label passes |
| **[cut]** P6.16 | ~~The app still sells a Pro tier that was cancelled.~~ **RESOLVED 2026-08-28: the Pro tier is DEFERRED, not cancelled.** The Settings row, `Route.paywall` and the car-limit sheet's "Free keeps up to 3 cars. Archive one, or go Pro." all **stay as they are** - they are early, not wrong. Nothing to build. Kept as a row because the question will look obvious again the next time someone reads P6.3's history and sees the tier removed from that row: it was removed from **P6.3's scope**, not from the product |
| P6.17 | **Two Russian defects in the anomaly dismiss sheet, both invisible to every test.** (a) **`Changed tyres` -> «Поменял шины» is masculine past tense** - a woman dismissing the card reads a form that misgenders her. The other three options are nouns (`Зима`, `Буксировка`, `Прочее`), so the gendered verb is also the odd one out; making it a noun phrase (**«Замена шин»**) fixes the gender and the inconsistency in one edit, and Russian has no genderless past tense, so a verb here cannot be repaired by translation. (b) **`Dismissing with a reason keeps it quiet for this period.` -> «Отклонение с причиной скроет подсказку на этот период.»** - **«отклонение» means *deviation*** in exactly this domain, and it sits directly beneath a card about a consumption deviation, so the sentence reads as "a deviation with a cause" rather than "dismissing with a reason". Propose **«Если укажете причину, подсказка скроется на этот период.»** This is the same class as P4.7's «с вашего телефон Android»: the grammar is legal and the meaning is wrong, and only reading the rendered screen finds it | L4: the dismiss sheet in RU uses no gendered verb form for any option, and no word that collides with the domain vocabulary; EN unchanged. **Vacuous trap**: asserting the key exists or the sheet renders - both defects pass every test that does not read the words |
| **[~]** P6.18 | **CORE + SURFACE DONE 2026-08-28 (P6.18a, P6.18b). NOT ticked: the EN+RU screenshots are outstanding, together with P6.7's. P6.18a: `AppVersion`, the `appUpdate` document key and `AppConfig.updateRequirement(runningVersion:)`, 862 tests (from 845). The SURFACE is the follow-up (P6.18b) - nothing in `ios/App/` reads the requirement yet, so it is computed and never shown, the same shape P6.8 and the config client were in.** **The update requirement: a launch-time check that never blocks the app** (product owner, 2026-08-28). Delivered as an **`appUpdate` key in the signed `GET /v1/config` document**, not a new endpoint - it inherits the Ed25519 signature, version monotonicity, `ETag`/`304`, the offline cache and the bundled fallback, none of which would be free on a second endpoint (`docs/CONFIG.md` -> "App version and the update notice", written 2026-08-28). **The server states two facts, the client derives one sign**: `minSupportedVersion` and `latestVersion` in, `.none` / `.recommended` (soft) / `.required` (hard) out - deliberately not a `severity` string, which could contradict its own thresholds. **Hard never means blocked** (hard rule 1): below `minSupportedVersion` sync, cloud extract and import parse are withheld and everything local - log, edit, recompute, export - keeps working offline indefinitely. The surface is **shared with P6.11's open `426` half**, because `426 upgrade_required` is the same statement made per-response; build them together or the user meets two different sentences for one condition. **No server text and no server URL** - copy from the String Catalog (hard rules 7 and 10), destination from a compiled-in App Store id; an "update now" prompt is a better phishing surface than a maintenance banner. Also closes the standing gap that **nothing in `ios/App/Sources` ever constructs `ConfigStore`** - config is fully built in core and never fetched, the same shape as P6.8's power policy | L1: the three requirement outcomes off the two thresholds, and the **mutation that matters is a lexicographic version compare** - `1.10.0` vs `1.9.0` is the only pair that separates it from numeric, so a test set without it is vacuous. L1 **fails open**: absent `appUpdate`, a malformed threshold, and a malformed `CFBundleShortVersionString` each resolve `.none`; the mutation is failing *closed*, which must break these. L1: under `.required`, a fill-up still saves, edits persist, recompute runs and export completes with the network down; the mutation is gating any local path on the requirement. L1: the config fetch is **never awaited by a view** - launch resolves from the held snapshot (live/cache/bundled); the mutation is awaiting the refresh before first draw. L4: the soft row (Settings -> About, dismissible) and the hard notice (names its next step, no upsell - hard rule 7) in **EN+RU**, dark; RU must not put the version number inside a case-governed phrase (`docs/LOCALIZATION.md`) |
| P6.5 | Accessibility audit | Automated: contrast, VoiceOver labels (metric+unit+trend), Dynamic Type XL snapshots; M: VoiceOver walkthrough of J3 |
| P6.6 | Store assets EN/RU, privacy labels, TestFlight ring | M: release checklist; **ERRORS.md coverage walk** – every catalogued state reachable and correct |

## W · Marketing site (`tankbook.live`)

*Authority: `docs/SITE.md`. Design reference: `design/screens/SiteLanding.dc.html`. Unlike the app
rows, these produce no Swift and no simulator run - the gate is `hugo` exiting 0 and a set of greps
that can each fail on their own.*

| ID | Task | Checks |
|---|---|---|
| **[~]** W0 | **Domain: `tankbook.live`.** Purchased 2026-08-28; **apex serves the site, `api.tankbook.live` is the backend**. `HostAllowlist.allowedDomain` and `Config.default.json` now name it, which **closes `CONFIG.md`'s release blocker** - both files previously carried an unowned `tankbook.app`, and an allowlist naming a domain we do not control is worse than none, since anyone could register it and inherit every guardrail's trust. **Still open: DNS does not resolve**, which blocks W4 and nothing else | `swift test` 845 exit 0 and `swiftlint` 0 after the rename. **Mutation:** reverting `allowedDomain` to the old value fails **8 of the 15** allowlist assertions - done, it does. The signed `parity.*` config fixtures were deliberately **left on the old domain**: the signature is over those exact bytes, the host in them is arbitrary test data, and re-signing to chase a rename would weaken a fixture that exists to catch canonicalization drift. Remaining: `dig +short tankbook.live` and `api.tankbook.live` both answer |
| **[~]** W1 | **Site skeleton + landing page, EN and RU** (SITE.md S1+S2). The token generator with its `--check` guard, `hugo.toml` with the multilingual and typographer blocks, layouts and partials, the three hand-written CSS files, and the seven landing sections in both languages. **Dispatched 2026-08-28** on `zai-coding-plan/glm-5.3-flash`; brief at `agents/briefs/W1.md` | `hugo --minify` exit 0; `generate-site-tokens.swift --check` exit 0, and **exit 1 after a token is changed** (the hand-edit guard, mutation-checked); zero `rel="stylesheet"` in the output; **no literal hex in any hand-written CSS**; no em-dash anywhere under `site/`; the forbidden-copy grep finds nothing; blanking an `alt` **fails the build** |
| W2 | **Legal pages, EN and RU** (S3): `/privacy/`, `/terms/`, `/support/`, `/delete-account/`. **Generated from behaviour, never from a template** - a generator's boilerplate will contradict the app, and the contradiction is what App Store review catches. Every statement traces to `SECURITY.md`, `LOGGING.md`, `SYNC.md` or `API.md`, per SITE.md's legal table | Each row of SITE.md's legal table appears in both languages: no account needed, never logs domain values, gateway images never retained, **import parse stored 30 days**, deletion is a tombstone and local data stays local, export always free. `grep -in 'subscription'` on `/terms/` finds **nothing** while the Pro tier is deferred. Contradiction against any of the four source docs reopens the task |
| W3 | **The SEO surface** (S4): the canonical/hreflang partial, multilingual sitemap, JSON-LD (`SoftwareApplication`, `FAQPage`, `Organization`), per-language OG images composed from real screenshots, favicon and touch icons | **Every** built page carries an hreflang set including `x-default` and a self-referencing canonical - a loop over `site/public/**/index.html` that exits 1 on the first page missing one; every JSON-LD block parses with `python3 -m json.tool`; `xmllint` validates all three sitemaps and `xhtml:link` alternates are present; the OG image referenced actually exists and measures 1200 px wide; **Lighthouse 100 on all four categories, measured locally** |
| **[~]** W4 | **THE SITE IS LIVE at https://tankbook.live (delivered 2026-08-28).** Self-hosted runner registered and building; apex serves 200 over HTTPS in ~0.5 s, HTTP redirects, RU serves, WebP is delivered (12 KB where the raw PNG was 332 KB), the RU 404 titles in Russian, and canonical/hreflang/x-default are correct in the wild. **www is deliberately absent** - no DNS record, no SAN, and it must stay that way or unattended renewal breaks. Verified by curling the running site, which is also how it was found that **no security header was being served at all** (nginx `add_header` does not inherit into a location that sets its own; fixed in `93991e9`). **Remaining: the two search consoles.** Checked 2026-08-28: `tankbook.live` is registered in **neither** Google Search Console nor Yandex.Webmaster, so a sitemap cannot be submitted until the property exists and is verified. Verification needs a token only the account owner can read. The cheapest route given this deploy: take the **HTML-file** option in each console, drop the file into `site/static/` (it lands at the site root), push - the path filter fires on `site/**` - and click verify. DNS TXT works too and survives a re-deploy, but the file route needs no registrar visit. Yandex matters as much as Google here: half the audience searches in Russian**, and re-copying the nginx config so the headers go live | Google Search Console and Yandex.Webmaster both list `https://tankbook.live/sitemap.xml`; `curl -sI https://tankbook.live/ \| grep -i strict-transport` returns the header |
| **[x]** W5 | `/roadmap/` and `/press/` page bodies, EN and RU. Roadmap states shipped / next / **deliberately cut**, which is where pump capture and CarPlay belong | Both pages in both sitemaps; the roadmap names nothing as shipped that is not; pump-display capture appears **only** here and never in the hero (it ships off, P2.7) |
| W6 | **`VISION.md` §2 contradicts hard rule 15.** It still reads "Camera is the primary input; the form is the fallback, not the default" and offers a "five seconds and zero typing" tagline. Hard rule 15 - written later, with corpus evidence - says typing is a **peer path, never a fallback**. Found while writing the landing copy, where it would have shipped an over-promise straight to customers | `VISION.md` §2 no longer contradicts hard rule 15; the corpus numbers that forced the rule (receipts 38.3%, pump 0%, QR on 9 of 16) are cited at the point of the claim |
| W7 | **`design/screens/support.js` is missing** while every `.dc.html` artboard references `./support.js`. Pre-existing, and it means no artboard can be verified locally in a browser - `SiteLanding.dc.html` could only be checked by rendering it in Chrome, where the custom element degrades | Either the file is restored to the repo, or the artboards stop referencing it, or `design/screens/README` records that the canvas tool supplies it and local rendering is expected to degrade |

| **[~]** W8 | **FIXED 2026-08-28 alongside P6.7: light `headlight` moved to a value clearing 4.5:1 on both light grounds (5.62:1 and 6.08:1), pinned by a test that fails at the old value with the exact 4.217:1. Screenshots outstanding with P6.7.** **The light-theme tokens fail WCAG AA, and this is an APP bug before it is a site bug.** Computed from `design/tokens.json`: light `headlight` on light `midnight` is **4.22:1** and light `midnight` on light `taillight` is **4.32:1**, both under the 4.5:1 floor `DESIGN.md` sets. The site's button was fixed token-legally (foreground moved to `--dash`, 4.67:1), but the **link colour cannot be fixed without changing the token**, and that token is shared with the iOS app - so the same pair is likely failing in the app's light theme, where no test looks. Changing it means re-verifying the app and reconciling `SiteLanding.dc.html`, which draws the failing pair | Contrast computed, not eyeballed, for every token pair in **both** themes, reported as numbers; the app's light theme re-checked at the same pairs; the artboard updated in the same change |
| W9 | **Russian copy defects on the site**, found by reading the rendered pages. `roadmap.ru.md` «по желательному аккаунту» means *the account we recommend*, not *optional*; `privacy.ru.md` «устроен локально-первым» is a calque that cannot attach grammatically; `support.ru.md` «приложить сам случай» - you attach a file, not a case; `_index.ru.md` «голубым фар» is broken agreement; **электрокар** and **электромобиль** are used for the same thing on one site; and the hero caption quotes the app's button as «Ввести» when the real label is «Ввести вручную» - a caption whose entire point is "this is the real app" misquoting it. Held pending the product owner, who asked that texts stay as they are for now | A native read of both rendered pages; one term for EV site-wide; the caption matches `Localizable.xcstrings` exactly |
| W10 | **`SITE.md` states a rule its own artboard breaks.** The doc makes "two CTAs of equal weight" a design rule; `SiteLanding.dc.html` draws the hero CTAs primary/secondary and the built site follows the artboard. Hard rule 15 is **not** implicated - these two CTAs are mailto vs roadmap, not the two entry paths, and the doors section carries the entry-path equality correctly. But a doc rule the shipped design contradicts is exactly the drift `CLAUDE.md`'s conflict rule says to fix in the same change | `SITE.md` and the artboard agree; whichever way it resolves, the reason is written down |

| **[x]** W11 | **TLS renewal is automatic** (done 2026-08-28). The certificate was first issued with `certbot --manual` (DNS-01 by hand), which certbot itself said would never renew - the timer cannot repeat a manual challenge. Re-issued over `--webroot` with `--cert-name tankbook.live` so the lineage is reused rather than forked, and `--deploy-hook "systemctl reload nginx"` so nginx actually picks up the new certificate instead of serving the expired one until a human reloads it. Procedure and the failure modes behind each flag: `deploy/nginx/RENEWAL.md` | The check is `sudo certbot renew --dry-run` **succeeding** - a rehearsal against the staging server, not a config that looks right. Re-run it after any change to the challenge method, and after the first real unattended renewal confirm the served dates actually moved: `echo \| openssl s_client -connect tankbook.live:443 -servername tankbook.live \| openssl x509 -noout -dates` |

**`api.tankbook.live` does not answer yet, and that is intentional** (product owner, 2026-08-28). nginx proxies it and returns
**502** because no backend is listening on the upstream port. Postponed deliberately, not broken: the app is local-first, so an
unreachable API costs sync, cloud extract and import parse and **nothing else** - every screen keeps working offline (hard rule 1).
Two consequences worth knowing while it stays down: the config guardrail's `GET /health` probe cannot promote a candidate
`apiBaseUrl`, so the bundled default stands; and P6.18's update requirement resolves `.none`, because a config document that never
arrives carries no `appUpdate` - which is the fail-open rule behaving exactly as designed.

**Two open product decisions, neither of them engineering:** the analytics choice (none / the CDN
operator's own / a hosted cookieless service - see `SITE.md`), and whether **RU 152-ФЗ** obligations
apply alongside GDPR. W2 ships GDPR-framed with the second question tracked, not guessed at.

**Three Russian copy items** found by reading the rendered artboard, held pending the product owner:
`БЕСПЛАТНО … БЕСПЛАТНЫЙ` repeats a root in one strip; `Топливо, зарядки, сервис и всё остальное
хранится` takes a singular verb on a plural list; `как удобно` reads clipped against `как вам удобно`.

## Working rules

- One task = one PR = code + its checks; the PR description quotes the task ID and its check list, ticked.
- A task discovering an undocumented behavior extends the owning doc in the same PR (CLAUDE.md rule).
- Snapshot baselines are reviewed like code; accuracy high-water marks only ratchet up.
