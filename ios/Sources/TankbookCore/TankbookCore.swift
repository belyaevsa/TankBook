/// Container for the Tankbook core module.
///
/// Placeholder only - the domain model lands in P0.4-P0.7. `schemaVersion`
/// tracks the backup format version (docs/SCHEMA.md, Backup format).
public enum TankbookCore {
    public static let schemaVersion = 1
}
